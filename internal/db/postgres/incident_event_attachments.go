// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// IncidentEventAttachment is an object representing the database table.
type IncidentEventAttachment struct {
	ID          string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	EventID     string      `boil:"event_id" json:"event_id" toml:"event_id" yaml:"event_id"`
	WorkspaceID string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	Kind        string      `boil:"kind" json:"kind" toml:"kind" yaml:"kind"`
	Label       string      `boil:"label" json:"label" toml:"label" yaml:"label"`
	URL         string      `boil:"url" json:"url" toml:"url" yaml:"url"`
	Body        string      `boil:"body" json:"body" toml:"body" yaml:"body"`
	ObjectKey   string      `boil:"object_key" json:"object_key" toml:"object_key" yaml:"object_key"`
	ContentType string      `boil:"content_type" json:"content_type" toml:"content_type" yaml:"content_type"`
	SizeBytes   int64       `boil:"size_bytes" json:"size_bytes" toml:"size_bytes" yaml:"size_bytes"`
	CreatedAt   time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	CreatedBy   null.String `boil:"created_by" json:"created_by,omitempty" toml:"created_by" yaml:"created_by,omitempty"`

	R *incidentEventAttachmentR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L incidentEventAttachmentL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var IncidentEventAttachmentColumns = struct {
	ID          string
	EventID     string
	WorkspaceID string
	Kind        string
	Label       string
	URL         string
	Body        string
	ObjectKey   string
	ContentType string
	SizeBytes   string
	CreatedAt   string
	CreatedBy   string
}{
	ID:          "id",
	EventID:     "event_id",
	WorkspaceID: "workspace_id",
	Kind:        "kind",
	Label:       "label",
	URL:         "url",
	Body:        "body",
	ObjectKey:   "object_key",
	ContentType: "content_type",
	SizeBytes:   "size_bytes",
	CreatedAt:   "created_at",
	CreatedBy:   "created_by",
}

var IncidentEventAttachmentTableColumns = struct {
	ID          string
	EventID     string
	WorkspaceID string
	Kind        string
	Label       string
	URL         string
	Body        string
	ObjectKey   string
	ContentType string
	SizeBytes   string
	CreatedAt   string
	CreatedBy   string
}{
	ID:          "incident_event_attachments.id",
	EventID:     "incident_event_attachments.event_id",
	WorkspaceID: "incident_event_attachments.workspace_id",
	Kind:        "incident_event_attachments.kind",
	Label:       "incident_event_attachments.label",
	URL:         "incident_event_attachments.url",
	Body:        "incident_event_attachments.body",
	ObjectKey:   "incident_event_attachments.object_key",
	ContentType: "incident_event_attachments.content_type",
	SizeBytes:   "incident_event_attachments.size_bytes",
	CreatedAt:   "incident_event_attachments.created_at",
	CreatedBy:   "incident_event_attachments.created_by",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var IncidentEventAttachmentWhere = struct {
	ID          whereHelperstring
	EventID     whereHelperstring
	WorkspaceID whereHelperstring
	Kind        whereHelperstring
	Label       whereHelperstring
	URL         whereHelperstring
	Body        whereHelperstring
	ObjectKey   whereHelperstring
	ContentType whereHelperstring
	SizeBytes   whereHelperint64
	CreatedAt   whereHelpertime_Time
	CreatedBy   whereHelpernull_String
}{
	ID:          whereHelperstring{field: "\"incident_event_attachments\".\"id\""},
	EventID:     whereHelperstring{field: "\"incident_event_attachments\".\"event_id\""},
	WorkspaceID: whereHelperstring{field: "\"incident_event_attachments\".\"workspace_id\""},
	Kind:        whereHelperstring{field: "\"incident_event_attachments\".\"kind\""},
	Label:       whereHelperstring{field: "\"incident_event_attachments\".\"label\""},
	URL:         whereHelperstring{field: "\"incident_event_attachments\".\"url\""},
	Body:        whereHelperstring{field: "\"incident_event_attachments\".\"body\""},
	ObjectKey:   whereHelperstring{field: "\"incident_event_attachments\".\"object_key\""},
	ContentType: whereHelperstring{field: "\"incident_event_attachments\".\"content_type\""},
	SizeBytes:   whereHelperint64{field: "\"incident_event_attachments\".\"size_bytes\""},
	CreatedAt:   whereHelpertime_Time{field: "\"incident_event_attachments\".\"created_at\""},
	CreatedBy:   whereHelpernull_String{field: "\"incident_event_attachments\".\"created_by\""},
}

// IncidentEventAttachmentRels is where relationship names are stored.
var IncidentEventAttachmentRels = struct {
	CreatedByUser string
}{
	CreatedByUser: "CreatedByUser",
}

// incidentEventAttachmentR is where relationships are stored.
type incidentEventAttachmentR struct {
	CreatedByUser *User `boil:"CreatedByUser" json:"CreatedByUser" toml:"CreatedByUser" yaml:"CreatedByUser"`
}

// NewStruct creates a new relationship struct
func (*incidentEventAttachmentR) NewStruct() *incidentEventAttachmentR {
	return &incidentEventAttachmentR{}
}

func (o *IncidentEventAttachment) GetCreatedByUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByUser()
}

func (r *incidentEventAttachmentR) GetCreatedByUser() *User {
	if r == nil {
		return nil
	}

	return r.CreatedByUser
}

// incidentEventAttachmentL is where Load methods for each relationship are stored.
type incidentEventAttachmentL struct{}

var (
	incidentEventAttachmentAllColumns            = []string{"id", "event_id", "workspace_id", "kind", "label", "url", "body", "object_key", "content_type", "size_bytes", "created_at", "created_by"}
	incidentEventAttachmentColumnsWithoutDefault = []string{"event_id", "workspace_id", "kind"}
	incidentEventAttachmentColumnsWithDefault    = []string{"id", "label", "url", "body", "object_key", "content_type", "size_bytes", "created_at", "created_by"}
	incidentEventAttachmentPrimaryKeyColumns     = []string{"id"}
	incidentEventAttachmentGeneratedColumns      = []string{}
)

type (
	// IncidentEventAttachmentSlice is an alias for a slice of pointers to IncidentEventAttachment.
	// This should almost always be used instead of []IncidentEventAttachment.
	IncidentEventAttachmentSlice []*IncidentEventAttachment
	// IncidentEventAttachmentHook is the signature for custom IncidentEventAttachment hook methods
	IncidentEventAttachmentHook func(context.Context, boil.ContextExecutor, *IncidentEventAttachment) error

	incidentEventAttachmentQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	incidentEventAttachmentType                 = reflect.TypeOf(&IncidentEventAttachment{})
	incidentEventAttachmentMapping              = queries.MakeStructMapping(incidentEventAttachmentType)
	incidentEventAttachmentPrimaryKeyMapping, _ = queries.BindMapping(incidentEventAttachmentType, incidentEventAttachmentMapping, incidentEventAttachmentPrimaryKeyColumns)
	incidentEventAttachmentInsertCacheMut       sync.RWMutex
	incidentEventAttachmentInsertCache          = make(map[string]insertCache)
	incidentEventAttachmentUpdateCacheMut       sync.RWMutex
	incidentEventAttachmentUpdateCache          = make(map[string]updateCache)
	incidentEventAttachmentUpsertCacheMut       sync.RWMutex
	incidentEventAttachmentUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var incidentEventAttachmentAfterSelectMu sync.Mutex
var incidentEventAttachmentAfterSelectHooks []IncidentEventAttachmentHook

var incidentEventAttachmentBeforeInsertMu sync.Mutex
var incidentEventAttachmentBeforeInsertHooks []IncidentEventAttachmentHook
var incidentEventAttachmentAfterInsertMu sync.Mutex
var incidentEventAttachmentAfterInsertHooks []IncidentEventAttachmentHook

var incidentEventAttachmentBeforeUpdateMu sync.Mutex
var incidentEventAttachmentBeforeUpdateHooks []IncidentEventAttachmentHook
var incidentEventAttachmentAfterUpdateMu sync.Mutex
var incidentEventAttachmentAfterUpdateHooks []IncidentEventAttachmentHook

var incidentEventAttachmentBeforeDeleteMu sync.Mutex
var incidentEventAttachmentBeforeDeleteHooks []IncidentEventAttachmentHook
var incidentEventAttachmentAfterDeleteMu sync.Mutex
var incidentEventAttachmentAfterDeleteHooks []IncidentEventAttachmentHook

var incidentEventAttachmentBeforeUpsertMu sync.Mutex
var incidentEventAttachmentBeforeUpsertHooks []IncidentEventAttachmentHook
var incidentEventAttachmentAfterUpsertMu sync.Mutex
var incidentEventAttachmentAfterUpsertHooks []IncidentEventAttachmentHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *IncidentEventAttachment) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventAttachmentAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *IncidentEventAttachment) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventAttachmentBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *IncidentEventAttachment) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventAttachmentAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *IncidentEventAttachment) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventAttachmentBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *IncidentEventAttachment) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventAttachmentAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *IncidentEventAttachment) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventAttachmentBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *IncidentEventAttachment) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventAttachmentAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *IncidentEventAttachment) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventAttachmentBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *IncidentEventAttachment) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventAttachmentAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddIncidentEventAttachmentHook registers your hook function for all future operations.
func AddIncidentEventAttachmentHook(hookPoint boil.HookPoint, incidentEventAttachmentHook IncidentEventAttachmentHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		incidentEventAttachmentAfterSelectMu.Lock()
		incidentEventAttachmentAfterSelectHooks = append(incidentEventAttachmentAfterSelectHooks, incidentEventAttachmentHook)
		incidentEventAttachmentAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		incidentEventAttachmentBeforeInsertMu.Lock()
		incidentEventAttachmentBeforeInsertHooks = append(incidentEventAttachmentBeforeInsertHooks, incidentEventAttachmentHook)
		incidentEventAttachmentBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		incidentEventAttachmentAfterInsertMu.Lock()
		incidentEventAttachmentAfterInsertHooks = append(incidentEventAttachmentAfterInsertHooks, incidentEventAttachmentHook)
		incidentEventAttachmentAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		incidentEventAttachmentBeforeUpdateMu.Lock()
		incidentEventAttachmentBeforeUpdateHooks = append(incidentEventAttachmentBeforeUpdateHooks, incidentEventAttachmentHook)
		incidentEventAttachmentBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		incidentEventAttachmentAfterUpdateMu.Lock()
		incidentEventAttachmentAfterUpdateHooks = append(incidentEventAttachmentAfterUpdateHooks, incidentEventAttachmentHook)
		incidentEventAttachmentAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		incidentEventAttachmentBeforeDeleteMu.Lock()
		incidentEventAttachmentBeforeDeleteHooks = append(incidentEventAttachmentBeforeDeleteHooks, incidentEventAttachmentHook)
		incidentEventAttachmentBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		incidentEventAttachmentAfterDeleteMu.Lock()
		incidentEventAttachmentAfterDeleteHooks = append(incidentEventAttachmentAfterDeleteHooks, incidentEventAttachmentHook)
		incidentEventAttachmentAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		incidentEventAttachmentBeforeUpsertMu.Lock()
		incidentEventAttachmentBeforeUpsertHooks = append(incidentEventAttachmentBeforeUpsertHooks, incidentEventAttachmentHook)
		incidentEventAttachmentBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		incidentEventAttachmentAfterUpsertMu.Lock()
		incidentEventAttachmentAfterUpsertHooks = append(incidentEventAttachmentAfterUpsertHooks, incidentEventAttachmentHook)
		incidentEventAttachmentAfterUpsertMu.Unlock()
	}
}

// One returns a single incidentEventAttachment record from the query.
func (q incidentEventAttachmentQuery) One(ctx context.Context, exec boil.ContextExecutor) (*IncidentEventAttachment, error) {
	o := &IncidentEventAttachment{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for incident_event_attachments")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all IncidentEventAttachment records from the query.
func (q incidentEventAttachmentQuery) All(ctx context.Context, exec boil.ContextExecutor) (IncidentEventAttachmentSlice, error) {
	var o []*IncidentEventAttachment

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to IncidentEventAttachment slice")
	}

	if len(incidentEventAttachmentAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all IncidentEventAttachment records in the query.
func (q incidentEventAttachmentQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count incident_event_attachments rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q incidentEventAttachmentQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if incident_event_attachments exists")
	}

	return count > 0, nil
}

// CreatedByUser pointed to by the foreign key.
func (o *IncidentEventAttachment) CreatedByUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CreatedBy),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadCreatedByUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (incidentEventAttachmentL) LoadCreatedByUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncidentEventAttachment any, mods queries.Applicator) error {
	var slice []*IncidentEventAttachment
	var object *IncidentEventAttachment

	if singular {
		var ok bool
		object, ok = maybeIncidentEventAttachment.(*IncidentEventAttachment)
		if !ok {
			object = new(IncidentEventAttachment)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncidentEventAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncidentEventAttachment))
			}
		}
	} else {
		s, ok := maybeIncidentEventAttachment.(*[]*IncidentEventAttachment)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncidentEventAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncidentEventAttachment))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incidentEventAttachmentR{}
		}
		if !queries.IsNil(object.CreatedBy) {
			args[object.CreatedBy] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incidentEventAttachmentR{}
			}

			if !queries.IsNil(obj.CreatedBy) {
				args[obj.CreatedBy] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CreatedByUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.CreatedByIncidentEventAttachments = append(foreign.R.CreatedByIncidentEventAttachments, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.CreatedBy, foreign.ID) {
				local.R.CreatedByUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.CreatedByIncidentEventAttachments = append(foreign.R.CreatedByIncidentEventAttachments, local)
				break
			}
		}
	}

	return nil
}

// SetCreatedByUser of the incidentEventAttachment to the related item.
// Sets o.R.CreatedByUser to related.
// Adds o to related.R.CreatedByIncidentEventAttachments.
func (o *IncidentEventAttachment) SetCreatedByUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"incident_event_attachments\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
		strmangle.WhereClause("\"", "\"", 2, incidentEventAttachmentPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.CreatedBy, related.ID)
	if o.R == nil {
		o.R = &incidentEventAttachmentR{
			CreatedByUser: related,
		}
	} else {
		o.R.CreatedByUser = related
	}

	if related.R == nil {
		related.R = &userR{
			CreatedByIncidentEventAttachments: IncidentEventAttachmentSlice{o},
		}
	} else {
		related.R.CreatedByIncidentEventAttachments = append(related.R.CreatedByIncidentEventAttachments, o)
	}

	return nil
}

// RemoveCreatedByUser relationship.
// Sets o.R.CreatedByUser to nil.
// Removes o from all passed in related items' relationships struct.
func (o *IncidentEventAttachment) RemoveCreatedByUser(ctx context.Context, exec boil.ContextExecutor, related *User) error {
	var err error

	queries.SetScanner(&o.CreatedBy, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("created_by")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.CreatedByUser = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.CreatedByIncidentEventAttachments {
		if queries.Equal(o.CreatedBy, ri.CreatedBy) {
			continue
		}

		ln := len(related.R.CreatedByIncidentEventAttachments)
		if ln > 1 && i < ln-1 {
			related.R.CreatedByIncidentEventAttachments[i] = related.R.CreatedByIncidentEventAttachments[ln-1]
		}
		related.R.CreatedByIncidentEventAttachments = related.R.CreatedByIncidentEventAttachments[:ln-1]
		break
	}
	return nil
}

// IncidentEventAttachments retrieves all the records using an executor.
func IncidentEventAttachments(mods ...qm.QueryMod) incidentEventAttachmentQuery {
	mods = append(mods, qm.From("\"incident_event_attachments\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"incident_event_attachments\".*"})
	}

	return incidentEventAttachmentQuery{q}
}

// FindIncidentEventAttachment retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindIncidentEventAttachment(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*IncidentEventAttachment, error) {
	incidentEventAttachmentObj := &IncidentEventAttachment{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"incident_event_attachments\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, incidentEventAttachmentObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from incident_event_attachments")
	}

	if err = incidentEventAttachmentObj.doAfterSelectHooks(ctx, exec); err != nil {
		return incidentEventAttachmentObj, err
	}

	return incidentEventAttachmentObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *IncidentEventAttachment) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no incident_event_attachments provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incidentEventAttachmentColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	incidentEventAttachmentInsertCacheMut.RLock()
	cache, cached := incidentEventAttachmentInsertCache[key]
	incidentEventAttachmentInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			incidentEventAttachmentAllColumns,
			incidentEventAttachmentColumnsWithDefault,
			incidentEventAttachmentColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(incidentEventAttachmentType, incidentEventAttachmentMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(incidentEventAttachmentType, incidentEventAttachmentMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"incident_event_attachments\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"incident_event_attachments\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into incident_event_attachments")
	}

	if !cached {
		incidentEventAttachmentInsertCacheMut.Lock()
		incidentEventAttachmentInsertCache[key] = cache
		incidentEventAttachmentInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the IncidentEventAttachment.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *IncidentEventAttachment) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	incidentEventAttachmentUpdateCacheMut.RLock()
	cache, cached := incidentEventAttachmentUpdateCache[key]
	incidentEventAttachmentUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			incidentEventAttachmentAllColumns,
			incidentEventAttachmentPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update incident_event_attachments, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"incident_event_attachments\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, incidentEventAttachmentPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(incidentEventAttachmentType, incidentEventAttachmentMapping, append(wl, incidentEventAttachmentPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update incident_event_attachments row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for incident_event_attachments")
	}

	if !cached {
		incidentEventAttachmentUpdateCacheMut.Lock()
		incidentEventAttachmentUpdateCache[key] = cache
		incidentEventAttachmentUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q incidentEventAttachmentQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for incident_event_attachments")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for incident_event_attachments")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o IncidentEventAttachmentSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incidentEventAttachmentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"incident_event_attachments\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, incidentEventAttachmentPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in incidentEventAttachment slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all incidentEventAttachment")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *IncidentEventAttachment) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no incident_event_attachments provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incidentEventAttachmentColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	incidentEventAttachmentUpsertCacheMut.RLock()
	cache, cached := incidentEventAttachmentUpsertCache[key]
	incidentEventAttachmentUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			incidentEventAttachmentAllColumns,
			incidentEventAttachmentColumnsWithDefault,
			incidentEventAttachmentColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			incidentEventAttachmentAllColumns,
			incidentEventAttachmentPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert incident_event_attachments, could not build update column list")
		}

		ret := strmangle.SetComplement(incidentEventAttachmentAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(incidentEventAttachmentPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert incident_event_attachments, could not build conflict column list")
			}

			conflict = make([]string, len(incidentEventAttachmentPrimaryKeyColumns))
			copy(conflict, incidentEventAttachmentPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"incident_event_attachments\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(incidentEventAttachmentType, incidentEventAttachmentMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(incidentEventAttachmentType, incidentEventAttachmentMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert incident_event_attachments")
	}

	if !cached {
		incidentEventAttachmentUpsertCacheMut.Lock()
		incidentEventAttachmentUpsertCache[key] = cache
		incidentEventAttachmentUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single IncidentEventAttachment record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *IncidentEventAttachment) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no IncidentEventAttachment provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), incidentEventAttachmentPrimaryKeyMapping)
	sql := "DELETE FROM \"incident_event_attachments\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from incident_event_attachments")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for incident_event_attachments")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q incidentEventAttachmentQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no incidentEventAttachmentQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from incident_event_attachments")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for incident_event_attachments")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o IncidentEventAttachmentSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(incidentEventAttachmentBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incidentEventAttachmentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"incident_event_attachments\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incidentEventAttachmentPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from incidentEventAttachment slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for incident_event_attachments")
	}

	if len(incidentEventAttachmentAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *IncidentEventAttachment) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindIncidentEventAttachment(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *IncidentEventAttachmentSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := IncidentEventAttachmentSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incidentEventAttachmentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"incident_event_attachments\".* FROM \"incident_event_attachments\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incidentEventAttachmentPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in IncidentEventAttachmentSlice")
	}

	*o = slice

	return nil
}

// IncidentEventAttachmentExists checks if the IncidentEventAttachment row exists.
func IncidentEventAttachmentExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"incident_event_attachments\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if incident_event_attachments exists")
	}

	return exists, nil
}

// Exists checks if the IncidentEventAttachment row exists.
func (o *IncidentEventAttachment) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return IncidentEventAttachmentExists(ctx, exec, o.ID)
}
