// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// AlertIngestFailure is an object representing the database table.
type AlertIngestFailure struct {
	ID          string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	SourceID    null.String `boil:"source_id" json:"source_id,omitempty" toml:"source_id" yaml:"source_id,omitempty"`
	Reason      string      `boil:"reason" json:"reason" toml:"reason" yaml:"reason"`
	Detail      string      `boil:"detail" json:"detail" toml:"detail" yaml:"detail"`
	Payload     string      `boil:"payload" json:"payload" toml:"payload" yaml:"payload"`
	At          time.Time   `boil:"at" json:"at" toml:"at" yaml:"at"`

	R *alertIngestFailureR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L alertIngestFailureL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AlertIngestFailureColumns = struct {
	ID          string
	WorkspaceID string
	SourceID    string
	Reason      string
	Detail      string
	Payload     string
	At          string
}{
	ID:          "id",
	WorkspaceID: "workspace_id",
	SourceID:    "source_id",
	Reason:      "reason",
	Detail:      "detail",
	Payload:     "payload",
	At:          "at",
}

var AlertIngestFailureTableColumns = struct {
	ID          string
	WorkspaceID string
	SourceID    string
	Reason      string
	Detail      string
	Payload     string
	At          string
}{
	ID:          "alert_ingest_failures.id",
	WorkspaceID: "alert_ingest_failures.workspace_id",
	SourceID:    "alert_ingest_failures.source_id",
	Reason:      "alert_ingest_failures.reason",
	Detail:      "alert_ingest_failures.detail",
	Payload:     "alert_ingest_failures.payload",
	At:          "alert_ingest_failures.at",
}

// Generated where

var AlertIngestFailureWhere = struct {
	ID          whereHelperstring
	WorkspaceID whereHelperstring
	SourceID    whereHelpernull_String
	Reason      whereHelperstring
	Detail      whereHelperstring
	Payload     whereHelperstring
	At          whereHelpertime_Time
}{
	ID:          whereHelperstring{field: "\"alert_ingest_failures\".\"id\""},
	WorkspaceID: whereHelperstring{field: "\"alert_ingest_failures\".\"workspace_id\""},
	SourceID:    whereHelpernull_String{field: "\"alert_ingest_failures\".\"source_id\""},
	Reason:      whereHelperstring{field: "\"alert_ingest_failures\".\"reason\""},
	Detail:      whereHelperstring{field: "\"alert_ingest_failures\".\"detail\""},
	Payload:     whereHelperstring{field: "\"alert_ingest_failures\".\"payload\""},
	At:          whereHelpertime_Time{field: "\"alert_ingest_failures\".\"at\""},
}

// AlertIngestFailureRels is where relationship names are stored.
var AlertIngestFailureRels = struct {
	Source    string
	Workspace string
}{
	Source:    "Source",
	Workspace: "Workspace",
}

// alertIngestFailureR is where relationships are stored.
type alertIngestFailureR struct {
	Source    *AlertSource `boil:"Source" json:"Source" toml:"Source" yaml:"Source"`
	Workspace *Workspace   `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
}

// NewStruct creates a new relationship struct
func (*alertIngestFailureR) NewStruct() *alertIngestFailureR {
	return &alertIngestFailureR{}
}

func (o *AlertIngestFailure) GetSource() *AlertSource {
	if o == nil {
		return nil
	}

	return o.R.GetSource()
}

func (r *alertIngestFailureR) GetSource() *AlertSource {
	if r == nil {
		return nil
	}

	return r.Source
}

func (o *AlertIngestFailure) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *alertIngestFailureR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

// alertIngestFailureL is where Load methods for each relationship are stored.
type alertIngestFailureL struct{}

var (
	alertIngestFailureAllColumns            = []string{"id", "workspace_id", "source_id", "reason", "detail", "payload", "at"}
	alertIngestFailureColumnsWithoutDefault = []string{"workspace_id", "reason"}
	alertIngestFailureColumnsWithDefault    = []string{"id", "source_id", "detail", "payload", "at"}
	alertIngestFailurePrimaryKeyColumns     = []string{"id"}
	alertIngestFailureGeneratedColumns      = []string{}
)

type (
	// AlertIngestFailureSlice is an alias for a slice of pointers to AlertIngestFailure.
	// This should almost always be used instead of []AlertIngestFailure.
	AlertIngestFailureSlice []*AlertIngestFailure
	// AlertIngestFailureHook is the signature for custom AlertIngestFailure hook methods
	AlertIngestFailureHook func(context.Context, boil.ContextExecutor, *AlertIngestFailure) error

	alertIngestFailureQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	alertIngestFailureType                 = reflect.TypeOf(&AlertIngestFailure{})
	alertIngestFailureMapping              = queries.MakeStructMapping(alertIngestFailureType)
	alertIngestFailurePrimaryKeyMapping, _ = queries.BindMapping(alertIngestFailureType, alertIngestFailureMapping, alertIngestFailurePrimaryKeyColumns)
	alertIngestFailureInsertCacheMut       sync.RWMutex
	alertIngestFailureInsertCache          = make(map[string]insertCache)
	alertIngestFailureUpdateCacheMut       sync.RWMutex
	alertIngestFailureUpdateCache          = make(map[string]updateCache)
	alertIngestFailureUpsertCacheMut       sync.RWMutex
	alertIngestFailureUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var alertIngestFailureAfterSelectMu sync.Mutex
var alertIngestFailureAfterSelectHooks []AlertIngestFailureHook

var alertIngestFailureBeforeInsertMu sync.Mutex
var alertIngestFailureBeforeInsertHooks []AlertIngestFailureHook
var alertIngestFailureAfterInsertMu sync.Mutex
var alertIngestFailureAfterInsertHooks []AlertIngestFailureHook

var alertIngestFailureBeforeUpdateMu sync.Mutex
var alertIngestFailureBeforeUpdateHooks []AlertIngestFailureHook
var alertIngestFailureAfterUpdateMu sync.Mutex
var alertIngestFailureAfterUpdateHooks []AlertIngestFailureHook

var alertIngestFailureBeforeDeleteMu sync.Mutex
var alertIngestFailureBeforeDeleteHooks []AlertIngestFailureHook
var alertIngestFailureAfterDeleteMu sync.Mutex
var alertIngestFailureAfterDeleteHooks []AlertIngestFailureHook

var alertIngestFailureBeforeUpsertMu sync.Mutex
var alertIngestFailureBeforeUpsertHooks []AlertIngestFailureHook
var alertIngestFailureAfterUpsertMu sync.Mutex
var alertIngestFailureAfterUpsertHooks []AlertIngestFailureHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AlertIngestFailure) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestFailureAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AlertIngestFailure) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestFailureBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AlertIngestFailure) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestFailureAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AlertIngestFailure) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestFailureBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AlertIngestFailure) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestFailureAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AlertIngestFailure) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestFailureBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AlertIngestFailure) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestFailureAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AlertIngestFailure) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestFailureBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AlertIngestFailure) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestFailureAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAlertIngestFailureHook registers your hook function for all future operations.
func AddAlertIngestFailureHook(hookPoint boil.HookPoint, alertIngestFailureHook AlertIngestFailureHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		alertIngestFailureAfterSelectMu.Lock()
		alertIngestFailureAfterSelectHooks = append(alertIngestFailureAfterSelectHooks, alertIngestFailureHook)
		alertIngestFailureAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		alertIngestFailureBeforeInsertMu.Lock()
		alertIngestFailureBeforeInsertHooks = append(alertIngestFailureBeforeInsertHooks, alertIngestFailureHook)
		alertIngestFailureBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		alertIngestFailureAfterInsertMu.Lock()
		alertIngestFailureAfterInsertHooks = append(alertIngestFailureAfterInsertHooks, alertIngestFailureHook)
		alertIngestFailureAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		alertIngestFailureBeforeUpdateMu.Lock()
		alertIngestFailureBeforeUpdateHooks = append(alertIngestFailureBeforeUpdateHooks, alertIngestFailureHook)
		alertIngestFailureBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		alertIngestFailureAfterUpdateMu.Lock()
		alertIngestFailureAfterUpdateHooks = append(alertIngestFailureAfterUpdateHooks, alertIngestFailureHook)
		alertIngestFailureAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		alertIngestFailureBeforeDeleteMu.Lock()
		alertIngestFailureBeforeDeleteHooks = append(alertIngestFailureBeforeDeleteHooks, alertIngestFailureHook)
		alertIngestFailureBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		alertIngestFailureAfterDeleteMu.Lock()
		alertIngestFailureAfterDeleteHooks = append(alertIngestFailureAfterDeleteHooks, alertIngestFailureHook)
		alertIngestFailureAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		alertIngestFailureBeforeUpsertMu.Lock()
		alertIngestFailureBeforeUpsertHooks = append(alertIngestFailureBeforeUpsertHooks, alertIngestFailureHook)
		alertIngestFailureBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		alertIngestFailureAfterUpsertMu.Lock()
		alertIngestFailureAfterUpsertHooks = append(alertIngestFailureAfterUpsertHooks, alertIngestFailureHook)
		alertIngestFailureAfterUpsertMu.Unlock()
	}
}

// One returns a single alertIngestFailure record from the query.
func (q alertIngestFailureQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AlertIngestFailure, error) {
	o := &AlertIngestFailure{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for alert_ingest_failures")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AlertIngestFailure records from the query.
func (q alertIngestFailureQuery) All(ctx context.Context, exec boil.ContextExecutor) (AlertIngestFailureSlice, error) {
	var o []*AlertIngestFailure

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to AlertIngestFailure slice")
	}

	if len(alertIngestFailureAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AlertIngestFailure records in the query.
func (q alertIngestFailureQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count alert_ingest_failures rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q alertIngestFailureQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if alert_ingest_failures exists")
	}

	return count > 0, nil
}

// Source pointed to by the foreign key.
func (o *AlertIngestFailure) Source(mods ...qm.QueryMod) alertSourceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SourceID),
	}

	queryMods = append(queryMods, mods...)

	return AlertSources(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *AlertIngestFailure) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// LoadSource allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertIngestFailureL) LoadSource(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlertIngestFailure any, mods queries.Applicator) error {
	var slice []*AlertIngestFailure
	var object *AlertIngestFailure

	if singular {
		var ok bool
		object, ok = maybeAlertIngestFailure.(*AlertIngestFailure)
		if !ok {
			object = new(AlertIngestFailure)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlertIngestFailure)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlertIngestFailure))
			}
		}
	} else {
		s, ok := maybeAlertIngestFailure.(*[]*AlertIngestFailure)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlertIngestFailure)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlertIngestFailure))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertIngestFailureR{}
		}
		if !queries.IsNil(object.SourceID) {
			args[object.SourceID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertIngestFailureR{}
			}

			if !queries.IsNil(obj.SourceID) {
				args[obj.SourceID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_sources`),
		qm.WhereIn(`alert_sources.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AlertSource")
	}

	var resultSlice []*AlertSource
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AlertSource")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for alert_sources")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_sources")
	}

	if len(alertSourceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Source = foreign
		if foreign.R == nil {
			foreign.R = &alertSourceR{}
		}
		foreign.R.SourceAlertIngestFailures = append(foreign.R.SourceAlertIngestFailures, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.SourceID, foreign.ID) {
				local.R.Source = foreign
				if foreign.R == nil {
					foreign.R = &alertSourceR{}
				}
				foreign.R.SourceAlertIngestFailures = append(foreign.R.SourceAlertIngestFailures, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertIngestFailureL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlertIngestFailure any, mods queries.Applicator) error {
	var slice []*AlertIngestFailure
	var object *AlertIngestFailure

	if singular {
		var ok bool
		object, ok = maybeAlertIngestFailure.(*AlertIngestFailure)
		if !ok {
			object = new(AlertIngestFailure)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlertIngestFailure)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlertIngestFailure))
			}
		}
	} else {
		s, ok := maybeAlertIngestFailure.(*[]*AlertIngestFailure)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlertIngestFailure)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlertIngestFailure))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertIngestFailureR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertIngestFailureR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.AlertIngestFailures = append(foreign.R.AlertIngestFailures, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.AlertIngestFailures = append(foreign.R.AlertIngestFailures, local)
				break
			}
		}
	}

	return nil
}

// SetSource of the alertIngestFailure to the related item.
// Sets o.R.Source to related.
// Adds o to related.R.SourceAlertIngestFailures.
func (o *AlertIngestFailure) SetSource(ctx context.Context, exec boil.ContextExecutor, insert bool, related *AlertSource) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alert_ingest_failures\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"source_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertIngestFailurePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.SourceID, related.ID)
	if o.R == nil {
		o.R = &alertIngestFailureR{
			Source: related,
		}
	} else {
		o.R.Source = related
	}

	if related.R == nil {
		related.R = &alertSourceR{
			SourceAlertIngestFailures: AlertIngestFailureSlice{o},
		}
	} else {
		related.R.SourceAlertIngestFailures = append(related.R.SourceAlertIngestFailures, o)
	}

	return nil
}

// RemoveSource relationship.
// Sets o.R.Source to nil.
// Removes o from all passed in related items' relationships struct.
func (o *AlertIngestFailure) RemoveSource(ctx context.Context, exec boil.ContextExecutor, related *AlertSource) error {
	var err error

	queries.SetScanner(&o.SourceID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("source_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Source = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.SourceAlertIngestFailures {
		if queries.Equal(o.SourceID, ri.SourceID) {
			continue
		}

		ln := len(related.R.SourceAlertIngestFailures)
		if ln > 1 && i < ln-1 {
			related.R.SourceAlertIngestFailures[i] = related.R.SourceAlertIngestFailures[ln-1]
		}
		related.R.SourceAlertIngestFailures = related.R.SourceAlertIngestFailures[:ln-1]
		break
	}
	return nil
}

// SetWorkspace of the alertIngestFailure to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.AlertIngestFailures.
func (o *AlertIngestFailure) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alert_ingest_failures\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertIngestFailurePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &alertIngestFailureR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			AlertIngestFailures: AlertIngestFailureSlice{o},
		}
	} else {
		related.R.AlertIngestFailures = append(related.R.AlertIngestFailures, o)
	}

	return nil
}

// AlertIngestFailures retrieves all the records using an executor.
func AlertIngestFailures(mods ...qm.QueryMod) alertIngestFailureQuery {
	mods = append(mods, qm.From("\"alert_ingest_failures\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"alert_ingest_failures\".*"})
	}

	return alertIngestFailureQuery{q}
}

// FindAlertIngestFailure retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAlertIngestFailure(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*AlertIngestFailure, error) {
	alertIngestFailureObj := &AlertIngestFailure{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"alert_ingest_failures\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, alertIngestFailureObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from alert_ingest_failures")
	}

	if err = alertIngestFailureObj.doAfterSelectHooks(ctx, exec); err != nil {
		return alertIngestFailureObj, err
	}

	return alertIngestFailureObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AlertIngestFailure) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no alert_ingest_failures provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(alertIngestFailureColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	alertIngestFailureInsertCacheMut.RLock()
	cache, cached := alertIngestFailureInsertCache[key]
	alertIngestFailureInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			alertIngestFailureAllColumns,
			alertIngestFailureColumnsWithDefault,
			alertIngestFailureColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(alertIngestFailureType, alertIngestFailureMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(alertIngestFailureType, alertIngestFailureMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"alert_ingest_failures\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"alert_ingest_failures\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into alert_ingest_failures")
	}

	if !cached {
		alertIngestFailureInsertCacheMut.Lock()
		alertIngestFailureInsertCache[key] = cache
		alertIngestFailureInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AlertIngestFailure.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AlertIngestFailure) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	alertIngestFailureUpdateCacheMut.RLock()
	cache, cached := alertIngestFailureUpdateCache[key]
	alertIngestFailureUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			alertIngestFailureAllColumns,
			alertIngestFailurePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update alert_ingest_failures, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"alert_ingest_failures\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, alertIngestFailurePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(alertIngestFailureType, alertIngestFailureMapping, append(wl, alertIngestFailurePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update alert_ingest_failures row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for alert_ingest_failures")
	}

	if !cached {
		alertIngestFailureUpdateCacheMut.Lock()
		alertIngestFailureUpdateCache[key] = cache
		alertIngestFailureUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q alertIngestFailureQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for alert_ingest_failures")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for alert_ingest_failures")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AlertIngestFailureSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertIngestFailurePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"alert_ingest_failures\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, alertIngestFailurePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in alertIngestFailure slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all alertIngestFailure")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AlertIngestFailure) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no alert_ingest_failures provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(alertIngestFailureColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	alertIngestFailureUpsertCacheMut.RLock()
	cache, cached := alertIngestFailureUpsertCache[key]
	alertIngestFailureUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			alertIngestFailureAllColumns,
			alertIngestFailureColumnsWithDefault,
			alertIngestFailureColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			alertIngestFailureAllColumns,
			alertIngestFailurePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert alert_ingest_failures, could not build update column list")
		}

		ret := strmangle.SetComplement(alertIngestFailureAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(alertIngestFailurePrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert alert_ingest_failures, could not build conflict column list")
			}

			conflict = make([]string, len(alertIngestFailurePrimaryKeyColumns))
			copy(conflict, alertIngestFailurePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"alert_ingest_failures\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(alertIngestFailureType, alertIngestFailureMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(alertIngestFailureType, alertIngestFailureMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert alert_ingest_failures")
	}

	if !cached {
		alertIngestFailureUpsertCacheMut.Lock()
		alertIngestFailureUpsertCache[key] = cache
		alertIngestFailureUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AlertIngestFailure record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AlertIngestFailure) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no AlertIngestFailure provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), alertIngestFailurePrimaryKeyMapping)
	sql := "DELETE FROM \"alert_ingest_failures\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from alert_ingest_failures")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for alert_ingest_failures")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q alertIngestFailureQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no alertIngestFailureQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from alert_ingest_failures")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for alert_ingest_failures")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AlertIngestFailureSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(alertIngestFailureBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertIngestFailurePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"alert_ingest_failures\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, alertIngestFailurePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from alertIngestFailure slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for alert_ingest_failures")
	}

	if len(alertIngestFailureAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AlertIngestFailure) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAlertIngestFailure(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AlertIngestFailureSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AlertIngestFailureSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertIngestFailurePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"alert_ingest_failures\".* FROM \"alert_ingest_failures\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, alertIngestFailurePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in AlertIngestFailureSlice")
	}

	*o = slice

	return nil
}

// AlertIngestFailureExists checks if the AlertIngestFailure row exists.
func AlertIngestFailureExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"alert_ingest_failures\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if alert_ingest_failures exists")
	}

	return exists, nil
}

// Exists checks if the AlertIngestFailure row exists.
func (o *AlertIngestFailure) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return AlertIngestFailureExists(ctx, exec, o.ID)
}
