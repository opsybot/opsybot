// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// IncidentEventRevision is an object representing the database table.
type IncidentEventRevision struct {
	ID           string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	EventID      string      `boil:"event_id" json:"event_id" toml:"event_id" yaml:"event_id"`
	WorkspaceID  string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	At           time.Time   `boil:"at" json:"at" toml:"at" yaml:"at"`
	EditorUserID null.String `boil:"editor_user_id" json:"editor_user_id,omitempty" toml:"editor_user_id" yaml:"editor_user_id,omitempty"`
	EditorLabel  string      `boil:"editor_label" json:"editor_label" toml:"editor_label" yaml:"editor_label"`
	Text         string      `boil:"text" json:"text" toml:"text" yaml:"text"`
	Category     string      `boil:"category" json:"category" toml:"category" yaml:"category"`

	R *incidentEventRevisionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L incidentEventRevisionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var IncidentEventRevisionColumns = struct {
	ID           string
	EventID      string
	WorkspaceID  string
	At           string
	EditorUserID string
	EditorLabel  string
	Text         string
	Category     string
}{
	ID:           "id",
	EventID:      "event_id",
	WorkspaceID:  "workspace_id",
	At:           "at",
	EditorUserID: "editor_user_id",
	EditorLabel:  "editor_label",
	Text:         "text",
	Category:     "category",
}

var IncidentEventRevisionTableColumns = struct {
	ID           string
	EventID      string
	WorkspaceID  string
	At           string
	EditorUserID string
	EditorLabel  string
	Text         string
	Category     string
}{
	ID:           "incident_event_revisions.id",
	EventID:      "incident_event_revisions.event_id",
	WorkspaceID:  "incident_event_revisions.workspace_id",
	At:           "incident_event_revisions.at",
	EditorUserID: "incident_event_revisions.editor_user_id",
	EditorLabel:  "incident_event_revisions.editor_label",
	Text:         "incident_event_revisions.text",
	Category:     "incident_event_revisions.category",
}

// Generated where

var IncidentEventRevisionWhere = struct {
	ID           whereHelperstring
	EventID      whereHelperstring
	WorkspaceID  whereHelperstring
	At           whereHelpertime_Time
	EditorUserID whereHelpernull_String
	EditorLabel  whereHelperstring
	Text         whereHelperstring
	Category     whereHelperstring
}{
	ID:           whereHelperstring{field: "\"incident_event_revisions\".\"id\""},
	EventID:      whereHelperstring{field: "\"incident_event_revisions\".\"event_id\""},
	WorkspaceID:  whereHelperstring{field: "\"incident_event_revisions\".\"workspace_id\""},
	At:           whereHelpertime_Time{field: "\"incident_event_revisions\".\"at\""},
	EditorUserID: whereHelpernull_String{field: "\"incident_event_revisions\".\"editor_user_id\""},
	EditorLabel:  whereHelperstring{field: "\"incident_event_revisions\".\"editor_label\""},
	Text:         whereHelperstring{field: "\"incident_event_revisions\".\"text\""},
	Category:     whereHelperstring{field: "\"incident_event_revisions\".\"category\""},
}

// IncidentEventRevisionRels is where relationship names are stored.
var IncidentEventRevisionRels = struct {
	EditorUser string
}{
	EditorUser: "EditorUser",
}

// incidentEventRevisionR is where relationships are stored.
type incidentEventRevisionR struct {
	EditorUser *User `boil:"EditorUser" json:"EditorUser" toml:"EditorUser" yaml:"EditorUser"`
}

// NewStruct creates a new relationship struct
func (*incidentEventRevisionR) NewStruct() *incidentEventRevisionR {
	return &incidentEventRevisionR{}
}

func (o *IncidentEventRevision) GetEditorUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetEditorUser()
}

func (r *incidentEventRevisionR) GetEditorUser() *User {
	if r == nil {
		return nil
	}

	return r.EditorUser
}

// incidentEventRevisionL is where Load methods for each relationship are stored.
type incidentEventRevisionL struct{}

var (
	incidentEventRevisionAllColumns            = []string{"id", "event_id", "workspace_id", "at", "editor_user_id", "editor_label", "text", "category"}
	incidentEventRevisionColumnsWithoutDefault = []string{"event_id", "workspace_id"}
	incidentEventRevisionColumnsWithDefault    = []string{"id", "at", "editor_user_id", "editor_label", "text", "category"}
	incidentEventRevisionPrimaryKeyColumns     = []string{"id"}
	incidentEventRevisionGeneratedColumns      = []string{}
)

type (
	// IncidentEventRevisionSlice is an alias for a slice of pointers to IncidentEventRevision.
	// This should almost always be used instead of []IncidentEventRevision.
	IncidentEventRevisionSlice []*IncidentEventRevision
	// IncidentEventRevisionHook is the signature for custom IncidentEventRevision hook methods
	IncidentEventRevisionHook func(context.Context, boil.ContextExecutor, *IncidentEventRevision) error

	incidentEventRevisionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	incidentEventRevisionType                 = reflect.TypeOf(&IncidentEventRevision{})
	incidentEventRevisionMapping              = queries.MakeStructMapping(incidentEventRevisionType)
	incidentEventRevisionPrimaryKeyMapping, _ = queries.BindMapping(incidentEventRevisionType, incidentEventRevisionMapping, incidentEventRevisionPrimaryKeyColumns)
	incidentEventRevisionInsertCacheMut       sync.RWMutex
	incidentEventRevisionInsertCache          = make(map[string]insertCache)
	incidentEventRevisionUpdateCacheMut       sync.RWMutex
	incidentEventRevisionUpdateCache          = make(map[string]updateCache)
	incidentEventRevisionUpsertCacheMut       sync.RWMutex
	incidentEventRevisionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var incidentEventRevisionAfterSelectMu sync.Mutex
var incidentEventRevisionAfterSelectHooks []IncidentEventRevisionHook

var incidentEventRevisionBeforeInsertMu sync.Mutex
var incidentEventRevisionBeforeInsertHooks []IncidentEventRevisionHook
var incidentEventRevisionAfterInsertMu sync.Mutex
var incidentEventRevisionAfterInsertHooks []IncidentEventRevisionHook

var incidentEventRevisionBeforeUpdateMu sync.Mutex
var incidentEventRevisionBeforeUpdateHooks []IncidentEventRevisionHook
var incidentEventRevisionAfterUpdateMu sync.Mutex
var incidentEventRevisionAfterUpdateHooks []IncidentEventRevisionHook

var incidentEventRevisionBeforeDeleteMu sync.Mutex
var incidentEventRevisionBeforeDeleteHooks []IncidentEventRevisionHook
var incidentEventRevisionAfterDeleteMu sync.Mutex
var incidentEventRevisionAfterDeleteHooks []IncidentEventRevisionHook

var incidentEventRevisionBeforeUpsertMu sync.Mutex
var incidentEventRevisionBeforeUpsertHooks []IncidentEventRevisionHook
var incidentEventRevisionAfterUpsertMu sync.Mutex
var incidentEventRevisionAfterUpsertHooks []IncidentEventRevisionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *IncidentEventRevision) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventRevisionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *IncidentEventRevision) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventRevisionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *IncidentEventRevision) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventRevisionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *IncidentEventRevision) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventRevisionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *IncidentEventRevision) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventRevisionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *IncidentEventRevision) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventRevisionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *IncidentEventRevision) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventRevisionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *IncidentEventRevision) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventRevisionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *IncidentEventRevision) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentEventRevisionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddIncidentEventRevisionHook registers your hook function for all future operations.
func AddIncidentEventRevisionHook(hookPoint boil.HookPoint, incidentEventRevisionHook IncidentEventRevisionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		incidentEventRevisionAfterSelectMu.Lock()
		incidentEventRevisionAfterSelectHooks = append(incidentEventRevisionAfterSelectHooks, incidentEventRevisionHook)
		incidentEventRevisionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		incidentEventRevisionBeforeInsertMu.Lock()
		incidentEventRevisionBeforeInsertHooks = append(incidentEventRevisionBeforeInsertHooks, incidentEventRevisionHook)
		incidentEventRevisionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		incidentEventRevisionAfterInsertMu.Lock()
		incidentEventRevisionAfterInsertHooks = append(incidentEventRevisionAfterInsertHooks, incidentEventRevisionHook)
		incidentEventRevisionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		incidentEventRevisionBeforeUpdateMu.Lock()
		incidentEventRevisionBeforeUpdateHooks = append(incidentEventRevisionBeforeUpdateHooks, incidentEventRevisionHook)
		incidentEventRevisionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		incidentEventRevisionAfterUpdateMu.Lock()
		incidentEventRevisionAfterUpdateHooks = append(incidentEventRevisionAfterUpdateHooks, incidentEventRevisionHook)
		incidentEventRevisionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		incidentEventRevisionBeforeDeleteMu.Lock()
		incidentEventRevisionBeforeDeleteHooks = append(incidentEventRevisionBeforeDeleteHooks, incidentEventRevisionHook)
		incidentEventRevisionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		incidentEventRevisionAfterDeleteMu.Lock()
		incidentEventRevisionAfterDeleteHooks = append(incidentEventRevisionAfterDeleteHooks, incidentEventRevisionHook)
		incidentEventRevisionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		incidentEventRevisionBeforeUpsertMu.Lock()
		incidentEventRevisionBeforeUpsertHooks = append(incidentEventRevisionBeforeUpsertHooks, incidentEventRevisionHook)
		incidentEventRevisionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		incidentEventRevisionAfterUpsertMu.Lock()
		incidentEventRevisionAfterUpsertHooks = append(incidentEventRevisionAfterUpsertHooks, incidentEventRevisionHook)
		incidentEventRevisionAfterUpsertMu.Unlock()
	}
}

// One returns a single incidentEventRevision record from the query.
func (q incidentEventRevisionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*IncidentEventRevision, error) {
	o := &IncidentEventRevision{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for incident_event_revisions")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all IncidentEventRevision records from the query.
func (q incidentEventRevisionQuery) All(ctx context.Context, exec boil.ContextExecutor) (IncidentEventRevisionSlice, error) {
	var o []*IncidentEventRevision

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to IncidentEventRevision slice")
	}

	if len(incidentEventRevisionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all IncidentEventRevision records in the query.
func (q incidentEventRevisionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count incident_event_revisions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q incidentEventRevisionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if incident_event_revisions exists")
	}

	return count > 0, nil
}

// EditorUser pointed to by the foreign key.
func (o *IncidentEventRevision) EditorUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.EditorUserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadEditorUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (incidentEventRevisionL) LoadEditorUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncidentEventRevision any, mods queries.Applicator) error {
	var slice []*IncidentEventRevision
	var object *IncidentEventRevision

	if singular {
		var ok bool
		object, ok = maybeIncidentEventRevision.(*IncidentEventRevision)
		if !ok {
			object = new(IncidentEventRevision)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncidentEventRevision)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncidentEventRevision))
			}
		}
	} else {
		s, ok := maybeIncidentEventRevision.(*[]*IncidentEventRevision)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncidentEventRevision)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncidentEventRevision))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incidentEventRevisionR{}
		}
		if !queries.IsNil(object.EditorUserID) {
			args[object.EditorUserID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incidentEventRevisionR{}
			}

			if !queries.IsNil(obj.EditorUserID) {
				args[obj.EditorUserID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.EditorUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.EditorUserIncidentEventRevisions = append(foreign.R.EditorUserIncidentEventRevisions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.EditorUserID, foreign.ID) {
				local.R.EditorUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.EditorUserIncidentEventRevisions = append(foreign.R.EditorUserIncidentEventRevisions, local)
				break
			}
		}
	}

	return nil
}

// SetEditorUser of the incidentEventRevision to the related item.
// Sets o.R.EditorUser to related.
// Adds o to related.R.EditorUserIncidentEventRevisions.
func (o *IncidentEventRevision) SetEditorUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"incident_event_revisions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"editor_user_id"}),
		strmangle.WhereClause("\"", "\"", 2, incidentEventRevisionPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.EditorUserID, related.ID)
	if o.R == nil {
		o.R = &incidentEventRevisionR{
			EditorUser: related,
		}
	} else {
		o.R.EditorUser = related
	}

	if related.R == nil {
		related.R = &userR{
			EditorUserIncidentEventRevisions: IncidentEventRevisionSlice{o},
		}
	} else {
		related.R.EditorUserIncidentEventRevisions = append(related.R.EditorUserIncidentEventRevisions, o)
	}

	return nil
}

// RemoveEditorUser relationship.
// Sets o.R.EditorUser to nil.
// Removes o from all passed in related items' relationships struct.
func (o *IncidentEventRevision) RemoveEditorUser(ctx context.Context, exec boil.ContextExecutor, related *User) error {
	var err error

	queries.SetScanner(&o.EditorUserID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("editor_user_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.EditorUser = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.EditorUserIncidentEventRevisions {
		if queries.Equal(o.EditorUserID, ri.EditorUserID) {
			continue
		}

		ln := len(related.R.EditorUserIncidentEventRevisions)
		if ln > 1 && i < ln-1 {
			related.R.EditorUserIncidentEventRevisions[i] = related.R.EditorUserIncidentEventRevisions[ln-1]
		}
		related.R.EditorUserIncidentEventRevisions = related.R.EditorUserIncidentEventRevisions[:ln-1]
		break
	}
	return nil
}

// IncidentEventRevisions retrieves all the records using an executor.
func IncidentEventRevisions(mods ...qm.QueryMod) incidentEventRevisionQuery {
	mods = append(mods, qm.From("\"incident_event_revisions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"incident_event_revisions\".*"})
	}

	return incidentEventRevisionQuery{q}
}

// FindIncidentEventRevision retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindIncidentEventRevision(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*IncidentEventRevision, error) {
	incidentEventRevisionObj := &IncidentEventRevision{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"incident_event_revisions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, incidentEventRevisionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from incident_event_revisions")
	}

	if err = incidentEventRevisionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return incidentEventRevisionObj, err
	}

	return incidentEventRevisionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *IncidentEventRevision) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no incident_event_revisions provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incidentEventRevisionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	incidentEventRevisionInsertCacheMut.RLock()
	cache, cached := incidentEventRevisionInsertCache[key]
	incidentEventRevisionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			incidentEventRevisionAllColumns,
			incidentEventRevisionColumnsWithDefault,
			incidentEventRevisionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(incidentEventRevisionType, incidentEventRevisionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(incidentEventRevisionType, incidentEventRevisionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"incident_event_revisions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"incident_event_revisions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into incident_event_revisions")
	}

	if !cached {
		incidentEventRevisionInsertCacheMut.Lock()
		incidentEventRevisionInsertCache[key] = cache
		incidentEventRevisionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the IncidentEventRevision.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *IncidentEventRevision) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	incidentEventRevisionUpdateCacheMut.RLock()
	cache, cached := incidentEventRevisionUpdateCache[key]
	incidentEventRevisionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			incidentEventRevisionAllColumns,
			incidentEventRevisionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update incident_event_revisions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"incident_event_revisions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, incidentEventRevisionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(incidentEventRevisionType, incidentEventRevisionMapping, append(wl, incidentEventRevisionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update incident_event_revisions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for incident_event_revisions")
	}

	if !cached {
		incidentEventRevisionUpdateCacheMut.Lock()
		incidentEventRevisionUpdateCache[key] = cache
		incidentEventRevisionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q incidentEventRevisionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for incident_event_revisions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for incident_event_revisions")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o IncidentEventRevisionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incidentEventRevisionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"incident_event_revisions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, incidentEventRevisionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in incidentEventRevision slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all incidentEventRevision")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *IncidentEventRevision) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no incident_event_revisions provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incidentEventRevisionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	incidentEventRevisionUpsertCacheMut.RLock()
	cache, cached := incidentEventRevisionUpsertCache[key]
	incidentEventRevisionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			incidentEventRevisionAllColumns,
			incidentEventRevisionColumnsWithDefault,
			incidentEventRevisionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			incidentEventRevisionAllColumns,
			incidentEventRevisionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert incident_event_revisions, could not build update column list")
		}

		ret := strmangle.SetComplement(incidentEventRevisionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(incidentEventRevisionPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert incident_event_revisions, could not build conflict column list")
			}

			conflict = make([]string, len(incidentEventRevisionPrimaryKeyColumns))
			copy(conflict, incidentEventRevisionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"incident_event_revisions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(incidentEventRevisionType, incidentEventRevisionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(incidentEventRevisionType, incidentEventRevisionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert incident_event_revisions")
	}

	if !cached {
		incidentEventRevisionUpsertCacheMut.Lock()
		incidentEventRevisionUpsertCache[key] = cache
		incidentEventRevisionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single IncidentEventRevision record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *IncidentEventRevision) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no IncidentEventRevision provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), incidentEventRevisionPrimaryKeyMapping)
	sql := "DELETE FROM \"incident_event_revisions\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from incident_event_revisions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for incident_event_revisions")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q incidentEventRevisionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no incidentEventRevisionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from incident_event_revisions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for incident_event_revisions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o IncidentEventRevisionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(incidentEventRevisionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incidentEventRevisionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"incident_event_revisions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incidentEventRevisionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from incidentEventRevision slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for incident_event_revisions")
	}

	if len(incidentEventRevisionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *IncidentEventRevision) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindIncidentEventRevision(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *IncidentEventRevisionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := IncidentEventRevisionSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incidentEventRevisionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"incident_event_revisions\".* FROM \"incident_event_revisions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incidentEventRevisionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in IncidentEventRevisionSlice")
	}

	*o = slice

	return nil
}

// IncidentEventRevisionExists checks if the IncidentEventRevision row exists.
func IncidentEventRevisionExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"incident_event_revisions\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if incident_event_revisions exists")
	}

	return exists, nil
}

// Exists checks if the IncidentEventRevision row exists.
func (o *IncidentEventRevision) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return IncidentEventRevisionExists(ctx, exec, o.ID)
}
