// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// EscalationPolicy is an object representing the database table.
type EscalationPolicy struct {
	ID                string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID       string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	Slug              string      `boil:"slug" json:"slug" toml:"slug" yaml:"slug"`
	Name              string      `boil:"name" json:"name" toml:"name" yaml:"name"`
	TeamID            null.String `boil:"team_id" json:"team_id,omitempty" toml:"team_id" yaml:"team_id,omitempty"`
	RepeatCount       int         `boil:"repeat_count" json:"repeat_count" toml:"repeat_count" yaml:"repeat_count"`
	AckTimeoutSeconds int         `boil:"ack_timeout_seconds" json:"ack_timeout_seconds" toml:"ack_timeout_seconds" yaml:"ack_timeout_seconds"`
	Definition        types.JSON  `boil:"definition" json:"definition" toml:"definition" yaml:"definition"`
	CreatedAt         time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt         time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *escalationPolicyR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L escalationPolicyL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EscalationPolicyColumns = struct {
	ID                string
	WorkspaceID       string
	Slug              string
	Name              string
	TeamID            string
	RepeatCount       string
	AckTimeoutSeconds string
	Definition        string
	CreatedAt         string
	UpdatedAt         string
}{
	ID:                "id",
	WorkspaceID:       "workspace_id",
	Slug:              "slug",
	Name:              "name",
	TeamID:            "team_id",
	RepeatCount:       "repeat_count",
	AckTimeoutSeconds: "ack_timeout_seconds",
	Definition:        "definition",
	CreatedAt:         "created_at",
	UpdatedAt:         "updated_at",
}

var EscalationPolicyTableColumns = struct {
	ID                string
	WorkspaceID       string
	Slug              string
	Name              string
	TeamID            string
	RepeatCount       string
	AckTimeoutSeconds string
	Definition        string
	CreatedAt         string
	UpdatedAt         string
}{
	ID:                "escalation_policies.id",
	WorkspaceID:       "escalation_policies.workspace_id",
	Slug:              "escalation_policies.slug",
	Name:              "escalation_policies.name",
	TeamID:            "escalation_policies.team_id",
	RepeatCount:       "escalation_policies.repeat_count",
	AckTimeoutSeconds: "escalation_policies.ack_timeout_seconds",
	Definition:        "escalation_policies.definition",
	CreatedAt:         "escalation_policies.created_at",
	UpdatedAt:         "escalation_policies.updated_at",
}

// Generated where

var EscalationPolicyWhere = struct {
	ID                whereHelperstring
	WorkspaceID       whereHelperstring
	Slug              whereHelperstring
	Name              whereHelperstring
	TeamID            whereHelpernull_String
	RepeatCount       whereHelperint
	AckTimeoutSeconds whereHelperint
	Definition        whereHelpertypes_JSON
	CreatedAt         whereHelpertime_Time
	UpdatedAt         whereHelpertime_Time
}{
	ID:                whereHelperstring{field: "\"escalation_policies\".\"id\""},
	WorkspaceID:       whereHelperstring{field: "\"escalation_policies\".\"workspace_id\""},
	Slug:              whereHelperstring{field: "\"escalation_policies\".\"slug\""},
	Name:              whereHelperstring{field: "\"escalation_policies\".\"name\""},
	TeamID:            whereHelpernull_String{field: "\"escalation_policies\".\"team_id\""},
	RepeatCount:       whereHelperint{field: "\"escalation_policies\".\"repeat_count\""},
	AckTimeoutSeconds: whereHelperint{field: "\"escalation_policies\".\"ack_timeout_seconds\""},
	Definition:        whereHelpertypes_JSON{field: "\"escalation_policies\".\"definition\""},
	CreatedAt:         whereHelpertime_Time{field: "\"escalation_policies\".\"created_at\""},
	UpdatedAt:         whereHelpertime_Time{field: "\"escalation_policies\".\"updated_at\""},
}

// EscalationPolicyRels is where relationship names are stored.
var EscalationPolicyRels = struct {
	Team                                 string
	Workspace                            string
	PolicyAlertEscalations               string
	AlertMonitors                        string
	AlertRoutes                          string
	DefaultEscalationPolicyAlertSettings string
	Alerts                               string
	PolicyEscalationPolicyTargets        string
	PolicyEscalationRRStates             string
}{
	Team:                                 "Team",
	Workspace:                            "Workspace",
	PolicyAlertEscalations:               "PolicyAlertEscalations",
	AlertMonitors:                        "AlertMonitors",
	AlertRoutes:                          "AlertRoutes",
	DefaultEscalationPolicyAlertSettings: "DefaultEscalationPolicyAlertSettings",
	Alerts:                               "Alerts",
	PolicyEscalationPolicyTargets:        "PolicyEscalationPolicyTargets",
	PolicyEscalationRRStates:             "PolicyEscalationRRStates",
}

// escalationPolicyR is where relationships are stored.
type escalationPolicyR struct {
	Team                                 *Team                       `boil:"Team" json:"Team" toml:"Team" yaml:"Team"`
	Workspace                            *Workspace                  `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
	PolicyAlertEscalations               AlertEscalationSlice        `boil:"PolicyAlertEscalations" json:"PolicyAlertEscalations" toml:"PolicyAlertEscalations" yaml:"PolicyAlertEscalations"`
	AlertMonitors                        AlertMonitorSlice           `boil:"AlertMonitors" json:"AlertMonitors" toml:"AlertMonitors" yaml:"AlertMonitors"`
	AlertRoutes                          AlertRouteSlice             `boil:"AlertRoutes" json:"AlertRoutes" toml:"AlertRoutes" yaml:"AlertRoutes"`
	DefaultEscalationPolicyAlertSettings AlertSettingSlice           `boil:"DefaultEscalationPolicyAlertSettings" json:"DefaultEscalationPolicyAlertSettings" toml:"DefaultEscalationPolicyAlertSettings" yaml:"DefaultEscalationPolicyAlertSettings"`
	Alerts                               AlertSlice                  `boil:"Alerts" json:"Alerts" toml:"Alerts" yaml:"Alerts"`
	PolicyEscalationPolicyTargets        EscalationPolicyTargetSlice `boil:"PolicyEscalationPolicyTargets" json:"PolicyEscalationPolicyTargets" toml:"PolicyEscalationPolicyTargets" yaml:"PolicyEscalationPolicyTargets"`
	PolicyEscalationRRStates             EscalationRRStateSlice      `boil:"PolicyEscalationRRStates" json:"PolicyEscalationRRStates" toml:"PolicyEscalationRRStates" yaml:"PolicyEscalationRRStates"`
}

// NewStruct creates a new relationship struct
func (*escalationPolicyR) NewStruct() *escalationPolicyR {
	return &escalationPolicyR{}
}

func (o *EscalationPolicy) GetTeam() *Team {
	if o == nil {
		return nil
	}

	return o.R.GetTeam()
}

func (r *escalationPolicyR) GetTeam() *Team {
	if r == nil {
		return nil
	}

	return r.Team
}

func (o *EscalationPolicy) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *escalationPolicyR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

func (o *EscalationPolicy) GetPolicyAlertEscalations() AlertEscalationSlice {
	if o == nil {
		return nil
	}

	return o.R.GetPolicyAlertEscalations()
}

func (r *escalationPolicyR) GetPolicyAlertEscalations() AlertEscalationSlice {
	if r == nil {
		return nil
	}

	return r.PolicyAlertEscalations
}

func (o *EscalationPolicy) GetAlertMonitors() AlertMonitorSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertMonitors()
}

func (r *escalationPolicyR) GetAlertMonitors() AlertMonitorSlice {
	if r == nil {
		return nil
	}

	return r.AlertMonitors
}

func (o *EscalationPolicy) GetAlertRoutes() AlertRouteSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertRoutes()
}

func (r *escalationPolicyR) GetAlertRoutes() AlertRouteSlice {
	if r == nil {
		return nil
	}

	return r.AlertRoutes
}

func (o *EscalationPolicy) GetDefaultEscalationPolicyAlertSettings() AlertSettingSlice {
	if o == nil {
		return nil
	}

	return o.R.GetDefaultEscalationPolicyAlertSettings()
}

func (r *escalationPolicyR) GetDefaultEscalationPolicyAlertSettings() AlertSettingSlice {
	if r == nil {
		return nil
	}

	return r.DefaultEscalationPolicyAlertSettings
}

func (o *EscalationPolicy) GetAlerts() AlertSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlerts()
}

func (r *escalationPolicyR) GetAlerts() AlertSlice {
	if r == nil {
		return nil
	}

	return r.Alerts
}

func (o *EscalationPolicy) GetPolicyEscalationPolicyTargets() EscalationPolicyTargetSlice {
	if o == nil {
		return nil
	}

	return o.R.GetPolicyEscalationPolicyTargets()
}

func (r *escalationPolicyR) GetPolicyEscalationPolicyTargets() EscalationPolicyTargetSlice {
	if r == nil {
		return nil
	}

	return r.PolicyEscalationPolicyTargets
}

func (o *EscalationPolicy) GetPolicyEscalationRRStates() EscalationRRStateSlice {
	if o == nil {
		return nil
	}

	return o.R.GetPolicyEscalationRRStates()
}

func (r *escalationPolicyR) GetPolicyEscalationRRStates() EscalationRRStateSlice {
	if r == nil {
		return nil
	}

	return r.PolicyEscalationRRStates
}

// escalationPolicyL is where Load methods for each relationship are stored.
type escalationPolicyL struct{}

var (
	escalationPolicyAllColumns            = []string{"id", "workspace_id", "slug", "name", "team_id", "repeat_count", "ack_timeout_seconds", "definition", "created_at", "updated_at"}
	escalationPolicyColumnsWithoutDefault = []string{"workspace_id", "slug", "name", "definition"}
	escalationPolicyColumnsWithDefault    = []string{"id", "team_id", "repeat_count", "ack_timeout_seconds", "created_at", "updated_at"}
	escalationPolicyPrimaryKeyColumns     = []string{"id"}
	escalationPolicyGeneratedColumns      = []string{}
)

type (
	// EscalationPolicySlice is an alias for a slice of pointers to EscalationPolicy.
	// This should almost always be used instead of []EscalationPolicy.
	EscalationPolicySlice []*EscalationPolicy
	// EscalationPolicyHook is the signature for custom EscalationPolicy hook methods
	EscalationPolicyHook func(context.Context, boil.ContextExecutor, *EscalationPolicy) error

	escalationPolicyQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	escalationPolicyType                 = reflect.TypeOf(&EscalationPolicy{})
	escalationPolicyMapping              = queries.MakeStructMapping(escalationPolicyType)
	escalationPolicyPrimaryKeyMapping, _ = queries.BindMapping(escalationPolicyType, escalationPolicyMapping, escalationPolicyPrimaryKeyColumns)
	escalationPolicyInsertCacheMut       sync.RWMutex
	escalationPolicyInsertCache          = make(map[string]insertCache)
	escalationPolicyUpdateCacheMut       sync.RWMutex
	escalationPolicyUpdateCache          = make(map[string]updateCache)
	escalationPolicyUpsertCacheMut       sync.RWMutex
	escalationPolicyUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var escalationPolicyAfterSelectMu sync.Mutex
var escalationPolicyAfterSelectHooks []EscalationPolicyHook

var escalationPolicyBeforeInsertMu sync.Mutex
var escalationPolicyBeforeInsertHooks []EscalationPolicyHook
var escalationPolicyAfterInsertMu sync.Mutex
var escalationPolicyAfterInsertHooks []EscalationPolicyHook

var escalationPolicyBeforeUpdateMu sync.Mutex
var escalationPolicyBeforeUpdateHooks []EscalationPolicyHook
var escalationPolicyAfterUpdateMu sync.Mutex
var escalationPolicyAfterUpdateHooks []EscalationPolicyHook

var escalationPolicyBeforeDeleteMu sync.Mutex
var escalationPolicyBeforeDeleteHooks []EscalationPolicyHook
var escalationPolicyAfterDeleteMu sync.Mutex
var escalationPolicyAfterDeleteHooks []EscalationPolicyHook

var escalationPolicyBeforeUpsertMu sync.Mutex
var escalationPolicyBeforeUpsertHooks []EscalationPolicyHook
var escalationPolicyAfterUpsertMu sync.Mutex
var escalationPolicyAfterUpsertHooks []EscalationPolicyHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *EscalationPolicy) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range escalationPolicyAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *EscalationPolicy) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range escalationPolicyBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *EscalationPolicy) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range escalationPolicyAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *EscalationPolicy) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range escalationPolicyBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *EscalationPolicy) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range escalationPolicyAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *EscalationPolicy) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range escalationPolicyBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *EscalationPolicy) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range escalationPolicyAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *EscalationPolicy) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range escalationPolicyBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *EscalationPolicy) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range escalationPolicyAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddEscalationPolicyHook registers your hook function for all future operations.
func AddEscalationPolicyHook(hookPoint boil.HookPoint, escalationPolicyHook EscalationPolicyHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		escalationPolicyAfterSelectMu.Lock()
		escalationPolicyAfterSelectHooks = append(escalationPolicyAfterSelectHooks, escalationPolicyHook)
		escalationPolicyAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		escalationPolicyBeforeInsertMu.Lock()
		escalationPolicyBeforeInsertHooks = append(escalationPolicyBeforeInsertHooks, escalationPolicyHook)
		escalationPolicyBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		escalationPolicyAfterInsertMu.Lock()
		escalationPolicyAfterInsertHooks = append(escalationPolicyAfterInsertHooks, escalationPolicyHook)
		escalationPolicyAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		escalationPolicyBeforeUpdateMu.Lock()
		escalationPolicyBeforeUpdateHooks = append(escalationPolicyBeforeUpdateHooks, escalationPolicyHook)
		escalationPolicyBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		escalationPolicyAfterUpdateMu.Lock()
		escalationPolicyAfterUpdateHooks = append(escalationPolicyAfterUpdateHooks, escalationPolicyHook)
		escalationPolicyAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		escalationPolicyBeforeDeleteMu.Lock()
		escalationPolicyBeforeDeleteHooks = append(escalationPolicyBeforeDeleteHooks, escalationPolicyHook)
		escalationPolicyBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		escalationPolicyAfterDeleteMu.Lock()
		escalationPolicyAfterDeleteHooks = append(escalationPolicyAfterDeleteHooks, escalationPolicyHook)
		escalationPolicyAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		escalationPolicyBeforeUpsertMu.Lock()
		escalationPolicyBeforeUpsertHooks = append(escalationPolicyBeforeUpsertHooks, escalationPolicyHook)
		escalationPolicyBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		escalationPolicyAfterUpsertMu.Lock()
		escalationPolicyAfterUpsertHooks = append(escalationPolicyAfterUpsertHooks, escalationPolicyHook)
		escalationPolicyAfterUpsertMu.Unlock()
	}
}

// One returns a single escalationPolicy record from the query.
func (q escalationPolicyQuery) One(ctx context.Context, exec boil.ContextExecutor) (*EscalationPolicy, error) {
	o := &EscalationPolicy{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for escalation_policies")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all EscalationPolicy records from the query.
func (q escalationPolicyQuery) All(ctx context.Context, exec boil.ContextExecutor) (EscalationPolicySlice, error) {
	var o []*EscalationPolicy

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to EscalationPolicy slice")
	}

	if len(escalationPolicyAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all EscalationPolicy records in the query.
func (q escalationPolicyQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count escalation_policies rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q escalationPolicyQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if escalation_policies exists")
	}

	return count > 0, nil
}

// Team pointed to by the foreign key.
func (o *EscalationPolicy) Team(mods ...qm.QueryMod) teamQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TeamID),
	}

	queryMods = append(queryMods, mods...)

	return Teams(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *EscalationPolicy) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// PolicyAlertEscalations retrieves all the alert_escalation's AlertEscalations with an executor via policy_id column.
func (o *EscalationPolicy) PolicyAlertEscalations(mods ...qm.QueryMod) alertEscalationQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_escalations\".\"policy_id\"=?", o.ID),
	)

	return AlertEscalations(queryMods...)
}

// AlertMonitors retrieves all the alert_monitor's AlertMonitors with an executor.
func (o *EscalationPolicy) AlertMonitors(mods ...qm.QueryMod) alertMonitorQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_monitors\".\"escalation_policy_id\"=?", o.ID),
	)

	return AlertMonitors(queryMods...)
}

// AlertRoutes retrieves all the alert_route's AlertRoutes with an executor.
func (o *EscalationPolicy) AlertRoutes(mods ...qm.QueryMod) alertRouteQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_routes\".\"escalation_policy_id\"=?", o.ID),
	)

	return AlertRoutes(queryMods...)
}

// DefaultEscalationPolicyAlertSettings retrieves all the alert_setting's AlertSettings with an executor via default_escalation_policy_id column.
func (o *EscalationPolicy) DefaultEscalationPolicyAlertSettings(mods ...qm.QueryMod) alertSettingQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_settings\".\"default_escalation_policy_id\"=?", o.ID),
	)

	return AlertSettings(queryMods...)
}

// Alerts retrieves all the alert's Alerts with an executor.
func (o *EscalationPolicy) Alerts(mods ...qm.QueryMod) alertQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alerts\".\"escalation_policy_id\"=?", o.ID),
	)

	return Alerts(queryMods...)
}

// PolicyEscalationPolicyTargets retrieves all the escalation_policy_target's EscalationPolicyTargets with an executor via policy_id column.
func (o *EscalationPolicy) PolicyEscalationPolicyTargets(mods ...qm.QueryMod) escalationPolicyTargetQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"escalation_policy_targets\".\"policy_id\"=?", o.ID),
	)

	return EscalationPolicyTargets(queryMods...)
}

// PolicyEscalationRRStates retrieves all the escalation_rr_state's EscalationRRStates with an executor via policy_id column.
func (o *EscalationPolicy) PolicyEscalationRRStates(mods ...qm.QueryMod) escalationRRStateQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"escalation_rr_state\".\"policy_id\"=?", o.ID),
	)

	return EscalationRRStates(queryMods...)
}

// LoadTeam allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (escalationPolicyL) LoadTeam(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEscalationPolicy any, mods queries.Applicator) error {
	var slice []*EscalationPolicy
	var object *EscalationPolicy

	if singular {
		var ok bool
		object, ok = maybeEscalationPolicy.(*EscalationPolicy)
		if !ok {
			object = new(EscalationPolicy)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEscalationPolicy))
			}
		}
	} else {
		s, ok := maybeEscalationPolicy.(*[]*EscalationPolicy)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEscalationPolicy))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &escalationPolicyR{}
		}
		if !queries.IsNil(object.TeamID) {
			args[object.TeamID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &escalationPolicyR{}
			}

			if !queries.IsNil(obj.TeamID) {
				args[obj.TeamID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`teams`),
		qm.WhereIn(`teams.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Team")
	}

	var resultSlice []*Team
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Team")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for teams")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for teams")
	}

	if len(teamAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Team = foreign
		if foreign.R == nil {
			foreign.R = &teamR{}
		}
		foreign.R.EscalationPolicies = append(foreign.R.EscalationPolicies, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.TeamID, foreign.ID) {
				local.R.Team = foreign
				if foreign.R == nil {
					foreign.R = &teamR{}
				}
				foreign.R.EscalationPolicies = append(foreign.R.EscalationPolicies, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (escalationPolicyL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEscalationPolicy any, mods queries.Applicator) error {
	var slice []*EscalationPolicy
	var object *EscalationPolicy

	if singular {
		var ok bool
		object, ok = maybeEscalationPolicy.(*EscalationPolicy)
		if !ok {
			object = new(EscalationPolicy)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEscalationPolicy))
			}
		}
	} else {
		s, ok := maybeEscalationPolicy.(*[]*EscalationPolicy)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEscalationPolicy))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &escalationPolicyR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &escalationPolicyR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.EscalationPolicies = append(foreign.R.EscalationPolicies, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.EscalationPolicies = append(foreign.R.EscalationPolicies, local)
				break
			}
		}
	}

	return nil
}

// LoadPolicyAlertEscalations allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (escalationPolicyL) LoadPolicyAlertEscalations(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEscalationPolicy any, mods queries.Applicator) error {
	var slice []*EscalationPolicy
	var object *EscalationPolicy

	if singular {
		var ok bool
		object, ok = maybeEscalationPolicy.(*EscalationPolicy)
		if !ok {
			object = new(EscalationPolicy)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEscalationPolicy))
			}
		}
	} else {
		s, ok := maybeEscalationPolicy.(*[]*EscalationPolicy)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEscalationPolicy))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &escalationPolicyR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &escalationPolicyR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_escalations`),
		qm.WhereIn(`alert_escalations.policy_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_escalations")
	}

	var resultSlice []*AlertEscalation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_escalations")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_escalations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_escalations")
	}

	if len(alertEscalationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PolicyAlertEscalations = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertEscalationR{}
			}
			foreign.R.Policy = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.PolicyID {
				local.R.PolicyAlertEscalations = append(local.R.PolicyAlertEscalations, foreign)
				if foreign.R == nil {
					foreign.R = &alertEscalationR{}
				}
				foreign.R.Policy = local
				break
			}
		}
	}

	return nil
}

// LoadAlertMonitors allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (escalationPolicyL) LoadAlertMonitors(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEscalationPolicy any, mods queries.Applicator) error {
	var slice []*EscalationPolicy
	var object *EscalationPolicy

	if singular {
		var ok bool
		object, ok = maybeEscalationPolicy.(*EscalationPolicy)
		if !ok {
			object = new(EscalationPolicy)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEscalationPolicy))
			}
		}
	} else {
		s, ok := maybeEscalationPolicy.(*[]*EscalationPolicy)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEscalationPolicy))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &escalationPolicyR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &escalationPolicyR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_monitors`),
		qm.WhereIn(`alert_monitors.escalation_policy_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_monitors")
	}

	var resultSlice []*AlertMonitor
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_monitors")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_monitors")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_monitors")
	}

	if len(alertMonitorAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertMonitors = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertMonitorR{}
			}
			foreign.R.EscalationPolicy = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.EscalationPolicyID) {
				local.R.AlertMonitors = append(local.R.AlertMonitors, foreign)
				if foreign.R == nil {
					foreign.R = &alertMonitorR{}
				}
				foreign.R.EscalationPolicy = local
				break
			}
		}
	}

	return nil
}

// LoadAlertRoutes allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (escalationPolicyL) LoadAlertRoutes(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEscalationPolicy any, mods queries.Applicator) error {
	var slice []*EscalationPolicy
	var object *EscalationPolicy

	if singular {
		var ok bool
		object, ok = maybeEscalationPolicy.(*EscalationPolicy)
		if !ok {
			object = new(EscalationPolicy)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEscalationPolicy))
			}
		}
	} else {
		s, ok := maybeEscalationPolicy.(*[]*EscalationPolicy)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEscalationPolicy))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &escalationPolicyR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &escalationPolicyR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_routes`),
		qm.WhereIn(`alert_routes.escalation_policy_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_routes")
	}

	var resultSlice []*AlertRoute
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_routes")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_routes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_routes")
	}

	if len(alertRouteAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertRoutes = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertRouteR{}
			}
			foreign.R.EscalationPolicy = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.EscalationPolicyID {
				local.R.AlertRoutes = append(local.R.AlertRoutes, foreign)
				if foreign.R == nil {
					foreign.R = &alertRouteR{}
				}
				foreign.R.EscalationPolicy = local
				break
			}
		}
	}

	return nil
}

// LoadDefaultEscalationPolicyAlertSettings allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (escalationPolicyL) LoadDefaultEscalationPolicyAlertSettings(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEscalationPolicy any, mods queries.Applicator) error {
	var slice []*EscalationPolicy
	var object *EscalationPolicy

	if singular {
		var ok bool
		object, ok = maybeEscalationPolicy.(*EscalationPolicy)
		if !ok {
			object = new(EscalationPolicy)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEscalationPolicy))
			}
		}
	} else {
		s, ok := maybeEscalationPolicy.(*[]*EscalationPolicy)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEscalationPolicy))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &escalationPolicyR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &escalationPolicyR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_settings`),
		qm.WhereIn(`alert_settings.default_escalation_policy_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_settings")
	}

	var resultSlice []*AlertSetting
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_settings")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_settings")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_settings")
	}

	if len(alertSettingAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DefaultEscalationPolicyAlertSettings = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertSettingR{}
			}
			foreign.R.DefaultEscalationPolicy = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.DefaultEscalationPolicyID) {
				local.R.DefaultEscalationPolicyAlertSettings = append(local.R.DefaultEscalationPolicyAlertSettings, foreign)
				if foreign.R == nil {
					foreign.R = &alertSettingR{}
				}
				foreign.R.DefaultEscalationPolicy = local
				break
			}
		}
	}

	return nil
}

// LoadAlerts allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (escalationPolicyL) LoadAlerts(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEscalationPolicy any, mods queries.Applicator) error {
	var slice []*EscalationPolicy
	var object *EscalationPolicy

	if singular {
		var ok bool
		object, ok = maybeEscalationPolicy.(*EscalationPolicy)
		if !ok {
			object = new(EscalationPolicy)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEscalationPolicy))
			}
		}
	} else {
		s, ok := maybeEscalationPolicy.(*[]*EscalationPolicy)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEscalationPolicy))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &escalationPolicyR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &escalationPolicyR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alerts`),
		qm.WhereIn(`alerts.escalation_policy_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alerts")
	}

	var resultSlice []*Alert
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alerts")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alerts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alerts")
	}

	if len(alertAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.Alerts = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertR{}
			}
			foreign.R.EscalationPolicy = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.EscalationPolicyID) {
				local.R.Alerts = append(local.R.Alerts, foreign)
				if foreign.R == nil {
					foreign.R = &alertR{}
				}
				foreign.R.EscalationPolicy = local
				break
			}
		}
	}

	return nil
}

// LoadPolicyEscalationPolicyTargets allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (escalationPolicyL) LoadPolicyEscalationPolicyTargets(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEscalationPolicy any, mods queries.Applicator) error {
	var slice []*EscalationPolicy
	var object *EscalationPolicy

	if singular {
		var ok bool
		object, ok = maybeEscalationPolicy.(*EscalationPolicy)
		if !ok {
			object = new(EscalationPolicy)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEscalationPolicy))
			}
		}
	} else {
		s, ok := maybeEscalationPolicy.(*[]*EscalationPolicy)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEscalationPolicy))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &escalationPolicyR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &escalationPolicyR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`escalation_policy_targets`),
		qm.WhereIn(`escalation_policy_targets.policy_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load escalation_policy_targets")
	}

	var resultSlice []*EscalationPolicyTarget
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice escalation_policy_targets")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on escalation_policy_targets")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for escalation_policy_targets")
	}

	if len(escalationPolicyTargetAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PolicyEscalationPolicyTargets = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &escalationPolicyTargetR{}
			}
			foreign.R.Policy = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.PolicyID {
				local.R.PolicyEscalationPolicyTargets = append(local.R.PolicyEscalationPolicyTargets, foreign)
				if foreign.R == nil {
					foreign.R = &escalationPolicyTargetR{}
				}
				foreign.R.Policy = local
				break
			}
		}
	}

	return nil
}

// LoadPolicyEscalationRRStates allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (escalationPolicyL) LoadPolicyEscalationRRStates(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEscalationPolicy any, mods queries.Applicator) error {
	var slice []*EscalationPolicy
	var object *EscalationPolicy

	if singular {
		var ok bool
		object, ok = maybeEscalationPolicy.(*EscalationPolicy)
		if !ok {
			object = new(EscalationPolicy)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEscalationPolicy))
			}
		}
	} else {
		s, ok := maybeEscalationPolicy.(*[]*EscalationPolicy)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEscalationPolicy)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEscalationPolicy))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &escalationPolicyR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &escalationPolicyR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`escalation_rr_state`),
		qm.WhereIn(`escalation_rr_state.policy_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load escalation_rr_state")
	}

	var resultSlice []*EscalationRRState
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice escalation_rr_state")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on escalation_rr_state")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for escalation_rr_state")
	}

	if len(escalationRRStateAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PolicyEscalationRRStates = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &escalationRRStateR{}
			}
			foreign.R.Policy = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.PolicyID {
				local.R.PolicyEscalationRRStates = append(local.R.PolicyEscalationRRStates, foreign)
				if foreign.R == nil {
					foreign.R = &escalationRRStateR{}
				}
				foreign.R.Policy = local
				break
			}
		}
	}

	return nil
}

// SetTeam of the escalationPolicy to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.EscalationPolicies.
func (o *EscalationPolicy) SetTeam(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Team) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"escalation_policies\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
		strmangle.WhereClause("\"", "\"", 2, escalationPolicyPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.TeamID, related.ID)
	if o.R == nil {
		o.R = &escalationPolicyR{
			Team: related,
		}
	} else {
		o.R.Team = related
	}

	if related.R == nil {
		related.R = &teamR{
			EscalationPolicies: EscalationPolicySlice{o},
		}
	} else {
		related.R.EscalationPolicies = append(related.R.EscalationPolicies, o)
	}

	return nil
}

// RemoveTeam relationship.
// Sets o.R.Team to nil.
// Removes o from all passed in related items' relationships struct.
func (o *EscalationPolicy) RemoveTeam(ctx context.Context, exec boil.ContextExecutor, related *Team) error {
	var err error

	queries.SetScanner(&o.TeamID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("team_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Team = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.EscalationPolicies {
		if queries.Equal(o.TeamID, ri.TeamID) {
			continue
		}

		ln := len(related.R.EscalationPolicies)
		if ln > 1 && i < ln-1 {
			related.R.EscalationPolicies[i] = related.R.EscalationPolicies[ln-1]
		}
		related.R.EscalationPolicies = related.R.EscalationPolicies[:ln-1]
		break
	}
	return nil
}

// SetWorkspace of the escalationPolicy to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.EscalationPolicies.
func (o *EscalationPolicy) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"escalation_policies\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, escalationPolicyPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &escalationPolicyR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			EscalationPolicies: EscalationPolicySlice{o},
		}
	} else {
		related.R.EscalationPolicies = append(related.R.EscalationPolicies, o)
	}

	return nil
}

// AddPolicyAlertEscalations adds the given related objects to the existing relationships
// of the escalation_policy, optionally inserting them as new records.
// Appends related to o.R.PolicyAlertEscalations.
// Sets related.R.Policy appropriately.
func (o *EscalationPolicy) AddPolicyAlertEscalations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertEscalation) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PolicyID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_escalations\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"policy_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertEscalationPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PolicyID = o.ID
		}
	}

	if o.R == nil {
		o.R = &escalationPolicyR{
			PolicyAlertEscalations: related,
		}
	} else {
		o.R.PolicyAlertEscalations = append(o.R.PolicyAlertEscalations, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertEscalationR{
				Policy: o,
			}
		} else {
			rel.R.Policy = o
		}
	}
	return nil
}

// AddAlertMonitors adds the given related objects to the existing relationships
// of the escalation_policy, optionally inserting them as new records.
// Appends related to o.R.AlertMonitors.
// Sets related.R.EscalationPolicy appropriately.
func (o *EscalationPolicy) AddAlertMonitors(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertMonitor) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.EscalationPolicyID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_monitors\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"escalation_policy_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertMonitorPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.EscalationPolicyID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &escalationPolicyR{
			AlertMonitors: related,
		}
	} else {
		o.R.AlertMonitors = append(o.R.AlertMonitors, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertMonitorR{
				EscalationPolicy: o,
			}
		} else {
			rel.R.EscalationPolicy = o
		}
	}
	return nil
}

// SetAlertMonitors removes all previously related items of the
// escalation_policy replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.EscalationPolicy's AlertMonitors accordingly.
// Replaces o.R.AlertMonitors with related.
// Sets related.R.EscalationPolicy's AlertMonitors accordingly.
func (o *EscalationPolicy) SetAlertMonitors(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertMonitor) error {
	query := "update \"alert_monitors\" set \"escalation_policy_id\" = null where \"escalation_policy_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.AlertMonitors {
			queries.SetScanner(&rel.EscalationPolicyID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.EscalationPolicy = nil
		}
		o.R.AlertMonitors = nil
	}

	return o.AddAlertMonitors(ctx, exec, insert, related...)
}

// RemoveAlertMonitors relationships from objects passed in.
// Removes related items from R.AlertMonitors (uses pointer comparison, removal does not keep order)
// Sets related.R.EscalationPolicy.
func (o *EscalationPolicy) RemoveAlertMonitors(ctx context.Context, exec boil.ContextExecutor, related ...*AlertMonitor) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.EscalationPolicyID, nil)
		if rel.R != nil {
			rel.R.EscalationPolicy = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("escalation_policy_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.AlertMonitors {
			if rel != ri {
				continue
			}

			ln := len(o.R.AlertMonitors)
			if ln > 1 && i < ln-1 {
				o.R.AlertMonitors[i] = o.R.AlertMonitors[ln-1]
			}
			o.R.AlertMonitors = o.R.AlertMonitors[:ln-1]
			break
		}
	}

	return nil
}

// AddAlertRoutes adds the given related objects to the existing relationships
// of the escalation_policy, optionally inserting them as new records.
// Appends related to o.R.AlertRoutes.
// Sets related.R.EscalationPolicy appropriately.
func (o *EscalationPolicy) AddAlertRoutes(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertRoute) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.EscalationPolicyID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_routes\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"escalation_policy_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertRoutePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.EscalationPolicyID = o.ID
		}
	}

	if o.R == nil {
		o.R = &escalationPolicyR{
			AlertRoutes: related,
		}
	} else {
		o.R.AlertRoutes = append(o.R.AlertRoutes, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertRouteR{
				EscalationPolicy: o,
			}
		} else {
			rel.R.EscalationPolicy = o
		}
	}
	return nil
}

// AddDefaultEscalationPolicyAlertSettings adds the given related objects to the existing relationships
// of the escalation_policy, optionally inserting them as new records.
// Appends related to o.R.DefaultEscalationPolicyAlertSettings.
// Sets related.R.DefaultEscalationPolicy appropriately.
func (o *EscalationPolicy) AddDefaultEscalationPolicyAlertSettings(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertSetting) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.DefaultEscalationPolicyID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_settings\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"default_escalation_policy_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertSettingPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.WorkspaceID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.DefaultEscalationPolicyID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &escalationPolicyR{
			DefaultEscalationPolicyAlertSettings: related,
		}
	} else {
		o.R.DefaultEscalationPolicyAlertSettings = append(o.R.DefaultEscalationPolicyAlertSettings, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertSettingR{
				DefaultEscalationPolicy: o,
			}
		} else {
			rel.R.DefaultEscalationPolicy = o
		}
	}
	return nil
}

// SetDefaultEscalationPolicyAlertSettings removes all previously related items of the
// escalation_policy replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.DefaultEscalationPolicy's DefaultEscalationPolicyAlertSettings accordingly.
// Replaces o.R.DefaultEscalationPolicyAlertSettings with related.
// Sets related.R.DefaultEscalationPolicy's DefaultEscalationPolicyAlertSettings accordingly.
func (o *EscalationPolicy) SetDefaultEscalationPolicyAlertSettings(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertSetting) error {
	query := "update \"alert_settings\" set \"default_escalation_policy_id\" = null where \"default_escalation_policy_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.DefaultEscalationPolicyAlertSettings {
			queries.SetScanner(&rel.DefaultEscalationPolicyID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.DefaultEscalationPolicy = nil
		}
		o.R.DefaultEscalationPolicyAlertSettings = nil
	}

	return o.AddDefaultEscalationPolicyAlertSettings(ctx, exec, insert, related...)
}

// RemoveDefaultEscalationPolicyAlertSettings relationships from objects passed in.
// Removes related items from R.DefaultEscalationPolicyAlertSettings (uses pointer comparison, removal does not keep order)
// Sets related.R.DefaultEscalationPolicy.
func (o *EscalationPolicy) RemoveDefaultEscalationPolicyAlertSettings(ctx context.Context, exec boil.ContextExecutor, related ...*AlertSetting) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.DefaultEscalationPolicyID, nil)
		if rel.R != nil {
			rel.R.DefaultEscalationPolicy = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("default_escalation_policy_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.DefaultEscalationPolicyAlertSettings {
			if rel != ri {
				continue
			}

			ln := len(o.R.DefaultEscalationPolicyAlertSettings)
			if ln > 1 && i < ln-1 {
				o.R.DefaultEscalationPolicyAlertSettings[i] = o.R.DefaultEscalationPolicyAlertSettings[ln-1]
			}
			o.R.DefaultEscalationPolicyAlertSettings = o.R.DefaultEscalationPolicyAlertSettings[:ln-1]
			break
		}
	}

	return nil
}

// AddAlerts adds the given related objects to the existing relationships
// of the escalation_policy, optionally inserting them as new records.
// Appends related to o.R.Alerts.
// Sets related.R.EscalationPolicy appropriately.
func (o *EscalationPolicy) AddAlerts(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Alert) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.EscalationPolicyID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alerts\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"escalation_policy_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.EscalationPolicyID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &escalationPolicyR{
			Alerts: related,
		}
	} else {
		o.R.Alerts = append(o.R.Alerts, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertR{
				EscalationPolicy: o,
			}
		} else {
			rel.R.EscalationPolicy = o
		}
	}
	return nil
}

// SetAlerts removes all previously related items of the
// escalation_policy replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.EscalationPolicy's Alerts accordingly.
// Replaces o.R.Alerts with related.
// Sets related.R.EscalationPolicy's Alerts accordingly.
func (o *EscalationPolicy) SetAlerts(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Alert) error {
	query := "update \"alerts\" set \"escalation_policy_id\" = null where \"escalation_policy_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.Alerts {
			queries.SetScanner(&rel.EscalationPolicyID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.EscalationPolicy = nil
		}
		o.R.Alerts = nil
	}

	return o.AddAlerts(ctx, exec, insert, related...)
}

// RemoveAlerts relationships from objects passed in.
// Removes related items from R.Alerts (uses pointer comparison, removal does not keep order)
// Sets related.R.EscalationPolicy.
func (o *EscalationPolicy) RemoveAlerts(ctx context.Context, exec boil.ContextExecutor, related ...*Alert) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.EscalationPolicyID, nil)
		if rel.R != nil {
			rel.R.EscalationPolicy = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("escalation_policy_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.Alerts {
			if rel != ri {
				continue
			}

			ln := len(o.R.Alerts)
			if ln > 1 && i < ln-1 {
				o.R.Alerts[i] = o.R.Alerts[ln-1]
			}
			o.R.Alerts = o.R.Alerts[:ln-1]
			break
		}
	}

	return nil
}

// AddPolicyEscalationPolicyTargets adds the given related objects to the existing relationships
// of the escalation_policy, optionally inserting them as new records.
// Appends related to o.R.PolicyEscalationPolicyTargets.
// Sets related.R.Policy appropriately.
func (o *EscalationPolicy) AddPolicyEscalationPolicyTargets(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EscalationPolicyTarget) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PolicyID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"escalation_policy_targets\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"policy_id"}),
				strmangle.WhereClause("\"", "\"", 2, escalationPolicyTargetPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PolicyID = o.ID
		}
	}

	if o.R == nil {
		o.R = &escalationPolicyR{
			PolicyEscalationPolicyTargets: related,
		}
	} else {
		o.R.PolicyEscalationPolicyTargets = append(o.R.PolicyEscalationPolicyTargets, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &escalationPolicyTargetR{
				Policy: o,
			}
		} else {
			rel.R.Policy = o
		}
	}
	return nil
}

// AddPolicyEscalationRRStates adds the given related objects to the existing relationships
// of the escalation_policy, optionally inserting them as new records.
// Appends related to o.R.PolicyEscalationRRStates.
// Sets related.R.Policy appropriately.
func (o *EscalationPolicy) AddPolicyEscalationRRStates(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EscalationRRState) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PolicyID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"escalation_rr_state\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"policy_id"}),
				strmangle.WhereClause("\"", "\"", 2, escalationRRStatePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.PolicyID, rel.NodeID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PolicyID = o.ID
		}
	}

	if o.R == nil {
		o.R = &escalationPolicyR{
			PolicyEscalationRRStates: related,
		}
	} else {
		o.R.PolicyEscalationRRStates = append(o.R.PolicyEscalationRRStates, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &escalationRRStateR{
				Policy: o,
			}
		} else {
			rel.R.Policy = o
		}
	}
	return nil
}

// EscalationPolicies retrieves all the records using an executor.
func EscalationPolicies(mods ...qm.QueryMod) escalationPolicyQuery {
	mods = append(mods, qm.From("\"escalation_policies\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"escalation_policies\".*"})
	}

	return escalationPolicyQuery{q}
}

// FindEscalationPolicy retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEscalationPolicy(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*EscalationPolicy, error) {
	escalationPolicyObj := &EscalationPolicy{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"escalation_policies\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, escalationPolicyObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from escalation_policies")
	}

	if err = escalationPolicyObj.doAfterSelectHooks(ctx, exec); err != nil {
		return escalationPolicyObj, err
	}

	return escalationPolicyObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *EscalationPolicy) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no escalation_policies provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(escalationPolicyColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	escalationPolicyInsertCacheMut.RLock()
	cache, cached := escalationPolicyInsertCache[key]
	escalationPolicyInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			escalationPolicyAllColumns,
			escalationPolicyColumnsWithDefault,
			escalationPolicyColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(escalationPolicyType, escalationPolicyMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(escalationPolicyType, escalationPolicyMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"escalation_policies\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"escalation_policies\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into escalation_policies")
	}

	if !cached {
		escalationPolicyInsertCacheMut.Lock()
		escalationPolicyInsertCache[key] = cache
		escalationPolicyInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the EscalationPolicy.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *EscalationPolicy) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	escalationPolicyUpdateCacheMut.RLock()
	cache, cached := escalationPolicyUpdateCache[key]
	escalationPolicyUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			escalationPolicyAllColumns,
			escalationPolicyPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update escalation_policies, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"escalation_policies\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, escalationPolicyPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(escalationPolicyType, escalationPolicyMapping, append(wl, escalationPolicyPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update escalation_policies row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for escalation_policies")
	}

	if !cached {
		escalationPolicyUpdateCacheMut.Lock()
		escalationPolicyUpdateCache[key] = cache
		escalationPolicyUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q escalationPolicyQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for escalation_policies")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for escalation_policies")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EscalationPolicySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), escalationPolicyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"escalation_policies\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, escalationPolicyPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in escalationPolicy slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all escalationPolicy")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *EscalationPolicy) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no escalation_policies provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(escalationPolicyColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	escalationPolicyUpsertCacheMut.RLock()
	cache, cached := escalationPolicyUpsertCache[key]
	escalationPolicyUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			escalationPolicyAllColumns,
			escalationPolicyColumnsWithDefault,
			escalationPolicyColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			escalationPolicyAllColumns,
			escalationPolicyPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert escalation_policies, could not build update column list")
		}

		ret := strmangle.SetComplement(escalationPolicyAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(escalationPolicyPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert escalation_policies, could not build conflict column list")
			}

			conflict = make([]string, len(escalationPolicyPrimaryKeyColumns))
			copy(conflict, escalationPolicyPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"escalation_policies\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(escalationPolicyType, escalationPolicyMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(escalationPolicyType, escalationPolicyMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert escalation_policies")
	}

	if !cached {
		escalationPolicyUpsertCacheMut.Lock()
		escalationPolicyUpsertCache[key] = cache
		escalationPolicyUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single EscalationPolicy record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *EscalationPolicy) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no EscalationPolicy provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), escalationPolicyPrimaryKeyMapping)
	sql := "DELETE FROM \"escalation_policies\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from escalation_policies")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for escalation_policies")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q escalationPolicyQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no escalationPolicyQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from escalation_policies")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for escalation_policies")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EscalationPolicySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(escalationPolicyBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), escalationPolicyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"escalation_policies\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, escalationPolicyPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from escalationPolicy slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for escalation_policies")
	}

	if len(escalationPolicyAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *EscalationPolicy) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEscalationPolicy(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EscalationPolicySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EscalationPolicySlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), escalationPolicyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"escalation_policies\".* FROM \"escalation_policies\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, escalationPolicyPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in EscalationPolicySlice")
	}

	*o = slice

	return nil
}

// EscalationPolicyExists checks if the EscalationPolicy row exists.
func EscalationPolicyExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"escalation_policies\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if escalation_policies exists")
	}

	return exists, nil
}

// Exists checks if the EscalationPolicy row exists.
func (o *EscalationPolicy) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return EscalationPolicyExists(ctx, exec, o.ID)
}
