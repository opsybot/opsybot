// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// Alert is an object representing the database table.
type Alert struct {
	ID                    string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID           string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	SourceID              string      `boil:"source_id" json:"source_id" toml:"source_id" yaml:"source_id"`
	ParentAlertID         null.String `boil:"parent_alert_id" json:"parent_alert_id,omitempty" toml:"parent_alert_id" yaml:"parent_alert_id,omitempty"`
	DedupKey              string      `boil:"dedup_key" json:"dedup_key" toml:"dedup_key" yaml:"dedup_key"`
	GroupKey              string      `boil:"group_key" json:"group_key" toml:"group_key" yaml:"group_key"`
	Title                 string      `boil:"title" json:"title" toml:"title" yaml:"title"`
	Description           string      `boil:"description" json:"description" toml:"description" yaml:"description"`
	Severity              string      `boil:"severity" json:"severity" toml:"severity" yaml:"severity"`
	Status                string      `boil:"status" json:"status" toml:"status" yaml:"status"`
	SourceLabel           string      `boil:"source_label" json:"source_label" toml:"source_label" yaml:"source_label"`
	ServiceName           string      `boil:"service_name" json:"service_name" toml:"service_name" yaml:"service_name"`
	Labels                types.JSON  `boil:"labels" json:"labels" toml:"labels" yaml:"labels"`
	Count                 int         `boil:"count" json:"count" toml:"count" yaml:"count"`
	StartedAt             time.Time   `boil:"started_at" json:"started_at" toml:"started_at" yaml:"started_at"`
	LastSeenAt            time.Time   `boil:"last_seen_at" json:"last_seen_at" toml:"last_seen_at" yaml:"last_seen_at"`
	EndedAt               null.Time   `boil:"ended_at" json:"ended_at,omitempty" toml:"ended_at" yaml:"ended_at,omitempty"`
	AckedAt               null.Time   `boil:"acked_at" json:"acked_at,omitempty" toml:"acked_at" yaml:"acked_at,omitempty"`
	ResolvedAt            null.Time   `boil:"resolved_at" json:"resolved_at,omitempty" toml:"resolved_at" yaml:"resolved_at,omitempty"`
	AckedByUserID         null.String `boil:"acked_by_user_id" json:"acked_by_user_id,omitempty" toml:"acked_by_user_id" yaml:"acked_by_user_id,omitempty"`
	AckedByLabel          string      `boil:"acked_by_label" json:"acked_by_label" toml:"acked_by_label" yaml:"acked_by_label"`
	ResolveMode           string      `boil:"resolve_mode" json:"resolve_mode" toml:"resolve_mode" yaml:"resolve_mode"`
	SuppressedBySilenceID null.String `boil:"suppressed_by_silence_id" json:"suppressed_by_silence_id,omitempty" toml:"suppressed_by_silence_id" yaml:"suppressed_by_silence_id,omitempty"`
	SuppressedAt          null.Time   `boil:"suppressed_at" json:"suppressed_at,omitempty" toml:"suppressed_at" yaml:"suppressed_at,omitempty"`
	Payload               string      `boil:"payload" json:"payload" toml:"payload" yaml:"payload"`
	CreatedAt             time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt             time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	EscalationPolicyID    null.String `boil:"escalation_policy_id" json:"escalation_policy_id,omitempty" toml:"escalation_policy_id" yaml:"escalation_policy_id,omitempty"`

	R *alertR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L alertL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AlertColumns = struct {
	ID                    string
	WorkspaceID           string
	SourceID              string
	ParentAlertID         string
	DedupKey              string
	GroupKey              string
	Title                 string
	Description           string
	Severity              string
	Status                string
	SourceLabel           string
	ServiceName           string
	Labels                string
	Count                 string
	StartedAt             string
	LastSeenAt            string
	EndedAt               string
	AckedAt               string
	ResolvedAt            string
	AckedByUserID         string
	AckedByLabel          string
	ResolveMode           string
	SuppressedBySilenceID string
	SuppressedAt          string
	Payload               string
	CreatedAt             string
	UpdatedAt             string
	EscalationPolicyID    string
}{
	ID:                    "id",
	WorkspaceID:           "workspace_id",
	SourceID:              "source_id",
	ParentAlertID:         "parent_alert_id",
	DedupKey:              "dedup_key",
	GroupKey:              "group_key",
	Title:                 "title",
	Description:           "description",
	Severity:              "severity",
	Status:                "status",
	SourceLabel:           "source_label",
	ServiceName:           "service_name",
	Labels:                "labels",
	Count:                 "count",
	StartedAt:             "started_at",
	LastSeenAt:            "last_seen_at",
	EndedAt:               "ended_at",
	AckedAt:               "acked_at",
	ResolvedAt:            "resolved_at",
	AckedByUserID:         "acked_by_user_id",
	AckedByLabel:          "acked_by_label",
	ResolveMode:           "resolve_mode",
	SuppressedBySilenceID: "suppressed_by_silence_id",
	SuppressedAt:          "suppressed_at",
	Payload:               "payload",
	CreatedAt:             "created_at",
	UpdatedAt:             "updated_at",
	EscalationPolicyID:    "escalation_policy_id",
}

var AlertTableColumns = struct {
	ID                    string
	WorkspaceID           string
	SourceID              string
	ParentAlertID         string
	DedupKey              string
	GroupKey              string
	Title                 string
	Description           string
	Severity              string
	Status                string
	SourceLabel           string
	ServiceName           string
	Labels                string
	Count                 string
	StartedAt             string
	LastSeenAt            string
	EndedAt               string
	AckedAt               string
	ResolvedAt            string
	AckedByUserID         string
	AckedByLabel          string
	ResolveMode           string
	SuppressedBySilenceID string
	SuppressedAt          string
	Payload               string
	CreatedAt             string
	UpdatedAt             string
	EscalationPolicyID    string
}{
	ID:                    "alerts.id",
	WorkspaceID:           "alerts.workspace_id",
	SourceID:              "alerts.source_id",
	ParentAlertID:         "alerts.parent_alert_id",
	DedupKey:              "alerts.dedup_key",
	GroupKey:              "alerts.group_key",
	Title:                 "alerts.title",
	Description:           "alerts.description",
	Severity:              "alerts.severity",
	Status:                "alerts.status",
	SourceLabel:           "alerts.source_label",
	ServiceName:           "alerts.service_name",
	Labels:                "alerts.labels",
	Count:                 "alerts.count",
	StartedAt:             "alerts.started_at",
	LastSeenAt:            "alerts.last_seen_at",
	EndedAt:               "alerts.ended_at",
	AckedAt:               "alerts.acked_at",
	ResolvedAt:            "alerts.resolved_at",
	AckedByUserID:         "alerts.acked_by_user_id",
	AckedByLabel:          "alerts.acked_by_label",
	ResolveMode:           "alerts.resolve_mode",
	SuppressedBySilenceID: "alerts.suppressed_by_silence_id",
	SuppressedAt:          "alerts.suppressed_at",
	Payload:               "alerts.payload",
	CreatedAt:             "alerts.created_at",
	UpdatedAt:             "alerts.updated_at",
	EscalationPolicyID:    "alerts.escalation_policy_id",
}

// Generated where

var AlertWhere = struct {
	ID                    whereHelperstring
	WorkspaceID           whereHelperstring
	SourceID              whereHelperstring
	ParentAlertID         whereHelpernull_String
	DedupKey              whereHelperstring
	GroupKey              whereHelperstring
	Title                 whereHelperstring
	Description           whereHelperstring
	Severity              whereHelperstring
	Status                whereHelperstring
	SourceLabel           whereHelperstring
	ServiceName           whereHelperstring
	Labels                whereHelpertypes_JSON
	Count                 whereHelperint
	StartedAt             whereHelpertime_Time
	LastSeenAt            whereHelpertime_Time
	EndedAt               whereHelpernull_Time
	AckedAt               whereHelpernull_Time
	ResolvedAt            whereHelpernull_Time
	AckedByUserID         whereHelpernull_String
	AckedByLabel          whereHelperstring
	ResolveMode           whereHelperstring
	SuppressedBySilenceID whereHelpernull_String
	SuppressedAt          whereHelpernull_Time
	Payload               whereHelperstring
	CreatedAt             whereHelpertime_Time
	UpdatedAt             whereHelpertime_Time
	EscalationPolicyID    whereHelpernull_String
}{
	ID:                    whereHelperstring{field: "\"alerts\".\"id\""},
	WorkspaceID:           whereHelperstring{field: "\"alerts\".\"workspace_id\""},
	SourceID:              whereHelperstring{field: "\"alerts\".\"source_id\""},
	ParentAlertID:         whereHelpernull_String{field: "\"alerts\".\"parent_alert_id\""},
	DedupKey:              whereHelperstring{field: "\"alerts\".\"dedup_key\""},
	GroupKey:              whereHelperstring{field: "\"alerts\".\"group_key\""},
	Title:                 whereHelperstring{field: "\"alerts\".\"title\""},
	Description:           whereHelperstring{field: "\"alerts\".\"description\""},
	Severity:              whereHelperstring{field: "\"alerts\".\"severity\""},
	Status:                whereHelperstring{field: "\"alerts\".\"status\""},
	SourceLabel:           whereHelperstring{field: "\"alerts\".\"source_label\""},
	ServiceName:           whereHelperstring{field: "\"alerts\".\"service_name\""},
	Labels:                whereHelpertypes_JSON{field: "\"alerts\".\"labels\""},
	Count:                 whereHelperint{field: "\"alerts\".\"count\""},
	StartedAt:             whereHelpertime_Time{field: "\"alerts\".\"started_at\""},
	LastSeenAt:            whereHelpertime_Time{field: "\"alerts\".\"last_seen_at\""},
	EndedAt:               whereHelpernull_Time{field: "\"alerts\".\"ended_at\""},
	AckedAt:               whereHelpernull_Time{field: "\"alerts\".\"acked_at\""},
	ResolvedAt:            whereHelpernull_Time{field: "\"alerts\".\"resolved_at\""},
	AckedByUserID:         whereHelpernull_String{field: "\"alerts\".\"acked_by_user_id\""},
	AckedByLabel:          whereHelperstring{field: "\"alerts\".\"acked_by_label\""},
	ResolveMode:           whereHelperstring{field: "\"alerts\".\"resolve_mode\""},
	SuppressedBySilenceID: whereHelpernull_String{field: "\"alerts\".\"suppressed_by_silence_id\""},
	SuppressedAt:          whereHelpernull_Time{field: "\"alerts\".\"suppressed_at\""},
	Payload:               whereHelperstring{field: "\"alerts\".\"payload\""},
	CreatedAt:             whereHelpertime_Time{field: "\"alerts\".\"created_at\""},
	UpdatedAt:             whereHelpertime_Time{field: "\"alerts\".\"updated_at\""},
	EscalationPolicyID:    whereHelpernull_String{field: "\"alerts\".\"escalation_policy_id\""},
}

// AlertRels is where relationship names are stored.
var AlertRels = struct {
	AckedByUser         string
	EscalationPolicy    string
	ParentAlert         string
	SuppressedBySilence string
	Workspace           string
	AlertEscalation     string
	AlertEvents         string
	AlertIngestEvents   string
	AlertLinks          string
	ParentAlertAlerts   string
}{
	AckedByUser:         "AckedByUser",
	EscalationPolicy:    "EscalationPolicy",
	ParentAlert:         "ParentAlert",
	SuppressedBySilence: "SuppressedBySilence",
	Workspace:           "Workspace",
	AlertEscalation:     "AlertEscalation",
	AlertEvents:         "AlertEvents",
	AlertIngestEvents:   "AlertIngestEvents",
	AlertLinks:          "AlertLinks",
	ParentAlertAlerts:   "ParentAlertAlerts",
}

// alertR is where relationships are stored.
type alertR struct {
	AckedByUser         *User                 `boil:"AckedByUser" json:"AckedByUser" toml:"AckedByUser" yaml:"AckedByUser"`
	EscalationPolicy    *EscalationPolicy     `boil:"EscalationPolicy" json:"EscalationPolicy" toml:"EscalationPolicy" yaml:"EscalationPolicy"`
	ParentAlert         *Alert                `boil:"ParentAlert" json:"ParentAlert" toml:"ParentAlert" yaml:"ParentAlert"`
	SuppressedBySilence *AlertSilence         `boil:"SuppressedBySilence" json:"SuppressedBySilence" toml:"SuppressedBySilence" yaml:"SuppressedBySilence"`
	Workspace           *Workspace            `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
	AlertEscalation     *AlertEscalation      `boil:"AlertEscalation" json:"AlertEscalation" toml:"AlertEscalation" yaml:"AlertEscalation"`
	AlertEvents         AlertEventSlice       `boil:"AlertEvents" json:"AlertEvents" toml:"AlertEvents" yaml:"AlertEvents"`
	AlertIngestEvents   AlertIngestEventSlice `boil:"AlertIngestEvents" json:"AlertIngestEvents" toml:"AlertIngestEvents" yaml:"AlertIngestEvents"`
	AlertLinks          AlertLinkSlice        `boil:"AlertLinks" json:"AlertLinks" toml:"AlertLinks" yaml:"AlertLinks"`
	ParentAlertAlerts   AlertSlice            `boil:"ParentAlertAlerts" json:"ParentAlertAlerts" toml:"ParentAlertAlerts" yaml:"ParentAlertAlerts"`
}

// NewStruct creates a new relationship struct
func (*alertR) NewStruct() *alertR {
	return &alertR{}
}

func (o *Alert) GetAckedByUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetAckedByUser()
}

func (r *alertR) GetAckedByUser() *User {
	if r == nil {
		return nil
	}

	return r.AckedByUser
}

func (o *Alert) GetEscalationPolicy() *EscalationPolicy {
	if o == nil {
		return nil
	}

	return o.R.GetEscalationPolicy()
}

func (r *alertR) GetEscalationPolicy() *EscalationPolicy {
	if r == nil {
		return nil
	}

	return r.EscalationPolicy
}

func (o *Alert) GetParentAlert() *Alert {
	if o == nil {
		return nil
	}

	return o.R.GetParentAlert()
}

func (r *alertR) GetParentAlert() *Alert {
	if r == nil {
		return nil
	}

	return r.ParentAlert
}

func (o *Alert) GetSuppressedBySilence() *AlertSilence {
	if o == nil {
		return nil
	}

	return o.R.GetSuppressedBySilence()
}

func (r *alertR) GetSuppressedBySilence() *AlertSilence {
	if r == nil {
		return nil
	}

	return r.SuppressedBySilence
}

func (o *Alert) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *alertR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

func (o *Alert) GetAlertEscalation() *AlertEscalation {
	if o == nil {
		return nil
	}

	return o.R.GetAlertEscalation()
}

func (r *alertR) GetAlertEscalation() *AlertEscalation {
	if r == nil {
		return nil
	}

	return r.AlertEscalation
}

func (o *Alert) GetAlertEvents() AlertEventSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertEvents()
}

func (r *alertR) GetAlertEvents() AlertEventSlice {
	if r == nil {
		return nil
	}

	return r.AlertEvents
}

func (o *Alert) GetAlertIngestEvents() AlertIngestEventSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertIngestEvents()
}

func (r *alertR) GetAlertIngestEvents() AlertIngestEventSlice {
	if r == nil {
		return nil
	}

	return r.AlertIngestEvents
}

func (o *Alert) GetAlertLinks() AlertLinkSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertLinks()
}

func (r *alertR) GetAlertLinks() AlertLinkSlice {
	if r == nil {
		return nil
	}

	return r.AlertLinks
}

func (o *Alert) GetParentAlertAlerts() AlertSlice {
	if o == nil {
		return nil
	}

	return o.R.GetParentAlertAlerts()
}

func (r *alertR) GetParentAlertAlerts() AlertSlice {
	if r == nil {
		return nil
	}

	return r.ParentAlertAlerts
}

// alertL is where Load methods for each relationship are stored.
type alertL struct{}

var (
	alertAllColumns            = []string{"id", "workspace_id", "source_id", "parent_alert_id", "dedup_key", "group_key", "title", "description", "severity", "status", "source_label", "service_name", "labels", "count", "started_at", "last_seen_at", "ended_at", "acked_at", "resolved_at", "acked_by_user_id", "acked_by_label", "resolve_mode", "suppressed_by_silence_id", "suppressed_at", "payload", "created_at", "updated_at", "escalation_policy_id"}
	alertColumnsWithoutDefault = []string{"workspace_id", "source_id", "dedup_key", "title", "severity", "started_at", "last_seen_at"}
	alertColumnsWithDefault    = []string{"id", "parent_alert_id", "group_key", "description", "status", "source_label", "service_name", "labels", "count", "ended_at", "acked_at", "resolved_at", "acked_by_user_id", "acked_by_label", "resolve_mode", "suppressed_by_silence_id", "suppressed_at", "payload", "created_at", "updated_at", "escalation_policy_id"}
	alertPrimaryKeyColumns     = []string{"id"}
	alertGeneratedColumns      = []string{}
)

type (
	// AlertSlice is an alias for a slice of pointers to Alert.
	// This should almost always be used instead of []Alert.
	AlertSlice []*Alert
	// AlertHook is the signature for custom Alert hook methods
	AlertHook func(context.Context, boil.ContextExecutor, *Alert) error

	alertQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	alertType                 = reflect.TypeOf(&Alert{})
	alertMapping              = queries.MakeStructMapping(alertType)
	alertPrimaryKeyMapping, _ = queries.BindMapping(alertType, alertMapping, alertPrimaryKeyColumns)
	alertInsertCacheMut       sync.RWMutex
	alertInsertCache          = make(map[string]insertCache)
	alertUpdateCacheMut       sync.RWMutex
	alertUpdateCache          = make(map[string]updateCache)
	alertUpsertCacheMut       sync.RWMutex
	alertUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var alertAfterSelectMu sync.Mutex
var alertAfterSelectHooks []AlertHook

var alertBeforeInsertMu sync.Mutex
var alertBeforeInsertHooks []AlertHook
var alertAfterInsertMu sync.Mutex
var alertAfterInsertHooks []AlertHook

var alertBeforeUpdateMu sync.Mutex
var alertBeforeUpdateHooks []AlertHook
var alertAfterUpdateMu sync.Mutex
var alertAfterUpdateHooks []AlertHook

var alertBeforeDeleteMu sync.Mutex
var alertBeforeDeleteHooks []AlertHook
var alertAfterDeleteMu sync.Mutex
var alertAfterDeleteHooks []AlertHook

var alertBeforeUpsertMu sync.Mutex
var alertBeforeUpsertHooks []AlertHook
var alertAfterUpsertMu sync.Mutex
var alertAfterUpsertHooks []AlertHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Alert) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Alert) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Alert) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Alert) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Alert) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Alert) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Alert) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Alert) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Alert) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAlertHook registers your hook function for all future operations.
func AddAlertHook(hookPoint boil.HookPoint, alertHook AlertHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		alertAfterSelectMu.Lock()
		alertAfterSelectHooks = append(alertAfterSelectHooks, alertHook)
		alertAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		alertBeforeInsertMu.Lock()
		alertBeforeInsertHooks = append(alertBeforeInsertHooks, alertHook)
		alertBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		alertAfterInsertMu.Lock()
		alertAfterInsertHooks = append(alertAfterInsertHooks, alertHook)
		alertAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		alertBeforeUpdateMu.Lock()
		alertBeforeUpdateHooks = append(alertBeforeUpdateHooks, alertHook)
		alertBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		alertAfterUpdateMu.Lock()
		alertAfterUpdateHooks = append(alertAfterUpdateHooks, alertHook)
		alertAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		alertBeforeDeleteMu.Lock()
		alertBeforeDeleteHooks = append(alertBeforeDeleteHooks, alertHook)
		alertBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		alertAfterDeleteMu.Lock()
		alertAfterDeleteHooks = append(alertAfterDeleteHooks, alertHook)
		alertAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		alertBeforeUpsertMu.Lock()
		alertBeforeUpsertHooks = append(alertBeforeUpsertHooks, alertHook)
		alertBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		alertAfterUpsertMu.Lock()
		alertAfterUpsertHooks = append(alertAfterUpsertHooks, alertHook)
		alertAfterUpsertMu.Unlock()
	}
}

// One returns a single alert record from the query.
func (q alertQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Alert, error) {
	o := &Alert{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for alerts")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Alert records from the query.
func (q alertQuery) All(ctx context.Context, exec boil.ContextExecutor) (AlertSlice, error) {
	var o []*Alert

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to Alert slice")
	}

	if len(alertAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Alert records in the query.
func (q alertQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count alerts rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q alertQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if alerts exists")
	}

	return count > 0, nil
}

// AckedByUser pointed to by the foreign key.
func (o *Alert) AckedByUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AckedByUserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// EscalationPolicy pointed to by the foreign key.
func (o *Alert) EscalationPolicy(mods ...qm.QueryMod) escalationPolicyQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.EscalationPolicyID),
	}

	queryMods = append(queryMods, mods...)

	return EscalationPolicies(queryMods...)
}

// ParentAlert pointed to by the foreign key.
func (o *Alert) ParentAlert(mods ...qm.QueryMod) alertQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ParentAlertID),
	}

	queryMods = append(queryMods, mods...)

	return Alerts(queryMods...)
}

// SuppressedBySilence pointed to by the foreign key.
func (o *Alert) SuppressedBySilence(mods ...qm.QueryMod) alertSilenceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SuppressedBySilenceID),
	}

	queryMods = append(queryMods, mods...)

	return AlertSilences(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *Alert) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// AlertEscalation pointed to by the foreign key.
func (o *Alert) AlertEscalation(mods ...qm.QueryMod) alertEscalationQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"alert_id\" = ?", o.ID),
	}

	queryMods = append(queryMods, mods...)

	return AlertEscalations(queryMods...)
}

// AlertEvents retrieves all the alert_event's AlertEvents with an executor.
func (o *Alert) AlertEvents(mods ...qm.QueryMod) alertEventQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_events\".\"alert_id\"=?", o.ID),
	)

	return AlertEvents(queryMods...)
}

// AlertIngestEvents retrieves all the alert_ingest_event's AlertIngestEvents with an executor.
func (o *Alert) AlertIngestEvents(mods ...qm.QueryMod) alertIngestEventQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_ingest_events\".\"alert_id\"=?", o.ID),
	)

	return AlertIngestEvents(queryMods...)
}

// AlertLinks retrieves all the alert_link's AlertLinks with an executor.
func (o *Alert) AlertLinks(mods ...qm.QueryMod) alertLinkQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_links\".\"alert_id\"=?", o.ID),
	)

	return AlertLinks(queryMods...)
}

// ParentAlertAlerts retrieves all the alert's Alerts with an executor via parent_alert_id column.
func (o *Alert) ParentAlertAlerts(mods ...qm.QueryMod) alertQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alerts\".\"parent_alert_id\"=?", o.ID),
	)

	return Alerts(queryMods...)
}

// LoadAckedByUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertL) LoadAckedByUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		if !queries.IsNil(object.AckedByUserID) {
			args[object.AckedByUserID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}

			if !queries.IsNil(obj.AckedByUserID) {
				args[obj.AckedByUserID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.AckedByUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.AckedByUserAlerts = append(foreign.R.AckedByUserAlerts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.AckedByUserID, foreign.ID) {
				local.R.AckedByUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.AckedByUserAlerts = append(foreign.R.AckedByUserAlerts, local)
				break
			}
		}
	}

	return nil
}

// LoadEscalationPolicy allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertL) LoadEscalationPolicy(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		if !queries.IsNil(object.EscalationPolicyID) {
			args[object.EscalationPolicyID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}

			if !queries.IsNil(obj.EscalationPolicyID) {
				args[obj.EscalationPolicyID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`escalation_policies`),
		qm.WhereIn(`escalation_policies.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EscalationPolicy")
	}

	var resultSlice []*EscalationPolicy
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EscalationPolicy")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for escalation_policies")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for escalation_policies")
	}

	if len(escalationPolicyAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.EscalationPolicy = foreign
		if foreign.R == nil {
			foreign.R = &escalationPolicyR{}
		}
		foreign.R.Alerts = append(foreign.R.Alerts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.EscalationPolicyID, foreign.ID) {
				local.R.EscalationPolicy = foreign
				if foreign.R == nil {
					foreign.R = &escalationPolicyR{}
				}
				foreign.R.Alerts = append(foreign.R.Alerts, local)
				break
			}
		}
	}

	return nil
}

// LoadParentAlert allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertL) LoadParentAlert(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		if !queries.IsNil(object.ParentAlertID) {
			args[object.ParentAlertID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}

			if !queries.IsNil(obj.ParentAlertID) {
				args[obj.ParentAlertID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alerts`),
		qm.WhereIn(`alerts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Alert")
	}

	var resultSlice []*Alert
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Alert")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for alerts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alerts")
	}

	if len(alertAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ParentAlert = foreign
		if foreign.R == nil {
			foreign.R = &alertR{}
		}
		foreign.R.ParentAlertAlerts = append(foreign.R.ParentAlertAlerts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ParentAlertID, foreign.ID) {
				local.R.ParentAlert = foreign
				if foreign.R == nil {
					foreign.R = &alertR{}
				}
				foreign.R.ParentAlertAlerts = append(foreign.R.ParentAlertAlerts, local)
				break
			}
		}
	}

	return nil
}

// LoadSuppressedBySilence allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertL) LoadSuppressedBySilence(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		if !queries.IsNil(object.SuppressedBySilenceID) {
			args[object.SuppressedBySilenceID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}

			if !queries.IsNil(obj.SuppressedBySilenceID) {
				args[obj.SuppressedBySilenceID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_silences`),
		qm.WhereIn(`alert_silences.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AlertSilence")
	}

	var resultSlice []*AlertSilence
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AlertSilence")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for alert_silences")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_silences")
	}

	if len(alertSilenceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.SuppressedBySilence = foreign
		if foreign.R == nil {
			foreign.R = &alertSilenceR{}
		}
		foreign.R.SuppressedBySilenceAlerts = append(foreign.R.SuppressedBySilenceAlerts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.SuppressedBySilenceID, foreign.ID) {
				local.R.SuppressedBySilence = foreign
				if foreign.R == nil {
					foreign.R = &alertSilenceR{}
				}
				foreign.R.SuppressedBySilenceAlerts = append(foreign.R.SuppressedBySilenceAlerts, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.Alerts = append(foreign.R.Alerts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.Alerts = append(foreign.R.Alerts, local)
				break
			}
		}
	}

	return nil
}

// LoadAlertEscalation allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-1 relationship.
func (alertL) LoadAlertEscalation(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}

			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_escalations`),
		qm.WhereIn(`alert_escalations.alert_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AlertEscalation")
	}

	var resultSlice []*AlertEscalation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AlertEscalation")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for alert_escalations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_escalations")
	}

	if len(alertEscalationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.AlertEscalation = foreign
		if foreign.R == nil {
			foreign.R = &alertEscalationR{}
		}
		foreign.R.Alert = object
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ID == foreign.AlertID {
				local.R.AlertEscalation = foreign
				if foreign.R == nil {
					foreign.R = &alertEscalationR{}
				}
				foreign.R.Alert = local
				break
			}
		}
	}

	return nil
}

// LoadAlertEvents allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (alertL) LoadAlertEvents(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_events`),
		qm.WhereIn(`alert_events.alert_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_events")
	}

	var resultSlice []*AlertEvent
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_events")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_events")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_events")
	}

	if len(alertEventAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertEvents = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertEventR{}
			}
			foreign.R.Alert = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AlertID {
				local.R.AlertEvents = append(local.R.AlertEvents, foreign)
				if foreign.R == nil {
					foreign.R = &alertEventR{}
				}
				foreign.R.Alert = local
				break
			}
		}
	}

	return nil
}

// LoadAlertIngestEvents allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (alertL) LoadAlertIngestEvents(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_ingest_events`),
		qm.WhereIn(`alert_ingest_events.alert_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_ingest_events")
	}

	var resultSlice []*AlertIngestEvent
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_ingest_events")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_ingest_events")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_ingest_events")
	}

	if len(alertIngestEventAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertIngestEvents = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertIngestEventR{}
			}
			foreign.R.Alert = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.AlertID) {
				local.R.AlertIngestEvents = append(local.R.AlertIngestEvents, foreign)
				if foreign.R == nil {
					foreign.R = &alertIngestEventR{}
				}
				foreign.R.Alert = local
				break
			}
		}
	}

	return nil
}

// LoadAlertLinks allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (alertL) LoadAlertLinks(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_links`),
		qm.WhereIn(`alert_links.alert_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_links")
	}

	var resultSlice []*AlertLink
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_links")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_links")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_links")
	}

	if len(alertLinkAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertLinks = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertLinkR{}
			}
			foreign.R.Alert = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AlertID {
				local.R.AlertLinks = append(local.R.AlertLinks, foreign)
				if foreign.R == nil {
					foreign.R = &alertLinkR{}
				}
				foreign.R.Alert = local
				break
			}
		}
	}

	return nil
}

// LoadParentAlertAlerts allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (alertL) LoadParentAlertAlerts(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlert any, mods queries.Applicator) error {
	var slice []*Alert
	var object *Alert

	if singular {
		var ok bool
		object, ok = maybeAlert.(*Alert)
		if !ok {
			object = new(Alert)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlert))
			}
		}
	} else {
		s, ok := maybeAlert.(*[]*Alert)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlert)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlert))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alerts`),
		qm.WhereIn(`alerts.parent_alert_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alerts")
	}

	var resultSlice []*Alert
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alerts")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alerts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alerts")
	}

	if len(alertAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.ParentAlertAlerts = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertR{}
			}
			foreign.R.ParentAlert = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.ParentAlertID) {
				local.R.ParentAlertAlerts = append(local.R.ParentAlertAlerts, foreign)
				if foreign.R == nil {
					foreign.R = &alertR{}
				}
				foreign.R.ParentAlert = local
				break
			}
		}
	}

	return nil
}

// SetAckedByUser of the alert to the related item.
// Sets o.R.AckedByUser to related.
// Adds o to related.R.AckedByUserAlerts.
func (o *Alert) SetAckedByUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alerts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"acked_by_user_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.AckedByUserID, related.ID)
	if o.R == nil {
		o.R = &alertR{
			AckedByUser: related,
		}
	} else {
		o.R.AckedByUser = related
	}

	if related.R == nil {
		related.R = &userR{
			AckedByUserAlerts: AlertSlice{o},
		}
	} else {
		related.R.AckedByUserAlerts = append(related.R.AckedByUserAlerts, o)
	}

	return nil
}

// RemoveAckedByUser relationship.
// Sets o.R.AckedByUser to nil.
// Removes o from all passed in related items' relationships struct.
func (o *Alert) RemoveAckedByUser(ctx context.Context, exec boil.ContextExecutor, related *User) error {
	var err error

	queries.SetScanner(&o.AckedByUserID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("acked_by_user_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.AckedByUser = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.AckedByUserAlerts {
		if queries.Equal(o.AckedByUserID, ri.AckedByUserID) {
			continue
		}

		ln := len(related.R.AckedByUserAlerts)
		if ln > 1 && i < ln-1 {
			related.R.AckedByUserAlerts[i] = related.R.AckedByUserAlerts[ln-1]
		}
		related.R.AckedByUserAlerts = related.R.AckedByUserAlerts[:ln-1]
		break
	}
	return nil
}

// SetEscalationPolicy of the alert to the related item.
// Sets o.R.EscalationPolicy to related.
// Adds o to related.R.Alerts.
func (o *Alert) SetEscalationPolicy(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EscalationPolicy) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alerts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"escalation_policy_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.EscalationPolicyID, related.ID)
	if o.R == nil {
		o.R = &alertR{
			EscalationPolicy: related,
		}
	} else {
		o.R.EscalationPolicy = related
	}

	if related.R == nil {
		related.R = &escalationPolicyR{
			Alerts: AlertSlice{o},
		}
	} else {
		related.R.Alerts = append(related.R.Alerts, o)
	}

	return nil
}

// RemoveEscalationPolicy relationship.
// Sets o.R.EscalationPolicy to nil.
// Removes o from all passed in related items' relationships struct.
func (o *Alert) RemoveEscalationPolicy(ctx context.Context, exec boil.ContextExecutor, related *EscalationPolicy) error {
	var err error

	queries.SetScanner(&o.EscalationPolicyID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("escalation_policy_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.EscalationPolicy = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.Alerts {
		if queries.Equal(o.EscalationPolicyID, ri.EscalationPolicyID) {
			continue
		}

		ln := len(related.R.Alerts)
		if ln > 1 && i < ln-1 {
			related.R.Alerts[i] = related.R.Alerts[ln-1]
		}
		related.R.Alerts = related.R.Alerts[:ln-1]
		break
	}
	return nil
}

// SetParentAlert of the alert to the related item.
// Sets o.R.ParentAlert to related.
// Adds o to related.R.ParentAlertAlerts.
func (o *Alert) SetParentAlert(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Alert) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alerts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"parent_alert_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ParentAlertID, related.ID)
	if o.R == nil {
		o.R = &alertR{
			ParentAlert: related,
		}
	} else {
		o.R.ParentAlert = related
	}

	if related.R == nil {
		related.R = &alertR{
			ParentAlertAlerts: AlertSlice{o},
		}
	} else {
		related.R.ParentAlertAlerts = append(related.R.ParentAlertAlerts, o)
	}

	return nil
}

// RemoveParentAlert relationship.
// Sets o.R.ParentAlert to nil.
// Removes o from all passed in related items' relationships struct.
func (o *Alert) RemoveParentAlert(ctx context.Context, exec boil.ContextExecutor, related *Alert) error {
	var err error

	queries.SetScanner(&o.ParentAlertID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("parent_alert_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.ParentAlert = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ParentAlertAlerts {
		if queries.Equal(o.ParentAlertID, ri.ParentAlertID) {
			continue
		}

		ln := len(related.R.ParentAlertAlerts)
		if ln > 1 && i < ln-1 {
			related.R.ParentAlertAlerts[i] = related.R.ParentAlertAlerts[ln-1]
		}
		related.R.ParentAlertAlerts = related.R.ParentAlertAlerts[:ln-1]
		break
	}
	return nil
}

// SetSuppressedBySilence of the alert to the related item.
// Sets o.R.SuppressedBySilence to related.
// Adds o to related.R.SuppressedBySilenceAlerts.
func (o *Alert) SetSuppressedBySilence(ctx context.Context, exec boil.ContextExecutor, insert bool, related *AlertSilence) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alerts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"suppressed_by_silence_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.SuppressedBySilenceID, related.ID)
	if o.R == nil {
		o.R = &alertR{
			SuppressedBySilence: related,
		}
	} else {
		o.R.SuppressedBySilence = related
	}

	if related.R == nil {
		related.R = &alertSilenceR{
			SuppressedBySilenceAlerts: AlertSlice{o},
		}
	} else {
		related.R.SuppressedBySilenceAlerts = append(related.R.SuppressedBySilenceAlerts, o)
	}

	return nil
}

// RemoveSuppressedBySilence relationship.
// Sets o.R.SuppressedBySilence to nil.
// Removes o from all passed in related items' relationships struct.
func (o *Alert) RemoveSuppressedBySilence(ctx context.Context, exec boil.ContextExecutor, related *AlertSilence) error {
	var err error

	queries.SetScanner(&o.SuppressedBySilenceID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("suppressed_by_silence_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.SuppressedBySilence = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.SuppressedBySilenceAlerts {
		if queries.Equal(o.SuppressedBySilenceID, ri.SuppressedBySilenceID) {
			continue
		}

		ln := len(related.R.SuppressedBySilenceAlerts)
		if ln > 1 && i < ln-1 {
			related.R.SuppressedBySilenceAlerts[i] = related.R.SuppressedBySilenceAlerts[ln-1]
		}
		related.R.SuppressedBySilenceAlerts = related.R.SuppressedBySilenceAlerts[:ln-1]
		break
	}
	return nil
}

// SetWorkspace of the alert to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.Alerts.
func (o *Alert) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alerts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &alertR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			Alerts: AlertSlice{o},
		}
	} else {
		related.R.Alerts = append(related.R.Alerts, o)
	}

	return nil
}

// SetAlertEscalation of the alert to the related item.
// Sets o.R.AlertEscalation to related.
// Adds o to related.R.Alert.
func (o *Alert) SetAlertEscalation(ctx context.Context, exec boil.ContextExecutor, insert bool, related *AlertEscalation) error {
	var err error

	if insert {
		related.AlertID = o.ID

		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	} else {
		updateQuery := fmt.Sprintf(
			"UPDATE \"alert_escalations\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, []string{"alert_id"}),
			strmangle.WhereClause("\"", "\"", 2, alertEscalationPrimaryKeyColumns),
		)
		values := []any{o.ID, related.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, updateQuery)
			fmt.Fprintln(writer, values)
		}
		if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
			return errors.Wrap(err, "failed to update foreign table")
		}

		related.AlertID = o.ID
	}

	if o.R == nil {
		o.R = &alertR{
			AlertEscalation: related,
		}
	} else {
		o.R.AlertEscalation = related
	}

	if related.R == nil {
		related.R = &alertEscalationR{
			Alert: o,
		}
	} else {
		related.R.Alert = o
	}
	return nil
}

// AddAlertEvents adds the given related objects to the existing relationships
// of the alert, optionally inserting them as new records.
// Appends related to o.R.AlertEvents.
// Sets related.R.Alert appropriately.
func (o *Alert) AddAlertEvents(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertEvent) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AlertID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_events\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"alert_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertEventPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AlertID = o.ID
		}
	}

	if o.R == nil {
		o.R = &alertR{
			AlertEvents: related,
		}
	} else {
		o.R.AlertEvents = append(o.R.AlertEvents, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertEventR{
				Alert: o,
			}
		} else {
			rel.R.Alert = o
		}
	}
	return nil
}

// AddAlertIngestEvents adds the given related objects to the existing relationships
// of the alert, optionally inserting them as new records.
// Appends related to o.R.AlertIngestEvents.
// Sets related.R.Alert appropriately.
func (o *Alert) AddAlertIngestEvents(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertIngestEvent) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.AlertID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_ingest_events\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"alert_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertIngestEventPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.AlertID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &alertR{
			AlertIngestEvents: related,
		}
	} else {
		o.R.AlertIngestEvents = append(o.R.AlertIngestEvents, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertIngestEventR{
				Alert: o,
			}
		} else {
			rel.R.Alert = o
		}
	}
	return nil
}

// SetAlertIngestEvents removes all previously related items of the
// alert replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Alert's AlertIngestEvents accordingly.
// Replaces o.R.AlertIngestEvents with related.
// Sets related.R.Alert's AlertIngestEvents accordingly.
func (o *Alert) SetAlertIngestEvents(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertIngestEvent) error {
	query := "update \"alert_ingest_events\" set \"alert_id\" = null where \"alert_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.AlertIngestEvents {
			queries.SetScanner(&rel.AlertID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Alert = nil
		}
		o.R.AlertIngestEvents = nil
	}

	return o.AddAlertIngestEvents(ctx, exec, insert, related...)
}

// RemoveAlertIngestEvents relationships from objects passed in.
// Removes related items from R.AlertIngestEvents (uses pointer comparison, removal does not keep order)
// Sets related.R.Alert.
func (o *Alert) RemoveAlertIngestEvents(ctx context.Context, exec boil.ContextExecutor, related ...*AlertIngestEvent) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.AlertID, nil)
		if rel.R != nil {
			rel.R.Alert = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("alert_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.AlertIngestEvents {
			if rel != ri {
				continue
			}

			ln := len(o.R.AlertIngestEvents)
			if ln > 1 && i < ln-1 {
				o.R.AlertIngestEvents[i] = o.R.AlertIngestEvents[ln-1]
			}
			o.R.AlertIngestEvents = o.R.AlertIngestEvents[:ln-1]
			break
		}
	}

	return nil
}

// AddAlertLinks adds the given related objects to the existing relationships
// of the alert, optionally inserting them as new records.
// Appends related to o.R.AlertLinks.
// Sets related.R.Alert appropriately.
func (o *Alert) AddAlertLinks(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertLink) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AlertID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_links\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"alert_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertLinkPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AlertID = o.ID
		}
	}

	if o.R == nil {
		o.R = &alertR{
			AlertLinks: related,
		}
	} else {
		o.R.AlertLinks = append(o.R.AlertLinks, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertLinkR{
				Alert: o,
			}
		} else {
			rel.R.Alert = o
		}
	}
	return nil
}

// AddParentAlertAlerts adds the given related objects to the existing relationships
// of the alert, optionally inserting them as new records.
// Appends related to o.R.ParentAlertAlerts.
// Sets related.R.ParentAlert appropriately.
func (o *Alert) AddParentAlertAlerts(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Alert) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.ParentAlertID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alerts\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"parent_alert_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.ParentAlertID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &alertR{
			ParentAlertAlerts: related,
		}
	} else {
		o.R.ParentAlertAlerts = append(o.R.ParentAlertAlerts, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertR{
				ParentAlert: o,
			}
		} else {
			rel.R.ParentAlert = o
		}
	}
	return nil
}

// SetParentAlertAlerts removes all previously related items of the
// alert replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.ParentAlert's ParentAlertAlerts accordingly.
// Replaces o.R.ParentAlertAlerts with related.
// Sets related.R.ParentAlert's ParentAlertAlerts accordingly.
func (o *Alert) SetParentAlertAlerts(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Alert) error {
	query := "update \"alerts\" set \"parent_alert_id\" = null where \"parent_alert_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.ParentAlertAlerts {
			queries.SetScanner(&rel.ParentAlertID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.ParentAlert = nil
		}
		o.R.ParentAlertAlerts = nil
	}

	return o.AddParentAlertAlerts(ctx, exec, insert, related...)
}

// RemoveParentAlertAlerts relationships from objects passed in.
// Removes related items from R.ParentAlertAlerts (uses pointer comparison, removal does not keep order)
// Sets related.R.ParentAlert.
func (o *Alert) RemoveParentAlertAlerts(ctx context.Context, exec boil.ContextExecutor, related ...*Alert) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.ParentAlertID, nil)
		if rel.R != nil {
			rel.R.ParentAlert = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("parent_alert_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.ParentAlertAlerts {
			if rel != ri {
				continue
			}

			ln := len(o.R.ParentAlertAlerts)
			if ln > 1 && i < ln-1 {
				o.R.ParentAlertAlerts[i] = o.R.ParentAlertAlerts[ln-1]
			}
			o.R.ParentAlertAlerts = o.R.ParentAlertAlerts[:ln-1]
			break
		}
	}

	return nil
}

// Alerts retrieves all the records using an executor.
func Alerts(mods ...qm.QueryMod) alertQuery {
	mods = append(mods, qm.From("\"alerts\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"alerts\".*"})
	}

	return alertQuery{q}
}

// FindAlert retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAlert(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*Alert, error) {
	alertObj := &Alert{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"alerts\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, alertObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from alerts")
	}

	if err = alertObj.doAfterSelectHooks(ctx, exec); err != nil {
		return alertObj, err
	}

	return alertObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Alert) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no alerts provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(alertColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	alertInsertCacheMut.RLock()
	cache, cached := alertInsertCache[key]
	alertInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			alertAllColumns,
			alertColumnsWithDefault,
			alertColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(alertType, alertMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(alertType, alertMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"alerts\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"alerts\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into alerts")
	}

	if !cached {
		alertInsertCacheMut.Lock()
		alertInsertCache[key] = cache
		alertInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Alert.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Alert) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	alertUpdateCacheMut.RLock()
	cache, cached := alertUpdateCache[key]
	alertUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			alertAllColumns,
			alertPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update alerts, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"alerts\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, alertPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(alertType, alertMapping, append(wl, alertPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update alerts row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for alerts")
	}

	if !cached {
		alertUpdateCacheMut.Lock()
		alertUpdateCache[key] = cache
		alertUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q alertQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for alerts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for alerts")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AlertSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"alerts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, alertPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in alert slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all alert")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Alert) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no alerts provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(alertColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	alertUpsertCacheMut.RLock()
	cache, cached := alertUpsertCache[key]
	alertUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			alertAllColumns,
			alertColumnsWithDefault,
			alertColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			alertAllColumns,
			alertPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert alerts, could not build update column list")
		}

		ret := strmangle.SetComplement(alertAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(alertPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert alerts, could not build conflict column list")
			}

			conflict = make([]string, len(alertPrimaryKeyColumns))
			copy(conflict, alertPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"alerts\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(alertType, alertMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(alertType, alertMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert alerts")
	}

	if !cached {
		alertUpsertCacheMut.Lock()
		alertUpsertCache[key] = cache
		alertUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Alert record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Alert) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no Alert provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), alertPrimaryKeyMapping)
	sql := "DELETE FROM \"alerts\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from alerts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for alerts")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q alertQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no alertQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from alerts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for alerts")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AlertSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(alertBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"alerts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, alertPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from alert slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for alerts")
	}

	if len(alertAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Alert) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAlert(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AlertSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AlertSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"alerts\".* FROM \"alerts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, alertPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in AlertSlice")
	}

	*o = slice

	return nil
}

// AlertExists checks if the Alert row exists.
func AlertExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"alerts\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if alerts exists")
	}

	return exists, nil
}

// Exists checks if the Alert row exists.
func (o *Alert) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return AlertExists(ctx, exec, o.ID)
}
