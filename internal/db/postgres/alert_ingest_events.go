// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// AlertIngestEvent is an object representing the database table.
type AlertIngestEvent struct {
	ID          string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	SourceID    string      `boil:"source_id" json:"source_id" toml:"source_id" yaml:"source_id"`
	AlertID     null.String `boil:"alert_id" json:"alert_id,omitempty" toml:"alert_id" yaml:"alert_id,omitempty"`
	DedupKey    string      `boil:"dedup_key" json:"dedup_key" toml:"dedup_key" yaml:"dedup_key"`
	Outcome     string      `boil:"outcome" json:"outcome" toml:"outcome" yaml:"outcome"`
	At          time.Time   `boil:"at" json:"at" toml:"at" yaml:"at"`

	R *alertIngestEventR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L alertIngestEventL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AlertIngestEventColumns = struct {
	ID          string
	WorkspaceID string
	SourceID    string
	AlertID     string
	DedupKey    string
	Outcome     string
	At          string
}{
	ID:          "id",
	WorkspaceID: "workspace_id",
	SourceID:    "source_id",
	AlertID:     "alert_id",
	DedupKey:    "dedup_key",
	Outcome:     "outcome",
	At:          "at",
}

var AlertIngestEventTableColumns = struct {
	ID          string
	WorkspaceID string
	SourceID    string
	AlertID     string
	DedupKey    string
	Outcome     string
	At          string
}{
	ID:          "alert_ingest_events.id",
	WorkspaceID: "alert_ingest_events.workspace_id",
	SourceID:    "alert_ingest_events.source_id",
	AlertID:     "alert_ingest_events.alert_id",
	DedupKey:    "alert_ingest_events.dedup_key",
	Outcome:     "alert_ingest_events.outcome",
	At:          "alert_ingest_events.at",
}

// Generated where

type whereHelpernull_String struct{ field string }

func (w whereHelpernull_String) EQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_String) NEQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_String) LT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_String) LTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_String) GT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_String) GTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelpernull_String) LIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" LIKE ?", x)
}
func (w whereHelpernull_String) NLIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT LIKE ?", x)
}
func (w whereHelpernull_String) ILIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" ILIKE ?", x)
}
func (w whereHelpernull_String) NILIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT ILIKE ?", x)
}
func (w whereHelpernull_String) SIMILAR(x null.String) qm.QueryMod {
	return qm.Where(w.field+" SIMILAR TO ?", x)
}
func (w whereHelpernull_String) NSIMILAR(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT SIMILAR TO ?", x)
}
func (w whereHelpernull_String) IN(slice []string) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelpernull_String) NIN(slice []string) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

func (w whereHelpernull_String) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_String) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var AlertIngestEventWhere = struct {
	ID          whereHelperstring
	WorkspaceID whereHelperstring
	SourceID    whereHelperstring
	AlertID     whereHelpernull_String
	DedupKey    whereHelperstring
	Outcome     whereHelperstring
	At          whereHelpertime_Time
}{
	ID:          whereHelperstring{field: "\"alert_ingest_events\".\"id\""},
	WorkspaceID: whereHelperstring{field: "\"alert_ingest_events\".\"workspace_id\""},
	SourceID:    whereHelperstring{field: "\"alert_ingest_events\".\"source_id\""},
	AlertID:     whereHelpernull_String{field: "\"alert_ingest_events\".\"alert_id\""},
	DedupKey:    whereHelperstring{field: "\"alert_ingest_events\".\"dedup_key\""},
	Outcome:     whereHelperstring{field: "\"alert_ingest_events\".\"outcome\""},
	At:          whereHelpertime_Time{field: "\"alert_ingest_events\".\"at\""},
}

// AlertIngestEventRels is where relationship names are stored.
var AlertIngestEventRels = struct {
	Alert     string
	Source    string
	Workspace string
}{
	Alert:     "Alert",
	Source:    "Source",
	Workspace: "Workspace",
}

// alertIngestEventR is where relationships are stored.
type alertIngestEventR struct {
	Alert     *Alert       `boil:"Alert" json:"Alert" toml:"Alert" yaml:"Alert"`
	Source    *AlertSource `boil:"Source" json:"Source" toml:"Source" yaml:"Source"`
	Workspace *Workspace   `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
}

// NewStruct creates a new relationship struct
func (*alertIngestEventR) NewStruct() *alertIngestEventR {
	return &alertIngestEventR{}
}

func (o *AlertIngestEvent) GetAlert() *Alert {
	if o == nil {
		return nil
	}

	return o.R.GetAlert()
}

func (r *alertIngestEventR) GetAlert() *Alert {
	if r == nil {
		return nil
	}

	return r.Alert
}

func (o *AlertIngestEvent) GetSource() *AlertSource {
	if o == nil {
		return nil
	}

	return o.R.GetSource()
}

func (r *alertIngestEventR) GetSource() *AlertSource {
	if r == nil {
		return nil
	}

	return r.Source
}

func (o *AlertIngestEvent) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *alertIngestEventR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

// alertIngestEventL is where Load methods for each relationship are stored.
type alertIngestEventL struct{}

var (
	alertIngestEventAllColumns            = []string{"id", "workspace_id", "source_id", "alert_id", "dedup_key", "outcome", "at"}
	alertIngestEventColumnsWithoutDefault = []string{"workspace_id", "source_id", "outcome"}
	alertIngestEventColumnsWithDefault    = []string{"id", "alert_id", "dedup_key", "at"}
	alertIngestEventPrimaryKeyColumns     = []string{"id"}
	alertIngestEventGeneratedColumns      = []string{}
)

type (
	// AlertIngestEventSlice is an alias for a slice of pointers to AlertIngestEvent.
	// This should almost always be used instead of []AlertIngestEvent.
	AlertIngestEventSlice []*AlertIngestEvent
	// AlertIngestEventHook is the signature for custom AlertIngestEvent hook methods
	AlertIngestEventHook func(context.Context, boil.ContextExecutor, *AlertIngestEvent) error

	alertIngestEventQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	alertIngestEventType                 = reflect.TypeOf(&AlertIngestEvent{})
	alertIngestEventMapping              = queries.MakeStructMapping(alertIngestEventType)
	alertIngestEventPrimaryKeyMapping, _ = queries.BindMapping(alertIngestEventType, alertIngestEventMapping, alertIngestEventPrimaryKeyColumns)
	alertIngestEventInsertCacheMut       sync.RWMutex
	alertIngestEventInsertCache          = make(map[string]insertCache)
	alertIngestEventUpdateCacheMut       sync.RWMutex
	alertIngestEventUpdateCache          = make(map[string]updateCache)
	alertIngestEventUpsertCacheMut       sync.RWMutex
	alertIngestEventUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var alertIngestEventAfterSelectMu sync.Mutex
var alertIngestEventAfterSelectHooks []AlertIngestEventHook

var alertIngestEventBeforeInsertMu sync.Mutex
var alertIngestEventBeforeInsertHooks []AlertIngestEventHook
var alertIngestEventAfterInsertMu sync.Mutex
var alertIngestEventAfterInsertHooks []AlertIngestEventHook

var alertIngestEventBeforeUpdateMu sync.Mutex
var alertIngestEventBeforeUpdateHooks []AlertIngestEventHook
var alertIngestEventAfterUpdateMu sync.Mutex
var alertIngestEventAfterUpdateHooks []AlertIngestEventHook

var alertIngestEventBeforeDeleteMu sync.Mutex
var alertIngestEventBeforeDeleteHooks []AlertIngestEventHook
var alertIngestEventAfterDeleteMu sync.Mutex
var alertIngestEventAfterDeleteHooks []AlertIngestEventHook

var alertIngestEventBeforeUpsertMu sync.Mutex
var alertIngestEventBeforeUpsertHooks []AlertIngestEventHook
var alertIngestEventAfterUpsertMu sync.Mutex
var alertIngestEventAfterUpsertHooks []AlertIngestEventHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AlertIngestEvent) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestEventAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AlertIngestEvent) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestEventBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AlertIngestEvent) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestEventAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AlertIngestEvent) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestEventBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AlertIngestEvent) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestEventAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AlertIngestEvent) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestEventBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AlertIngestEvent) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestEventAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AlertIngestEvent) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestEventBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AlertIngestEvent) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertIngestEventAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAlertIngestEventHook registers your hook function for all future operations.
func AddAlertIngestEventHook(hookPoint boil.HookPoint, alertIngestEventHook AlertIngestEventHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		alertIngestEventAfterSelectMu.Lock()
		alertIngestEventAfterSelectHooks = append(alertIngestEventAfterSelectHooks, alertIngestEventHook)
		alertIngestEventAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		alertIngestEventBeforeInsertMu.Lock()
		alertIngestEventBeforeInsertHooks = append(alertIngestEventBeforeInsertHooks, alertIngestEventHook)
		alertIngestEventBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		alertIngestEventAfterInsertMu.Lock()
		alertIngestEventAfterInsertHooks = append(alertIngestEventAfterInsertHooks, alertIngestEventHook)
		alertIngestEventAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		alertIngestEventBeforeUpdateMu.Lock()
		alertIngestEventBeforeUpdateHooks = append(alertIngestEventBeforeUpdateHooks, alertIngestEventHook)
		alertIngestEventBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		alertIngestEventAfterUpdateMu.Lock()
		alertIngestEventAfterUpdateHooks = append(alertIngestEventAfterUpdateHooks, alertIngestEventHook)
		alertIngestEventAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		alertIngestEventBeforeDeleteMu.Lock()
		alertIngestEventBeforeDeleteHooks = append(alertIngestEventBeforeDeleteHooks, alertIngestEventHook)
		alertIngestEventBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		alertIngestEventAfterDeleteMu.Lock()
		alertIngestEventAfterDeleteHooks = append(alertIngestEventAfterDeleteHooks, alertIngestEventHook)
		alertIngestEventAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		alertIngestEventBeforeUpsertMu.Lock()
		alertIngestEventBeforeUpsertHooks = append(alertIngestEventBeforeUpsertHooks, alertIngestEventHook)
		alertIngestEventBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		alertIngestEventAfterUpsertMu.Lock()
		alertIngestEventAfterUpsertHooks = append(alertIngestEventAfterUpsertHooks, alertIngestEventHook)
		alertIngestEventAfterUpsertMu.Unlock()
	}
}

// One returns a single alertIngestEvent record from the query.
func (q alertIngestEventQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AlertIngestEvent, error) {
	o := &AlertIngestEvent{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for alert_ingest_events")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AlertIngestEvent records from the query.
func (q alertIngestEventQuery) All(ctx context.Context, exec boil.ContextExecutor) (AlertIngestEventSlice, error) {
	var o []*AlertIngestEvent

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to AlertIngestEvent slice")
	}

	if len(alertIngestEventAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AlertIngestEvent records in the query.
func (q alertIngestEventQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count alert_ingest_events rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q alertIngestEventQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if alert_ingest_events exists")
	}

	return count > 0, nil
}

// Alert pointed to by the foreign key.
func (o *AlertIngestEvent) Alert(mods ...qm.QueryMod) alertQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AlertID),
	}

	queryMods = append(queryMods, mods...)

	return Alerts(queryMods...)
}

// Source pointed to by the foreign key.
func (o *AlertIngestEvent) Source(mods ...qm.QueryMod) alertSourceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SourceID),
	}

	queryMods = append(queryMods, mods...)

	return AlertSources(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *AlertIngestEvent) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// LoadAlert allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertIngestEventL) LoadAlert(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlertIngestEvent any, mods queries.Applicator) error {
	var slice []*AlertIngestEvent
	var object *AlertIngestEvent

	if singular {
		var ok bool
		object, ok = maybeAlertIngestEvent.(*AlertIngestEvent)
		if !ok {
			object = new(AlertIngestEvent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlertIngestEvent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlertIngestEvent))
			}
		}
	} else {
		s, ok := maybeAlertIngestEvent.(*[]*AlertIngestEvent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlertIngestEvent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlertIngestEvent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertIngestEventR{}
		}
		if !queries.IsNil(object.AlertID) {
			args[object.AlertID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertIngestEventR{}
			}

			if !queries.IsNil(obj.AlertID) {
				args[obj.AlertID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alerts`),
		qm.WhereIn(`alerts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Alert")
	}

	var resultSlice []*Alert
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Alert")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for alerts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alerts")
	}

	if len(alertAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Alert = foreign
		if foreign.R == nil {
			foreign.R = &alertR{}
		}
		foreign.R.AlertIngestEvents = append(foreign.R.AlertIngestEvents, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.AlertID, foreign.ID) {
				local.R.Alert = foreign
				if foreign.R == nil {
					foreign.R = &alertR{}
				}
				foreign.R.AlertIngestEvents = append(foreign.R.AlertIngestEvents, local)
				break
			}
		}
	}

	return nil
}

// LoadSource allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertIngestEventL) LoadSource(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlertIngestEvent any, mods queries.Applicator) error {
	var slice []*AlertIngestEvent
	var object *AlertIngestEvent

	if singular {
		var ok bool
		object, ok = maybeAlertIngestEvent.(*AlertIngestEvent)
		if !ok {
			object = new(AlertIngestEvent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlertIngestEvent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlertIngestEvent))
			}
		}
	} else {
		s, ok := maybeAlertIngestEvent.(*[]*AlertIngestEvent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlertIngestEvent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlertIngestEvent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertIngestEventR{}
		}
		args[object.SourceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertIngestEventR{}
			}

			args[obj.SourceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_sources`),
		qm.WhereIn(`alert_sources.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AlertSource")
	}

	var resultSlice []*AlertSource
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AlertSource")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for alert_sources")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_sources")
	}

	if len(alertSourceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Source = foreign
		if foreign.R == nil {
			foreign.R = &alertSourceR{}
		}
		foreign.R.SourceAlertIngestEvents = append(foreign.R.SourceAlertIngestEvents, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SourceID == foreign.ID {
				local.R.Source = foreign
				if foreign.R == nil {
					foreign.R = &alertSourceR{}
				}
				foreign.R.SourceAlertIngestEvents = append(foreign.R.SourceAlertIngestEvents, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertIngestEventL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlertIngestEvent any, mods queries.Applicator) error {
	var slice []*AlertIngestEvent
	var object *AlertIngestEvent

	if singular {
		var ok bool
		object, ok = maybeAlertIngestEvent.(*AlertIngestEvent)
		if !ok {
			object = new(AlertIngestEvent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlertIngestEvent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlertIngestEvent))
			}
		}
	} else {
		s, ok := maybeAlertIngestEvent.(*[]*AlertIngestEvent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlertIngestEvent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlertIngestEvent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertIngestEventR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertIngestEventR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.AlertIngestEvents = append(foreign.R.AlertIngestEvents, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.AlertIngestEvents = append(foreign.R.AlertIngestEvents, local)
				break
			}
		}
	}

	return nil
}

// SetAlert of the alertIngestEvent to the related item.
// Sets o.R.Alert to related.
// Adds o to related.R.AlertIngestEvents.
func (o *AlertIngestEvent) SetAlert(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Alert) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alert_ingest_events\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"alert_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertIngestEventPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.AlertID, related.ID)
	if o.R == nil {
		o.R = &alertIngestEventR{
			Alert: related,
		}
	} else {
		o.R.Alert = related
	}

	if related.R == nil {
		related.R = &alertR{
			AlertIngestEvents: AlertIngestEventSlice{o},
		}
	} else {
		related.R.AlertIngestEvents = append(related.R.AlertIngestEvents, o)
	}

	return nil
}

// RemoveAlert relationship.
// Sets o.R.Alert to nil.
// Removes o from all passed in related items' relationships struct.
func (o *AlertIngestEvent) RemoveAlert(ctx context.Context, exec boil.ContextExecutor, related *Alert) error {
	var err error

	queries.SetScanner(&o.AlertID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("alert_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Alert = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.AlertIngestEvents {
		if queries.Equal(o.AlertID, ri.AlertID) {
			continue
		}

		ln := len(related.R.AlertIngestEvents)
		if ln > 1 && i < ln-1 {
			related.R.AlertIngestEvents[i] = related.R.AlertIngestEvents[ln-1]
		}
		related.R.AlertIngestEvents = related.R.AlertIngestEvents[:ln-1]
		break
	}
	return nil
}

// SetSource of the alertIngestEvent to the related item.
// Sets o.R.Source to related.
// Adds o to related.R.SourceAlertIngestEvents.
func (o *AlertIngestEvent) SetSource(ctx context.Context, exec boil.ContextExecutor, insert bool, related *AlertSource) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alert_ingest_events\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"source_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertIngestEventPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SourceID = related.ID
	if o.R == nil {
		o.R = &alertIngestEventR{
			Source: related,
		}
	} else {
		o.R.Source = related
	}

	if related.R == nil {
		related.R = &alertSourceR{
			SourceAlertIngestEvents: AlertIngestEventSlice{o},
		}
	} else {
		related.R.SourceAlertIngestEvents = append(related.R.SourceAlertIngestEvents, o)
	}

	return nil
}

// SetWorkspace of the alertIngestEvent to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.AlertIngestEvents.
func (o *AlertIngestEvent) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"alert_ingest_events\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, alertIngestEventPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &alertIngestEventR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			AlertIngestEvents: AlertIngestEventSlice{o},
		}
	} else {
		related.R.AlertIngestEvents = append(related.R.AlertIngestEvents, o)
	}

	return nil
}

// AlertIngestEvents retrieves all the records using an executor.
func AlertIngestEvents(mods ...qm.QueryMod) alertIngestEventQuery {
	mods = append(mods, qm.From("\"alert_ingest_events\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"alert_ingest_events\".*"})
	}

	return alertIngestEventQuery{q}
}

// FindAlertIngestEvent retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAlertIngestEvent(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*AlertIngestEvent, error) {
	alertIngestEventObj := &AlertIngestEvent{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"alert_ingest_events\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, alertIngestEventObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from alert_ingest_events")
	}

	if err = alertIngestEventObj.doAfterSelectHooks(ctx, exec); err != nil {
		return alertIngestEventObj, err
	}

	return alertIngestEventObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AlertIngestEvent) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no alert_ingest_events provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(alertIngestEventColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	alertIngestEventInsertCacheMut.RLock()
	cache, cached := alertIngestEventInsertCache[key]
	alertIngestEventInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			alertIngestEventAllColumns,
			alertIngestEventColumnsWithDefault,
			alertIngestEventColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(alertIngestEventType, alertIngestEventMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(alertIngestEventType, alertIngestEventMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"alert_ingest_events\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"alert_ingest_events\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into alert_ingest_events")
	}

	if !cached {
		alertIngestEventInsertCacheMut.Lock()
		alertIngestEventInsertCache[key] = cache
		alertIngestEventInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AlertIngestEvent.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AlertIngestEvent) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	alertIngestEventUpdateCacheMut.RLock()
	cache, cached := alertIngestEventUpdateCache[key]
	alertIngestEventUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			alertIngestEventAllColumns,
			alertIngestEventPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update alert_ingest_events, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"alert_ingest_events\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, alertIngestEventPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(alertIngestEventType, alertIngestEventMapping, append(wl, alertIngestEventPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update alert_ingest_events row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for alert_ingest_events")
	}

	if !cached {
		alertIngestEventUpdateCacheMut.Lock()
		alertIngestEventUpdateCache[key] = cache
		alertIngestEventUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q alertIngestEventQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for alert_ingest_events")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for alert_ingest_events")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AlertIngestEventSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertIngestEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"alert_ingest_events\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, alertIngestEventPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in alertIngestEvent slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all alertIngestEvent")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AlertIngestEvent) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no alert_ingest_events provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(alertIngestEventColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	alertIngestEventUpsertCacheMut.RLock()
	cache, cached := alertIngestEventUpsertCache[key]
	alertIngestEventUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			alertIngestEventAllColumns,
			alertIngestEventColumnsWithDefault,
			alertIngestEventColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			alertIngestEventAllColumns,
			alertIngestEventPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert alert_ingest_events, could not build update column list")
		}

		ret := strmangle.SetComplement(alertIngestEventAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(alertIngestEventPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert alert_ingest_events, could not build conflict column list")
			}

			conflict = make([]string, len(alertIngestEventPrimaryKeyColumns))
			copy(conflict, alertIngestEventPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"alert_ingest_events\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(alertIngestEventType, alertIngestEventMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(alertIngestEventType, alertIngestEventMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert alert_ingest_events")
	}

	if !cached {
		alertIngestEventUpsertCacheMut.Lock()
		alertIngestEventUpsertCache[key] = cache
		alertIngestEventUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AlertIngestEvent record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AlertIngestEvent) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no AlertIngestEvent provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), alertIngestEventPrimaryKeyMapping)
	sql := "DELETE FROM \"alert_ingest_events\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from alert_ingest_events")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for alert_ingest_events")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q alertIngestEventQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no alertIngestEventQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from alert_ingest_events")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for alert_ingest_events")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AlertIngestEventSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(alertIngestEventBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertIngestEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"alert_ingest_events\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, alertIngestEventPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from alertIngestEvent slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for alert_ingest_events")
	}

	if len(alertIngestEventAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AlertIngestEvent) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAlertIngestEvent(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AlertIngestEventSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AlertIngestEventSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertIngestEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"alert_ingest_events\".* FROM \"alert_ingest_events\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, alertIngestEventPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in AlertIngestEventSlice")
	}

	*o = slice

	return nil
}

// AlertIngestEventExists checks if the AlertIngestEvent row exists.
func AlertIngestEventExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"alert_ingest_events\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if alert_ingest_events exists")
	}

	return exists, nil
}

// Exists checks if the AlertIngestEvent row exists.
func (o *AlertIngestEvent) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return AlertIngestEventExists(ctx, exec, o.ID)
}
