// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// IncidentRelation is an object representing the database table.
type IncidentRelation struct {
	ID                string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID       string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	IncidentID        string    `boil:"incident_id" json:"incident_id" toml:"incident_id" yaml:"incident_id"`
	RelatedIncidentID string    `boil:"related_incident_id" json:"related_incident_id" toml:"related_incident_id" yaml:"related_incident_id"`
	Kind              string    `boil:"kind" json:"kind" toml:"kind" yaml:"kind"`
	CreatedAt         time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *incidentRelationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L incidentRelationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var IncidentRelationColumns = struct {
	ID                string
	WorkspaceID       string
	IncidentID        string
	RelatedIncidentID string
	Kind              string
	CreatedAt         string
}{
	ID:                "id",
	WorkspaceID:       "workspace_id",
	IncidentID:        "incident_id",
	RelatedIncidentID: "related_incident_id",
	Kind:              "kind",
	CreatedAt:         "created_at",
}

var IncidentRelationTableColumns = struct {
	ID                string
	WorkspaceID       string
	IncidentID        string
	RelatedIncidentID string
	Kind              string
	CreatedAt         string
}{
	ID:                "incident_relations.id",
	WorkspaceID:       "incident_relations.workspace_id",
	IncidentID:        "incident_relations.incident_id",
	RelatedIncidentID: "incident_relations.related_incident_id",
	Kind:              "incident_relations.kind",
	CreatedAt:         "incident_relations.created_at",
}

// Generated where

var IncidentRelationWhere = struct {
	ID                whereHelperstring
	WorkspaceID       whereHelperstring
	IncidentID        whereHelperstring
	RelatedIncidentID whereHelperstring
	Kind              whereHelperstring
	CreatedAt         whereHelpertime_Time
}{
	ID:                whereHelperstring{field: "\"incident_relations\".\"id\""},
	WorkspaceID:       whereHelperstring{field: "\"incident_relations\".\"workspace_id\""},
	IncidentID:        whereHelperstring{field: "\"incident_relations\".\"incident_id\""},
	RelatedIncidentID: whereHelperstring{field: "\"incident_relations\".\"related_incident_id\""},
	Kind:              whereHelperstring{field: "\"incident_relations\".\"kind\""},
	CreatedAt:         whereHelpertime_Time{field: "\"incident_relations\".\"created_at\""},
}

// IncidentRelationRels is where relationship names are stored.
var IncidentRelationRels = struct {
}{}

// incidentRelationR is where relationships are stored.
type incidentRelationR struct {
}

// NewStruct creates a new relationship struct
func (*incidentRelationR) NewStruct() *incidentRelationR {
	return &incidentRelationR{}
}

// incidentRelationL is where Load methods for each relationship are stored.
type incidentRelationL struct{}

var (
	incidentRelationAllColumns            = []string{"id", "workspace_id", "incident_id", "related_incident_id", "kind", "created_at"}
	incidentRelationColumnsWithoutDefault = []string{"workspace_id", "incident_id", "related_incident_id", "kind"}
	incidentRelationColumnsWithDefault    = []string{"id", "created_at"}
	incidentRelationPrimaryKeyColumns     = []string{"id"}
	incidentRelationGeneratedColumns      = []string{}
)

type (
	// IncidentRelationSlice is an alias for a slice of pointers to IncidentRelation.
	// This should almost always be used instead of []IncidentRelation.
	IncidentRelationSlice []*IncidentRelation
	// IncidentRelationHook is the signature for custom IncidentRelation hook methods
	IncidentRelationHook func(context.Context, boil.ContextExecutor, *IncidentRelation) error

	incidentRelationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	incidentRelationType                 = reflect.TypeOf(&IncidentRelation{})
	incidentRelationMapping              = queries.MakeStructMapping(incidentRelationType)
	incidentRelationPrimaryKeyMapping, _ = queries.BindMapping(incidentRelationType, incidentRelationMapping, incidentRelationPrimaryKeyColumns)
	incidentRelationInsertCacheMut       sync.RWMutex
	incidentRelationInsertCache          = make(map[string]insertCache)
	incidentRelationUpdateCacheMut       sync.RWMutex
	incidentRelationUpdateCache          = make(map[string]updateCache)
	incidentRelationUpsertCacheMut       sync.RWMutex
	incidentRelationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var incidentRelationAfterSelectMu sync.Mutex
var incidentRelationAfterSelectHooks []IncidentRelationHook

var incidentRelationBeforeInsertMu sync.Mutex
var incidentRelationBeforeInsertHooks []IncidentRelationHook
var incidentRelationAfterInsertMu sync.Mutex
var incidentRelationAfterInsertHooks []IncidentRelationHook

var incidentRelationBeforeUpdateMu sync.Mutex
var incidentRelationBeforeUpdateHooks []IncidentRelationHook
var incidentRelationAfterUpdateMu sync.Mutex
var incidentRelationAfterUpdateHooks []IncidentRelationHook

var incidentRelationBeforeDeleteMu sync.Mutex
var incidentRelationBeforeDeleteHooks []IncidentRelationHook
var incidentRelationAfterDeleteMu sync.Mutex
var incidentRelationAfterDeleteHooks []IncidentRelationHook

var incidentRelationBeforeUpsertMu sync.Mutex
var incidentRelationBeforeUpsertHooks []IncidentRelationHook
var incidentRelationAfterUpsertMu sync.Mutex
var incidentRelationAfterUpsertHooks []IncidentRelationHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *IncidentRelation) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentRelationAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *IncidentRelation) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentRelationBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *IncidentRelation) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentRelationAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *IncidentRelation) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentRelationBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *IncidentRelation) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentRelationAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *IncidentRelation) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentRelationBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *IncidentRelation) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentRelationAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *IncidentRelation) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentRelationBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *IncidentRelation) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incidentRelationAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddIncidentRelationHook registers your hook function for all future operations.
func AddIncidentRelationHook(hookPoint boil.HookPoint, incidentRelationHook IncidentRelationHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		incidentRelationAfterSelectMu.Lock()
		incidentRelationAfterSelectHooks = append(incidentRelationAfterSelectHooks, incidentRelationHook)
		incidentRelationAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		incidentRelationBeforeInsertMu.Lock()
		incidentRelationBeforeInsertHooks = append(incidentRelationBeforeInsertHooks, incidentRelationHook)
		incidentRelationBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		incidentRelationAfterInsertMu.Lock()
		incidentRelationAfterInsertHooks = append(incidentRelationAfterInsertHooks, incidentRelationHook)
		incidentRelationAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		incidentRelationBeforeUpdateMu.Lock()
		incidentRelationBeforeUpdateHooks = append(incidentRelationBeforeUpdateHooks, incidentRelationHook)
		incidentRelationBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		incidentRelationAfterUpdateMu.Lock()
		incidentRelationAfterUpdateHooks = append(incidentRelationAfterUpdateHooks, incidentRelationHook)
		incidentRelationAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		incidentRelationBeforeDeleteMu.Lock()
		incidentRelationBeforeDeleteHooks = append(incidentRelationBeforeDeleteHooks, incidentRelationHook)
		incidentRelationBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		incidentRelationAfterDeleteMu.Lock()
		incidentRelationAfterDeleteHooks = append(incidentRelationAfterDeleteHooks, incidentRelationHook)
		incidentRelationAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		incidentRelationBeforeUpsertMu.Lock()
		incidentRelationBeforeUpsertHooks = append(incidentRelationBeforeUpsertHooks, incidentRelationHook)
		incidentRelationBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		incidentRelationAfterUpsertMu.Lock()
		incidentRelationAfterUpsertHooks = append(incidentRelationAfterUpsertHooks, incidentRelationHook)
		incidentRelationAfterUpsertMu.Unlock()
	}
}

// One returns a single incidentRelation record from the query.
func (q incidentRelationQuery) One(ctx context.Context, exec boil.ContextExecutor) (*IncidentRelation, error) {
	o := &IncidentRelation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for incident_relations")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all IncidentRelation records from the query.
func (q incidentRelationQuery) All(ctx context.Context, exec boil.ContextExecutor) (IncidentRelationSlice, error) {
	var o []*IncidentRelation

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to IncidentRelation slice")
	}

	if len(incidentRelationAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all IncidentRelation records in the query.
func (q incidentRelationQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count incident_relations rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q incidentRelationQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if incident_relations exists")
	}

	return count > 0, nil
}

// IncidentRelations retrieves all the records using an executor.
func IncidentRelations(mods ...qm.QueryMod) incidentRelationQuery {
	mods = append(mods, qm.From("\"incident_relations\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"incident_relations\".*"})
	}

	return incidentRelationQuery{q}
}

// FindIncidentRelation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindIncidentRelation(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*IncidentRelation, error) {
	incidentRelationObj := &IncidentRelation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"incident_relations\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, incidentRelationObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from incident_relations")
	}

	if err = incidentRelationObj.doAfterSelectHooks(ctx, exec); err != nil {
		return incidentRelationObj, err
	}

	return incidentRelationObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *IncidentRelation) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no incident_relations provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incidentRelationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	incidentRelationInsertCacheMut.RLock()
	cache, cached := incidentRelationInsertCache[key]
	incidentRelationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			incidentRelationAllColumns,
			incidentRelationColumnsWithDefault,
			incidentRelationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(incidentRelationType, incidentRelationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(incidentRelationType, incidentRelationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"incident_relations\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"incident_relations\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into incident_relations")
	}

	if !cached {
		incidentRelationInsertCacheMut.Lock()
		incidentRelationInsertCache[key] = cache
		incidentRelationInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the IncidentRelation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *IncidentRelation) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	incidentRelationUpdateCacheMut.RLock()
	cache, cached := incidentRelationUpdateCache[key]
	incidentRelationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			incidentRelationAllColumns,
			incidentRelationPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update incident_relations, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"incident_relations\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, incidentRelationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(incidentRelationType, incidentRelationMapping, append(wl, incidentRelationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update incident_relations row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for incident_relations")
	}

	if !cached {
		incidentRelationUpdateCacheMut.Lock()
		incidentRelationUpdateCache[key] = cache
		incidentRelationUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q incidentRelationQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for incident_relations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for incident_relations")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o IncidentRelationSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incidentRelationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"incident_relations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, incidentRelationPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in incidentRelation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all incidentRelation")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *IncidentRelation) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no incident_relations provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incidentRelationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	incidentRelationUpsertCacheMut.RLock()
	cache, cached := incidentRelationUpsertCache[key]
	incidentRelationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			incidentRelationAllColumns,
			incidentRelationColumnsWithDefault,
			incidentRelationColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			incidentRelationAllColumns,
			incidentRelationPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert incident_relations, could not build update column list")
		}

		ret := strmangle.SetComplement(incidentRelationAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(incidentRelationPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert incident_relations, could not build conflict column list")
			}

			conflict = make([]string, len(incidentRelationPrimaryKeyColumns))
			copy(conflict, incidentRelationPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"incident_relations\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(incidentRelationType, incidentRelationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(incidentRelationType, incidentRelationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert incident_relations")
	}

	if !cached {
		incidentRelationUpsertCacheMut.Lock()
		incidentRelationUpsertCache[key] = cache
		incidentRelationUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single IncidentRelation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *IncidentRelation) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no IncidentRelation provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), incidentRelationPrimaryKeyMapping)
	sql := "DELETE FROM \"incident_relations\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from incident_relations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for incident_relations")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q incidentRelationQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no incidentRelationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from incident_relations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for incident_relations")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o IncidentRelationSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(incidentRelationBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incidentRelationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"incident_relations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incidentRelationPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from incidentRelation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for incident_relations")
	}

	if len(incidentRelationAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *IncidentRelation) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindIncidentRelation(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *IncidentRelationSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := IncidentRelationSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incidentRelationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"incident_relations\".* FROM \"incident_relations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incidentRelationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in IncidentRelationSlice")
	}

	*o = slice

	return nil
}

// IncidentRelationExists checks if the IncidentRelation row exists.
func IncidentRelationExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"incident_relations\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if incident_relations exists")
	}

	return exists, nil
}

// Exists checks if the IncidentRelation row exists.
func (o *IncidentRelation) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return IncidentRelationExists(ctx, exec, o.ID)
}
