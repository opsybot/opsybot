// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// Workspace is an object representing the database table.
type Workspace struct {
	ID          string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	Slug        string      `boil:"slug" json:"slug" toml:"slug" yaml:"slug"`
	Name        string      `boil:"name" json:"name" toml:"name" yaml:"name"`
	Timezone    string      `boil:"timezone" json:"timezone" toml:"timezone" yaml:"timezone"`
	Environment string      `boil:"environment" json:"environment" toml:"environment" yaml:"environment"`
	CreatedBy   null.String `boil:"created_by" json:"created_by,omitempty" toml:"created_by" yaml:"created_by,omitempty"`
	CreatedAt   time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt   time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceColumns = struct {
	ID          string
	Slug        string
	Name        string
	Timezone    string
	Environment string
	CreatedBy   string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "id",
	Slug:        "slug",
	Name:        "name",
	Timezone:    "timezone",
	Environment: "environment",
	CreatedBy:   "created_by",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var WorkspaceTableColumns = struct {
	ID          string
	Slug        string
	Name        string
	Timezone    string
	Environment string
	CreatedBy   string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "workspaces.id",
	Slug:        "workspaces.slug",
	Name:        "workspaces.name",
	Timezone:    "workspaces.timezone",
	Environment: "workspaces.environment",
	CreatedBy:   "workspaces.created_by",
	CreatedAt:   "workspaces.created_at",
	UpdatedAt:   "workspaces.updated_at",
}

// Generated where

var WorkspaceWhere = struct {
	ID          whereHelperstring
	Slug        whereHelperstring
	Name        whereHelperstring
	Timezone    whereHelperstring
	Environment whereHelperstring
	CreatedBy   whereHelpernull_String
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpertime_Time
}{
	ID:          whereHelperstring{field: "\"workspaces\".\"id\""},
	Slug:        whereHelperstring{field: "\"workspaces\".\"slug\""},
	Name:        whereHelperstring{field: "\"workspaces\".\"name\""},
	Timezone:    whereHelperstring{field: "\"workspaces\".\"timezone\""},
	Environment: whereHelperstring{field: "\"workspaces\".\"environment\""},
	CreatedBy:   whereHelpernull_String{field: "\"workspaces\".\"created_by\""},
	CreatedAt:   whereHelpertime_Time{field: "\"workspaces\".\"created_at\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"workspaces\".\"updated_at\""},
}

// WorkspaceRels is where relationship names are stored.
var WorkspaceRels = struct {
	CreatedByUser         string
	AlertSetting          string
	SsoConnection         string
	AlertEscalations      string
	AlertGroupRules       string
	AlertIngestEvents     string
	AlertIngestFailures   string
	AlertMonitors         string
	AlertRoutes           string
	AlertSilences         string
	AlertSources          string
	Alerts                string
	APIKeys               string
	AuditEvents           string
	EscalationPolicies    string
	EscalationWebhooks    string
	Invites               string
	NotificationAttempts  string
	NotificationRuns      string
	Schedules             string
	Teams                 string
	UserNotificationRules string
	WorkspaceMembers      string
}{
	CreatedByUser:         "CreatedByUser",
	AlertSetting:          "AlertSetting",
	SsoConnection:         "SsoConnection",
	AlertEscalations:      "AlertEscalations",
	AlertGroupRules:       "AlertGroupRules",
	AlertIngestEvents:     "AlertIngestEvents",
	AlertIngestFailures:   "AlertIngestFailures",
	AlertMonitors:         "AlertMonitors",
	AlertRoutes:           "AlertRoutes",
	AlertSilences:         "AlertSilences",
	AlertSources:          "AlertSources",
	Alerts:                "Alerts",
	APIKeys:               "APIKeys",
	AuditEvents:           "AuditEvents",
	EscalationPolicies:    "EscalationPolicies",
	EscalationWebhooks:    "EscalationWebhooks",
	Invites:               "Invites",
	NotificationAttempts:  "NotificationAttempts",
	NotificationRuns:      "NotificationRuns",
	Schedules:             "Schedules",
	Teams:                 "Teams",
	UserNotificationRules: "UserNotificationRules",
	WorkspaceMembers:      "WorkspaceMembers",
}

// workspaceR is where relationships are stored.
type workspaceR struct {
	CreatedByUser         *User                     `boil:"CreatedByUser" json:"CreatedByUser" toml:"CreatedByUser" yaml:"CreatedByUser"`
	AlertSetting          *AlertSetting             `boil:"AlertSetting" json:"AlertSetting" toml:"AlertSetting" yaml:"AlertSetting"`
	SsoConnection         *SsoConnection            `boil:"SsoConnection" json:"SsoConnection" toml:"SsoConnection" yaml:"SsoConnection"`
	AlertEscalations      AlertEscalationSlice      `boil:"AlertEscalations" json:"AlertEscalations" toml:"AlertEscalations" yaml:"AlertEscalations"`
	AlertGroupRules       AlertGroupRuleSlice       `boil:"AlertGroupRules" json:"AlertGroupRules" toml:"AlertGroupRules" yaml:"AlertGroupRules"`
	AlertIngestEvents     AlertIngestEventSlice     `boil:"AlertIngestEvents" json:"AlertIngestEvents" toml:"AlertIngestEvents" yaml:"AlertIngestEvents"`
	AlertIngestFailures   AlertIngestFailureSlice   `boil:"AlertIngestFailures" json:"AlertIngestFailures" toml:"AlertIngestFailures" yaml:"AlertIngestFailures"`
	AlertMonitors         AlertMonitorSlice         `boil:"AlertMonitors" json:"AlertMonitors" toml:"AlertMonitors" yaml:"AlertMonitors"`
	AlertRoutes           AlertRouteSlice           `boil:"AlertRoutes" json:"AlertRoutes" toml:"AlertRoutes" yaml:"AlertRoutes"`
	AlertSilences         AlertSilenceSlice         `boil:"AlertSilences" json:"AlertSilences" toml:"AlertSilences" yaml:"AlertSilences"`
	AlertSources          AlertSourceSlice          `boil:"AlertSources" json:"AlertSources" toml:"AlertSources" yaml:"AlertSources"`
	Alerts                AlertSlice                `boil:"Alerts" json:"Alerts" toml:"Alerts" yaml:"Alerts"`
	APIKeys               APIKeySlice               `boil:"APIKeys" json:"APIKeys" toml:"APIKeys" yaml:"APIKeys"`
	AuditEvents           AuditEventSlice           `boil:"AuditEvents" json:"AuditEvents" toml:"AuditEvents" yaml:"AuditEvents"`
	EscalationPolicies    EscalationPolicySlice     `boil:"EscalationPolicies" json:"EscalationPolicies" toml:"EscalationPolicies" yaml:"EscalationPolicies"`
	EscalationWebhooks    EscalationWebhookSlice    `boil:"EscalationWebhooks" json:"EscalationWebhooks" toml:"EscalationWebhooks" yaml:"EscalationWebhooks"`
	Invites               InviteSlice               `boil:"Invites" json:"Invites" toml:"Invites" yaml:"Invites"`
	NotificationAttempts  NotificationAttemptSlice  `boil:"NotificationAttempts" json:"NotificationAttempts" toml:"NotificationAttempts" yaml:"NotificationAttempts"`
	NotificationRuns      NotificationRunSlice      `boil:"NotificationRuns" json:"NotificationRuns" toml:"NotificationRuns" yaml:"NotificationRuns"`
	Schedules             ScheduleSlice             `boil:"Schedules" json:"Schedules" toml:"Schedules" yaml:"Schedules"`
	Teams                 TeamSlice                 `boil:"Teams" json:"Teams" toml:"Teams" yaml:"Teams"`
	UserNotificationRules UserNotificationRuleSlice `boil:"UserNotificationRules" json:"UserNotificationRules" toml:"UserNotificationRules" yaml:"UserNotificationRules"`
	WorkspaceMembers      WorkspaceMemberSlice      `boil:"WorkspaceMembers" json:"WorkspaceMembers" toml:"WorkspaceMembers" yaml:"WorkspaceMembers"`
}

// NewStruct creates a new relationship struct
func (*workspaceR) NewStruct() *workspaceR {
	return &workspaceR{}
}

func (o *Workspace) GetCreatedByUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByUser()
}

func (r *workspaceR) GetCreatedByUser() *User {
	if r == nil {
		return nil
	}

	return r.CreatedByUser
}

func (o *Workspace) GetAlertSetting() *AlertSetting {
	if o == nil {
		return nil
	}

	return o.R.GetAlertSetting()
}

func (r *workspaceR) GetAlertSetting() *AlertSetting {
	if r == nil {
		return nil
	}

	return r.AlertSetting
}

func (o *Workspace) GetSsoConnection() *SsoConnection {
	if o == nil {
		return nil
	}

	return o.R.GetSsoConnection()
}

func (r *workspaceR) GetSsoConnection() *SsoConnection {
	if r == nil {
		return nil
	}

	return r.SsoConnection
}

func (o *Workspace) GetAlertEscalations() AlertEscalationSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertEscalations()
}

func (r *workspaceR) GetAlertEscalations() AlertEscalationSlice {
	if r == nil {
		return nil
	}

	return r.AlertEscalations
}

func (o *Workspace) GetAlertGroupRules() AlertGroupRuleSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertGroupRules()
}

func (r *workspaceR) GetAlertGroupRules() AlertGroupRuleSlice {
	if r == nil {
		return nil
	}

	return r.AlertGroupRules
}

func (o *Workspace) GetAlertIngestEvents() AlertIngestEventSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertIngestEvents()
}

func (r *workspaceR) GetAlertIngestEvents() AlertIngestEventSlice {
	if r == nil {
		return nil
	}

	return r.AlertIngestEvents
}

func (o *Workspace) GetAlertIngestFailures() AlertIngestFailureSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertIngestFailures()
}

func (r *workspaceR) GetAlertIngestFailures() AlertIngestFailureSlice {
	if r == nil {
		return nil
	}

	return r.AlertIngestFailures
}

func (o *Workspace) GetAlertMonitors() AlertMonitorSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertMonitors()
}

func (r *workspaceR) GetAlertMonitors() AlertMonitorSlice {
	if r == nil {
		return nil
	}

	return r.AlertMonitors
}

func (o *Workspace) GetAlertRoutes() AlertRouteSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertRoutes()
}

func (r *workspaceR) GetAlertRoutes() AlertRouteSlice {
	if r == nil {
		return nil
	}

	return r.AlertRoutes
}

func (o *Workspace) GetAlertSilences() AlertSilenceSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertSilences()
}

func (r *workspaceR) GetAlertSilences() AlertSilenceSlice {
	if r == nil {
		return nil
	}

	return r.AlertSilences
}

func (o *Workspace) GetAlertSources() AlertSourceSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlertSources()
}

func (r *workspaceR) GetAlertSources() AlertSourceSlice {
	if r == nil {
		return nil
	}

	return r.AlertSources
}

func (o *Workspace) GetAlerts() AlertSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAlerts()
}

func (r *workspaceR) GetAlerts() AlertSlice {
	if r == nil {
		return nil
	}

	return r.Alerts
}

func (o *Workspace) GetAPIKeys() APIKeySlice {
	if o == nil {
		return nil
	}

	return o.R.GetAPIKeys()
}

func (r *workspaceR) GetAPIKeys() APIKeySlice {
	if r == nil {
		return nil
	}

	return r.APIKeys
}

func (o *Workspace) GetAuditEvents() AuditEventSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAuditEvents()
}

func (r *workspaceR) GetAuditEvents() AuditEventSlice {
	if r == nil {
		return nil
	}

	return r.AuditEvents
}

func (o *Workspace) GetEscalationPolicies() EscalationPolicySlice {
	if o == nil {
		return nil
	}

	return o.R.GetEscalationPolicies()
}

func (r *workspaceR) GetEscalationPolicies() EscalationPolicySlice {
	if r == nil {
		return nil
	}

	return r.EscalationPolicies
}

func (o *Workspace) GetEscalationWebhooks() EscalationWebhookSlice {
	if o == nil {
		return nil
	}

	return o.R.GetEscalationWebhooks()
}

func (r *workspaceR) GetEscalationWebhooks() EscalationWebhookSlice {
	if r == nil {
		return nil
	}

	return r.EscalationWebhooks
}

func (o *Workspace) GetInvites() InviteSlice {
	if o == nil {
		return nil
	}

	return o.R.GetInvites()
}

func (r *workspaceR) GetInvites() InviteSlice {
	if r == nil {
		return nil
	}

	return r.Invites
}

func (o *Workspace) GetNotificationAttempts() NotificationAttemptSlice {
	if o == nil {
		return nil
	}

	return o.R.GetNotificationAttempts()
}

func (r *workspaceR) GetNotificationAttempts() NotificationAttemptSlice {
	if r == nil {
		return nil
	}

	return r.NotificationAttempts
}

func (o *Workspace) GetNotificationRuns() NotificationRunSlice {
	if o == nil {
		return nil
	}

	return o.R.GetNotificationRuns()
}

func (r *workspaceR) GetNotificationRuns() NotificationRunSlice {
	if r == nil {
		return nil
	}

	return r.NotificationRuns
}

func (o *Workspace) GetSchedules() ScheduleSlice {
	if o == nil {
		return nil
	}

	return o.R.GetSchedules()
}

func (r *workspaceR) GetSchedules() ScheduleSlice {
	if r == nil {
		return nil
	}

	return r.Schedules
}

func (o *Workspace) GetTeams() TeamSlice {
	if o == nil {
		return nil
	}

	return o.R.GetTeams()
}

func (r *workspaceR) GetTeams() TeamSlice {
	if r == nil {
		return nil
	}

	return r.Teams
}

func (o *Workspace) GetUserNotificationRules() UserNotificationRuleSlice {
	if o == nil {
		return nil
	}

	return o.R.GetUserNotificationRules()
}

func (r *workspaceR) GetUserNotificationRules() UserNotificationRuleSlice {
	if r == nil {
		return nil
	}

	return r.UserNotificationRules
}

func (o *Workspace) GetWorkspaceMembers() WorkspaceMemberSlice {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspaceMembers()
}

func (r *workspaceR) GetWorkspaceMembers() WorkspaceMemberSlice {
	if r == nil {
		return nil
	}

	return r.WorkspaceMembers
}

// workspaceL is where Load methods for each relationship are stored.
type workspaceL struct{}

var (
	workspaceAllColumns            = []string{"id", "slug", "name", "timezone", "environment", "created_by", "created_at", "updated_at"}
	workspaceColumnsWithoutDefault = []string{"slug", "name"}
	workspaceColumnsWithDefault    = []string{"id", "timezone", "environment", "created_by", "created_at", "updated_at"}
	workspacePrimaryKeyColumns     = []string{"id"}
	workspaceGeneratedColumns      = []string{}
)

type (
	// WorkspaceSlice is an alias for a slice of pointers to Workspace.
	// This should almost always be used instead of []Workspace.
	WorkspaceSlice []*Workspace
	// WorkspaceHook is the signature for custom Workspace hook methods
	WorkspaceHook func(context.Context, boil.ContextExecutor, *Workspace) error

	workspaceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceType                 = reflect.TypeOf(&Workspace{})
	workspaceMapping              = queries.MakeStructMapping(workspaceType)
	workspacePrimaryKeyMapping, _ = queries.BindMapping(workspaceType, workspaceMapping, workspacePrimaryKeyColumns)
	workspaceInsertCacheMut       sync.RWMutex
	workspaceInsertCache          = make(map[string]insertCache)
	workspaceUpdateCacheMut       sync.RWMutex
	workspaceUpdateCache          = make(map[string]updateCache)
	workspaceUpsertCacheMut       sync.RWMutex
	workspaceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceAfterSelectMu sync.Mutex
var workspaceAfterSelectHooks []WorkspaceHook

var workspaceBeforeInsertMu sync.Mutex
var workspaceBeforeInsertHooks []WorkspaceHook
var workspaceAfterInsertMu sync.Mutex
var workspaceAfterInsertHooks []WorkspaceHook

var workspaceBeforeUpdateMu sync.Mutex
var workspaceBeforeUpdateHooks []WorkspaceHook
var workspaceAfterUpdateMu sync.Mutex
var workspaceAfterUpdateHooks []WorkspaceHook

var workspaceBeforeDeleteMu sync.Mutex
var workspaceBeforeDeleteHooks []WorkspaceHook
var workspaceAfterDeleteMu sync.Mutex
var workspaceAfterDeleteHooks []WorkspaceHook

var workspaceBeforeUpsertMu sync.Mutex
var workspaceBeforeUpsertHooks []WorkspaceHook
var workspaceAfterUpsertMu sync.Mutex
var workspaceAfterUpsertHooks []WorkspaceHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Workspace) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Workspace) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Workspace) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Workspace) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Workspace) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Workspace) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Workspace) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Workspace) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Workspace) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceHook registers your hook function for all future operations.
func AddWorkspaceHook(hookPoint boil.HookPoint, workspaceHook WorkspaceHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceAfterSelectMu.Lock()
		workspaceAfterSelectHooks = append(workspaceAfterSelectHooks, workspaceHook)
		workspaceAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceBeforeInsertMu.Lock()
		workspaceBeforeInsertHooks = append(workspaceBeforeInsertHooks, workspaceHook)
		workspaceBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceAfterInsertMu.Lock()
		workspaceAfterInsertHooks = append(workspaceAfterInsertHooks, workspaceHook)
		workspaceAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceBeforeUpdateMu.Lock()
		workspaceBeforeUpdateHooks = append(workspaceBeforeUpdateHooks, workspaceHook)
		workspaceBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceAfterUpdateMu.Lock()
		workspaceAfterUpdateHooks = append(workspaceAfterUpdateHooks, workspaceHook)
		workspaceAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceBeforeDeleteMu.Lock()
		workspaceBeforeDeleteHooks = append(workspaceBeforeDeleteHooks, workspaceHook)
		workspaceBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceAfterDeleteMu.Lock()
		workspaceAfterDeleteHooks = append(workspaceAfterDeleteHooks, workspaceHook)
		workspaceAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceBeforeUpsertMu.Lock()
		workspaceBeforeUpsertHooks = append(workspaceBeforeUpsertHooks, workspaceHook)
		workspaceBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceAfterUpsertMu.Lock()
		workspaceAfterUpsertHooks = append(workspaceAfterUpsertHooks, workspaceHook)
		workspaceAfterUpsertMu.Unlock()
	}
}

// One returns a single workspace record from the query.
func (q workspaceQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Workspace, error) {
	o := &Workspace{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspaces")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Workspace records from the query.
func (q workspaceQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceSlice, error) {
	var o []*Workspace

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to Workspace slice")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Workspace records in the query.
func (q workspaceQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspaces rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspaces exists")
	}

	return count > 0, nil
}

// CreatedByUser pointed to by the foreign key.
func (o *Workspace) CreatedByUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CreatedBy),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// AlertSetting pointed to by the foreign key.
func (o *Workspace) AlertSetting(mods ...qm.QueryMod) alertSettingQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"workspace_id\" = ?", o.ID),
	}

	queryMods = append(queryMods, mods...)

	return AlertSettings(queryMods...)
}

// SsoConnection pointed to by the foreign key.
func (o *Workspace) SsoConnection(mods ...qm.QueryMod) ssoConnectionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"workspace_id\" = ?", o.ID),
	}

	queryMods = append(queryMods, mods...)

	return SsoConnections(queryMods...)
}

// AlertEscalations retrieves all the alert_escalation's AlertEscalations with an executor.
func (o *Workspace) AlertEscalations(mods ...qm.QueryMod) alertEscalationQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_escalations\".\"workspace_id\"=?", o.ID),
	)

	return AlertEscalations(queryMods...)
}

// AlertGroupRules retrieves all the alert_group_rule's AlertGroupRules with an executor.
func (o *Workspace) AlertGroupRules(mods ...qm.QueryMod) alertGroupRuleQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_group_rules\".\"workspace_id\"=?", o.ID),
	)

	return AlertGroupRules(queryMods...)
}

// AlertIngestEvents retrieves all the alert_ingest_event's AlertIngestEvents with an executor.
func (o *Workspace) AlertIngestEvents(mods ...qm.QueryMod) alertIngestEventQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_ingest_events\".\"workspace_id\"=?", o.ID),
	)

	return AlertIngestEvents(queryMods...)
}

// AlertIngestFailures retrieves all the alert_ingest_failure's AlertIngestFailures with an executor.
func (o *Workspace) AlertIngestFailures(mods ...qm.QueryMod) alertIngestFailureQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_ingest_failures\".\"workspace_id\"=?", o.ID),
	)

	return AlertIngestFailures(queryMods...)
}

// AlertMonitors retrieves all the alert_monitor's AlertMonitors with an executor.
func (o *Workspace) AlertMonitors(mods ...qm.QueryMod) alertMonitorQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_monitors\".\"workspace_id\"=?", o.ID),
	)

	return AlertMonitors(queryMods...)
}

// AlertRoutes retrieves all the alert_route's AlertRoutes with an executor.
func (o *Workspace) AlertRoutes(mods ...qm.QueryMod) alertRouteQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_routes\".\"workspace_id\"=?", o.ID),
	)

	return AlertRoutes(queryMods...)
}

// AlertSilences retrieves all the alert_silence's AlertSilences with an executor.
func (o *Workspace) AlertSilences(mods ...qm.QueryMod) alertSilenceQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_silences\".\"workspace_id\"=?", o.ID),
	)

	return AlertSilences(queryMods...)
}

// AlertSources retrieves all the alert_source's AlertSources with an executor.
func (o *Workspace) AlertSources(mods ...qm.QueryMod) alertSourceQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alert_sources\".\"workspace_id\"=?", o.ID),
	)

	return AlertSources(queryMods...)
}

// Alerts retrieves all the alert's Alerts with an executor.
func (o *Workspace) Alerts(mods ...qm.QueryMod) alertQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"alerts\".\"workspace_id\"=?", o.ID),
	)

	return Alerts(queryMods...)
}

// APIKeys retrieves all the api_key's APIKeys with an executor.
func (o *Workspace) APIKeys(mods ...qm.QueryMod) apiKeyQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"api_keys\".\"workspace_id\"=?", o.ID),
	)

	return APIKeys(queryMods...)
}

// AuditEvents retrieves all the audit_event's AuditEvents with an executor.
func (o *Workspace) AuditEvents(mods ...qm.QueryMod) auditEventQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"audit_events\".\"workspace_id\"=?", o.ID),
	)

	return AuditEvents(queryMods...)
}

// EscalationPolicies retrieves all the escalation_policy's EscalationPolicies with an executor.
func (o *Workspace) EscalationPolicies(mods ...qm.QueryMod) escalationPolicyQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"escalation_policies\".\"workspace_id\"=?", o.ID),
	)

	return EscalationPolicies(queryMods...)
}

// EscalationWebhooks retrieves all the escalation_webhook's EscalationWebhooks with an executor.
func (o *Workspace) EscalationWebhooks(mods ...qm.QueryMod) escalationWebhookQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"escalation_webhooks\".\"workspace_id\"=?", o.ID),
	)

	return EscalationWebhooks(queryMods...)
}

// Invites retrieves all the invite's Invites with an executor.
func (o *Workspace) Invites(mods ...qm.QueryMod) inviteQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"invites\".\"workspace_id\"=?", o.ID),
	)

	return Invites(queryMods...)
}

// NotificationAttempts retrieves all the notification_attempt's NotificationAttempts with an executor.
func (o *Workspace) NotificationAttempts(mods ...qm.QueryMod) notificationAttemptQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"notification_attempts\".\"workspace_id\"=?", o.ID),
	)

	return NotificationAttempts(queryMods...)
}

// NotificationRuns retrieves all the notification_run's NotificationRuns with an executor.
func (o *Workspace) NotificationRuns(mods ...qm.QueryMod) notificationRunQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"notification_runs\".\"workspace_id\"=?", o.ID),
	)

	return NotificationRuns(queryMods...)
}

// Schedules retrieves all the schedule's Schedules with an executor.
func (o *Workspace) Schedules(mods ...qm.QueryMod) scheduleQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"schedules\".\"workspace_id\"=?", o.ID),
	)

	return Schedules(queryMods...)
}

// Teams retrieves all the team's Teams with an executor.
func (o *Workspace) Teams(mods ...qm.QueryMod) teamQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"teams\".\"workspace_id\"=?", o.ID),
	)

	return Teams(queryMods...)
}

// UserNotificationRules retrieves all the user_notification_rule's UserNotificationRules with an executor.
func (o *Workspace) UserNotificationRules(mods ...qm.QueryMod) userNotificationRuleQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"user_notification_rules\".\"workspace_id\"=?", o.ID),
	)

	return UserNotificationRules(queryMods...)
}

// WorkspaceMembers retrieves all the workspace_member's WorkspaceMembers with an executor.
func (o *Workspace) WorkspaceMembers(mods ...qm.QueryMod) workspaceMemberQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_members\".\"workspace_id\"=?", o.ID),
	)

	return WorkspaceMembers(queryMods...)
}

// LoadCreatedByUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceL) LoadCreatedByUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		if !queries.IsNil(object.CreatedBy) {
			args[object.CreatedBy] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}

			if !queries.IsNil(obj.CreatedBy) {
				args[obj.CreatedBy] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CreatedByUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.CreatedByWorkspaces = append(foreign.R.CreatedByWorkspaces, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.CreatedBy, foreign.ID) {
				local.R.CreatedByUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.CreatedByWorkspaces = append(foreign.R.CreatedByWorkspaces, local)
				break
			}
		}
	}

	return nil
}

// LoadAlertSetting allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-1 relationship.
func (workspaceL) LoadAlertSetting(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}

			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_settings`),
		qm.WhereIn(`alert_settings.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AlertSetting")
	}

	var resultSlice []*AlertSetting
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AlertSetting")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for alert_settings")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_settings")
	}

	if len(alertSettingAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.AlertSetting = foreign
		if foreign.R == nil {
			foreign.R = &alertSettingR{}
		}
		foreign.R.Workspace = object
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ID == foreign.WorkspaceID {
				local.R.AlertSetting = foreign
				if foreign.R == nil {
					foreign.R = &alertSettingR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadSsoConnection allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-1 relationship.
func (workspaceL) LoadSsoConnection(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}

			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`sso_connections`),
		qm.WhereIn(`sso_connections.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load SsoConnection")
	}

	var resultSlice []*SsoConnection
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice SsoConnection")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for sso_connections")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for sso_connections")
	}

	if len(ssoConnectionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.SsoConnection = foreign
		if foreign.R == nil {
			foreign.R = &ssoConnectionR{}
		}
		foreign.R.Workspace = object
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ID == foreign.WorkspaceID {
				local.R.SsoConnection = foreign
				if foreign.R == nil {
					foreign.R = &ssoConnectionR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAlertEscalations allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAlertEscalations(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_escalations`),
		qm.WhereIn(`alert_escalations.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_escalations")
	}

	var resultSlice []*AlertEscalation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_escalations")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_escalations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_escalations")
	}

	if len(alertEscalationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertEscalations = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertEscalationR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.AlertEscalations = append(local.R.AlertEscalations, foreign)
				if foreign.R == nil {
					foreign.R = &alertEscalationR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAlertGroupRules allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAlertGroupRules(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_group_rules`),
		qm.WhereIn(`alert_group_rules.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_group_rules")
	}

	var resultSlice []*AlertGroupRule
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_group_rules")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_group_rules")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_group_rules")
	}

	if len(alertGroupRuleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertGroupRules = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertGroupRuleR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.AlertGroupRules = append(local.R.AlertGroupRules, foreign)
				if foreign.R == nil {
					foreign.R = &alertGroupRuleR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAlertIngestEvents allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAlertIngestEvents(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_ingest_events`),
		qm.WhereIn(`alert_ingest_events.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_ingest_events")
	}

	var resultSlice []*AlertIngestEvent
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_ingest_events")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_ingest_events")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_ingest_events")
	}

	if len(alertIngestEventAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertIngestEvents = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertIngestEventR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.AlertIngestEvents = append(local.R.AlertIngestEvents, foreign)
				if foreign.R == nil {
					foreign.R = &alertIngestEventR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAlertIngestFailures allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAlertIngestFailures(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_ingest_failures`),
		qm.WhereIn(`alert_ingest_failures.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_ingest_failures")
	}

	var resultSlice []*AlertIngestFailure
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_ingest_failures")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_ingest_failures")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_ingest_failures")
	}

	if len(alertIngestFailureAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertIngestFailures = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertIngestFailureR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.AlertIngestFailures = append(local.R.AlertIngestFailures, foreign)
				if foreign.R == nil {
					foreign.R = &alertIngestFailureR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAlertMonitors allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAlertMonitors(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_monitors`),
		qm.WhereIn(`alert_monitors.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_monitors")
	}

	var resultSlice []*AlertMonitor
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_monitors")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_monitors")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_monitors")
	}

	if len(alertMonitorAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertMonitors = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertMonitorR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.AlertMonitors = append(local.R.AlertMonitors, foreign)
				if foreign.R == nil {
					foreign.R = &alertMonitorR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAlertRoutes allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAlertRoutes(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_routes`),
		qm.WhereIn(`alert_routes.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_routes")
	}

	var resultSlice []*AlertRoute
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_routes")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_routes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_routes")
	}

	if len(alertRouteAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertRoutes = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertRouteR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.AlertRoutes = append(local.R.AlertRoutes, foreign)
				if foreign.R == nil {
					foreign.R = &alertRouteR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAlertSilences allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAlertSilences(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_silences`),
		qm.WhereIn(`alert_silences.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_silences")
	}

	var resultSlice []*AlertSilence
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_silences")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_silences")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_silences")
	}

	if len(alertSilenceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertSilences = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertSilenceR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.AlertSilences = append(local.R.AlertSilences, foreign)
				if foreign.R == nil {
					foreign.R = &alertSilenceR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAlertSources allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAlertSources(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alert_sources`),
		qm.WhereIn(`alert_sources.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alert_sources")
	}

	var resultSlice []*AlertSource
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alert_sources")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alert_sources")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alert_sources")
	}

	if len(alertSourceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AlertSources = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertSourceR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.AlertSources = append(local.R.AlertSources, foreign)
				if foreign.R == nil {
					foreign.R = &alertSourceR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAlerts allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAlerts(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alerts`),
		qm.WhereIn(`alerts.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load alerts")
	}

	var resultSlice []*Alert
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice alerts")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on alerts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alerts")
	}

	if len(alertAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.Alerts = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &alertR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.Alerts = append(local.R.Alerts, foreign)
				if foreign.R == nil {
					foreign.R = &alertR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAPIKeys allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAPIKeys(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`api_keys`),
		qm.WhereIn(`api_keys.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load api_keys")
	}

	var resultSlice []*APIKey
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice api_keys")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on api_keys")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for api_keys")
	}

	if len(apiKeyAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.APIKeys = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &apiKeyR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.APIKeys = append(local.R.APIKeys, foreign)
				if foreign.R == nil {
					foreign.R = &apiKeyR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadAuditEvents allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadAuditEvents(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`audit_events`),
		qm.WhereIn(`audit_events.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load audit_events")
	}

	var resultSlice []*AuditEvent
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice audit_events")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on audit_events")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for audit_events")
	}

	if len(auditEventAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AuditEvents = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &auditEventR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.WorkspaceID) {
				local.R.AuditEvents = append(local.R.AuditEvents, foreign)
				if foreign.R == nil {
					foreign.R = &auditEventR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadEscalationPolicies allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadEscalationPolicies(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`escalation_policies`),
		qm.WhereIn(`escalation_policies.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load escalation_policies")
	}

	var resultSlice []*EscalationPolicy
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice escalation_policies")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on escalation_policies")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for escalation_policies")
	}

	if len(escalationPolicyAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.EscalationPolicies = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &escalationPolicyR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.EscalationPolicies = append(local.R.EscalationPolicies, foreign)
				if foreign.R == nil {
					foreign.R = &escalationPolicyR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadEscalationWebhooks allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadEscalationWebhooks(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`escalation_webhooks`),
		qm.WhereIn(`escalation_webhooks.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load escalation_webhooks")
	}

	var resultSlice []*EscalationWebhook
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice escalation_webhooks")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on escalation_webhooks")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for escalation_webhooks")
	}

	if len(escalationWebhookAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.EscalationWebhooks = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &escalationWebhookR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.EscalationWebhooks = append(local.R.EscalationWebhooks, foreign)
				if foreign.R == nil {
					foreign.R = &escalationWebhookR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadInvites allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadInvites(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`invites`),
		qm.WhereIn(`invites.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load invites")
	}

	var resultSlice []*Invite
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice invites")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on invites")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for invites")
	}

	if len(inviteAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.Invites = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &inviteR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.Invites = append(local.R.Invites, foreign)
				if foreign.R == nil {
					foreign.R = &inviteR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadNotificationAttempts allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadNotificationAttempts(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`notification_attempts`),
		qm.WhereIn(`notification_attempts.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load notification_attempts")
	}

	var resultSlice []*NotificationAttempt
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice notification_attempts")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on notification_attempts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for notification_attempts")
	}

	if len(notificationAttemptAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.NotificationAttempts = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notificationAttemptR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.NotificationAttempts = append(local.R.NotificationAttempts, foreign)
				if foreign.R == nil {
					foreign.R = &notificationAttemptR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadNotificationRuns allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadNotificationRuns(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`notification_runs`),
		qm.WhereIn(`notification_runs.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load notification_runs")
	}

	var resultSlice []*NotificationRun
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice notification_runs")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on notification_runs")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for notification_runs")
	}

	if len(notificationRunAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.NotificationRuns = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notificationRunR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.NotificationRuns = append(local.R.NotificationRuns, foreign)
				if foreign.R == nil {
					foreign.R = &notificationRunR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadSchedules allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadSchedules(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`schedules`),
		qm.WhereIn(`schedules.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load schedules")
	}

	var resultSlice []*Schedule
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice schedules")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on schedules")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for schedules")
	}

	if len(scheduleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.Schedules = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &scheduleR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.Schedules = append(local.R.Schedules, foreign)
				if foreign.R == nil {
					foreign.R = &scheduleR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadTeams allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadTeams(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`teams`),
		qm.WhereIn(`teams.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load teams")
	}

	var resultSlice []*Team
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice teams")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on teams")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for teams")
	}

	if len(teamAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.Teams = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &teamR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.Teams = append(local.R.Teams, foreign)
				if foreign.R == nil {
					foreign.R = &teamR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadUserNotificationRules allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadUserNotificationRules(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`user_notification_rules`),
		qm.WhereIn(`user_notification_rules.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load user_notification_rules")
	}

	var resultSlice []*UserNotificationRule
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice user_notification_rules")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on user_notification_rules")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for user_notification_rules")
	}

	if len(userNotificationRuleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UserNotificationRules = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &userNotificationRuleR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.UserNotificationRules = append(local.R.UserNotificationRules, foreign)
				if foreign.R == nil {
					foreign.R = &userNotificationRuleR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// LoadWorkspaceMembers allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceL) LoadWorkspaceMembers(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspace any, mods queries.Applicator) error {
	var slice []*Workspace
	var object *Workspace

	if singular {
		var ok bool
		object, ok = maybeWorkspace.(*Workspace)
		if !ok {
			object = new(Workspace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspace))
			}
		}
	} else {
		s, ok := maybeWorkspace.(*[]*Workspace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspace))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_members`),
		qm.WhereIn(`workspace_members.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_members")
	}

	var resultSlice []*WorkspaceMember
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_members")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_members")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_members")
	}

	if len(workspaceMemberAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WorkspaceMembers = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceMemberR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorkspaceID {
				local.R.WorkspaceMembers = append(local.R.WorkspaceMembers, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceMemberR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// SetCreatedByUser of the workspace to the related item.
// Sets o.R.CreatedByUser to related.
// Adds o to related.R.CreatedByWorkspaces.
func (o *Workspace) SetCreatedByUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspaces\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
		strmangle.WhereClause("\"", "\"", 2, workspacePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.CreatedBy, related.ID)
	if o.R == nil {
		o.R = &workspaceR{
			CreatedByUser: related,
		}
	} else {
		o.R.CreatedByUser = related
	}

	if related.R == nil {
		related.R = &userR{
			CreatedByWorkspaces: WorkspaceSlice{o},
		}
	} else {
		related.R.CreatedByWorkspaces = append(related.R.CreatedByWorkspaces, o)
	}

	return nil
}

// RemoveCreatedByUser relationship.
// Sets o.R.CreatedByUser to nil.
// Removes o from all passed in related items' relationships struct.
func (o *Workspace) RemoveCreatedByUser(ctx context.Context, exec boil.ContextExecutor, related *User) error {
	var err error

	queries.SetScanner(&o.CreatedBy, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("created_by")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.CreatedByUser = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.CreatedByWorkspaces {
		if queries.Equal(o.CreatedBy, ri.CreatedBy) {
			continue
		}

		ln := len(related.R.CreatedByWorkspaces)
		if ln > 1 && i < ln-1 {
			related.R.CreatedByWorkspaces[i] = related.R.CreatedByWorkspaces[ln-1]
		}
		related.R.CreatedByWorkspaces = related.R.CreatedByWorkspaces[:ln-1]
		break
	}
	return nil
}

// SetAlertSetting of the workspace to the related item.
// Sets o.R.AlertSetting to related.
// Adds o to related.R.Workspace.
func (o *Workspace) SetAlertSetting(ctx context.Context, exec boil.ContextExecutor, insert bool, related *AlertSetting) error {
	var err error

	if insert {
		related.WorkspaceID = o.ID

		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	} else {
		updateQuery := fmt.Sprintf(
			"UPDATE \"alert_settings\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
			strmangle.WhereClause("\"", "\"", 2, alertSettingPrimaryKeyColumns),
		)
		values := []any{o.ID, related.WorkspaceID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, updateQuery)
			fmt.Fprintln(writer, values)
		}
		if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
			return errors.Wrap(err, "failed to update foreign table")
		}

		related.WorkspaceID = o.ID
	}

	if o.R == nil {
		o.R = &workspaceR{
			AlertSetting: related,
		}
	} else {
		o.R.AlertSetting = related
	}

	if related.R == nil {
		related.R = &alertSettingR{
			Workspace: o,
		}
	} else {
		related.R.Workspace = o
	}
	return nil
}

// SetSsoConnection of the workspace to the related item.
// Sets o.R.SsoConnection to related.
// Adds o to related.R.Workspace.
func (o *Workspace) SetSsoConnection(ctx context.Context, exec boil.ContextExecutor, insert bool, related *SsoConnection) error {
	var err error

	if insert {
		related.WorkspaceID = o.ID

		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	} else {
		updateQuery := fmt.Sprintf(
			"UPDATE \"sso_connections\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
			strmangle.WhereClause("\"", "\"", 2, ssoConnectionPrimaryKeyColumns),
		)
		values := []any{o.ID, related.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, updateQuery)
			fmt.Fprintln(writer, values)
		}
		if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
			return errors.Wrap(err, "failed to update foreign table")
		}

		related.WorkspaceID = o.ID
	}

	if o.R == nil {
		o.R = &workspaceR{
			SsoConnection: related,
		}
	} else {
		o.R.SsoConnection = related
	}

	if related.R == nil {
		related.R = &ssoConnectionR{
			Workspace: o,
		}
	} else {
		related.R.Workspace = o
	}
	return nil
}

// AddAlertEscalations adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.AlertEscalations.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAlertEscalations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertEscalation) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_escalations\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertEscalationPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			AlertEscalations: related,
		}
	} else {
		o.R.AlertEscalations = append(o.R.AlertEscalations, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertEscalationR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAlertGroupRules adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.AlertGroupRules.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAlertGroupRules(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertGroupRule) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_group_rules\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertGroupRulePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			AlertGroupRules: related,
		}
	} else {
		o.R.AlertGroupRules = append(o.R.AlertGroupRules, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertGroupRuleR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAlertIngestEvents adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.AlertIngestEvents.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAlertIngestEvents(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertIngestEvent) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_ingest_events\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertIngestEventPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			AlertIngestEvents: related,
		}
	} else {
		o.R.AlertIngestEvents = append(o.R.AlertIngestEvents, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertIngestEventR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAlertIngestFailures adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.AlertIngestFailures.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAlertIngestFailures(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertIngestFailure) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_ingest_failures\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertIngestFailurePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			AlertIngestFailures: related,
		}
	} else {
		o.R.AlertIngestFailures = append(o.R.AlertIngestFailures, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertIngestFailureR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAlertMonitors adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.AlertMonitors.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAlertMonitors(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertMonitor) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_monitors\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertMonitorPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			AlertMonitors: related,
		}
	} else {
		o.R.AlertMonitors = append(o.R.AlertMonitors, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertMonitorR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAlertRoutes adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.AlertRoutes.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAlertRoutes(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertRoute) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_routes\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertRoutePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			AlertRoutes: related,
		}
	} else {
		o.R.AlertRoutes = append(o.R.AlertRoutes, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertRouteR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAlertSilences adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.AlertSilences.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAlertSilences(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertSilence) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_silences\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertSilencePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			AlertSilences: related,
		}
	} else {
		o.R.AlertSilences = append(o.R.AlertSilences, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertSilenceR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAlertSources adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.AlertSources.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAlertSources(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AlertSource) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alert_sources\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertSourcePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			AlertSources: related,
		}
	} else {
		o.R.AlertSources = append(o.R.AlertSources, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertSourceR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAlerts adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.Alerts.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAlerts(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Alert) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"alerts\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, alertPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			Alerts: related,
		}
	} else {
		o.R.Alerts = append(o.R.Alerts, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &alertR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAPIKeys adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.APIKeys.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAPIKeys(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*APIKey) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"api_keys\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, apiKeyPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			APIKeys: related,
		}
	} else {
		o.R.APIKeys = append(o.R.APIKeys, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &apiKeyR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddAuditEvents adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.AuditEvents.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddAuditEvents(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AuditEvent) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.WorkspaceID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"audit_events\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, auditEventPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.WorkspaceID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			AuditEvents: related,
		}
	} else {
		o.R.AuditEvents = append(o.R.AuditEvents, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &auditEventR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// SetAuditEvents removes all previously related items of the
// workspace replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Workspace's AuditEvents accordingly.
// Replaces o.R.AuditEvents with related.
// Sets related.R.Workspace's AuditEvents accordingly.
func (o *Workspace) SetAuditEvents(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AuditEvent) error {
	query := "update \"audit_events\" set \"workspace_id\" = null where \"workspace_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.AuditEvents {
			queries.SetScanner(&rel.WorkspaceID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Workspace = nil
		}
		o.R.AuditEvents = nil
	}

	return o.AddAuditEvents(ctx, exec, insert, related...)
}

// RemoveAuditEvents relationships from objects passed in.
// Removes related items from R.AuditEvents (uses pointer comparison, removal does not keep order)
// Sets related.R.Workspace.
func (o *Workspace) RemoveAuditEvents(ctx context.Context, exec boil.ContextExecutor, related ...*AuditEvent) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.WorkspaceID, nil)
		if rel.R != nil {
			rel.R.Workspace = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("workspace_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.AuditEvents {
			if rel != ri {
				continue
			}

			ln := len(o.R.AuditEvents)
			if ln > 1 && i < ln-1 {
				o.R.AuditEvents[i] = o.R.AuditEvents[ln-1]
			}
			o.R.AuditEvents = o.R.AuditEvents[:ln-1]
			break
		}
	}

	return nil
}

// AddEscalationPolicies adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.EscalationPolicies.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddEscalationPolicies(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EscalationPolicy) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"escalation_policies\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, escalationPolicyPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			EscalationPolicies: related,
		}
	} else {
		o.R.EscalationPolicies = append(o.R.EscalationPolicies, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &escalationPolicyR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddEscalationWebhooks adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.EscalationWebhooks.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddEscalationWebhooks(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EscalationWebhook) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"escalation_webhooks\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, escalationWebhookPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			EscalationWebhooks: related,
		}
	} else {
		o.R.EscalationWebhooks = append(o.R.EscalationWebhooks, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &escalationWebhookR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddInvites adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.Invites.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddInvites(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Invite) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"invites\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, invitePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			Invites: related,
		}
	} else {
		o.R.Invites = append(o.R.Invites, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &inviteR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddNotificationAttempts adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.NotificationAttempts.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddNotificationAttempts(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotificationAttempt) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"notification_attempts\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, notificationAttemptPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			NotificationAttempts: related,
		}
	} else {
		o.R.NotificationAttempts = append(o.R.NotificationAttempts, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notificationAttemptR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddNotificationRuns adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.NotificationRuns.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddNotificationRuns(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotificationRun) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"notification_runs\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, notificationRunPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			NotificationRuns: related,
		}
	} else {
		o.R.NotificationRuns = append(o.R.NotificationRuns, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notificationRunR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddSchedules adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.Schedules.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddSchedules(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Schedule) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"schedules\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, schedulePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			Schedules: related,
		}
	} else {
		o.R.Schedules = append(o.R.Schedules, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &scheduleR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddTeams adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.Teams.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddTeams(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Team) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"teams\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, teamPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			Teams: related,
		}
	} else {
		o.R.Teams = append(o.R.Teams, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &teamR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddUserNotificationRules adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.UserNotificationRules.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddUserNotificationRules(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*UserNotificationRule) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"user_notification_rules\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, userNotificationRulePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			UserNotificationRules: related,
		}
	} else {
		o.R.UserNotificationRules = append(o.R.UserNotificationRules, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &userNotificationRuleR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// AddWorkspaceMembers adds the given related objects to the existing relationships
// of the workspace, optionally inserting them as new records.
// Appends related to o.R.WorkspaceMembers.
// Sets related.R.Workspace appropriately.
func (o *Workspace) AddWorkspaceMembers(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceMember) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_members\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceMemberPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.WorkspaceID, rel.UserID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceR{
			WorkspaceMembers: related,
		}
	} else {
		o.R.WorkspaceMembers = append(o.R.WorkspaceMembers, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceMemberR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// Workspaces retrieves all the records using an executor.
func Workspaces(mods ...qm.QueryMod) workspaceQuery {
	mods = append(mods, qm.From("\"workspaces\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspaces\".*"})
	}

	return workspaceQuery{q}
}

// FindWorkspace retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspace(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*Workspace, error) {
	workspaceObj := &Workspace{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspaces\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspaces")
	}

	if err = workspaceObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceObj, err
	}

	return workspaceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Workspace) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspaces provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceInsertCacheMut.RLock()
	cache, cached := workspaceInsertCache[key]
	workspaceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceAllColumns,
			workspaceColumnsWithDefault,
			workspaceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceType, workspaceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceType, workspaceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspaces\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspaces\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspaces")
	}

	if !cached {
		workspaceInsertCacheMut.Lock()
		workspaceInsertCache[key] = cache
		workspaceInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Workspace.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Workspace) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceUpdateCacheMut.RLock()
	cache, cached := workspaceUpdateCache[key]
	workspaceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceAllColumns,
			workspacePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspaces, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspaces\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspacePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceType, workspaceMapping, append(wl, workspacePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspaces row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspaces")
	}

	if !cached {
		workspaceUpdateCacheMut.Lock()
		workspaceUpdateCache[key] = cache
		workspaceUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspaces")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspaces")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspacePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspaces\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspacePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspace slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspace")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Workspace) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspaces provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceUpsertCacheMut.RLock()
	cache, cached := workspaceUpsertCache[key]
	workspaceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceAllColumns,
			workspaceColumnsWithDefault,
			workspaceColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceAllColumns,
			workspacePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspaces, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspacePrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspaces, could not build conflict column list")
			}

			conflict = make([]string, len(workspacePrimaryKeyColumns))
			copy(conflict, workspacePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspaces\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceType, workspaceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceType, workspaceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspaces")
	}

	if !cached {
		workspaceUpsertCacheMut.Lock()
		workspaceUpsertCache[key] = cache
		workspaceUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Workspace record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Workspace) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no Workspace provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspacePrimaryKeyMapping)
	sql := "DELETE FROM \"workspaces\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspaces")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspaces")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaces")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspaces")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspacePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspaces\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspacePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspaces")
	}

	if len(workspaceAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Workspace) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspace(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspacePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspaces\".* FROM \"workspaces\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspacePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceSlice")
	}

	*o = slice

	return nil
}

// WorkspaceExists checks if the Workspace row exists.
func WorkspaceExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspaces\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspaces exists")
	}

	return exists, nil
}

// Exists checks if the Workspace row exists.
func (o *Workspace) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceExists(ctx, exec, o.ID)
}
